channel_priority: strict
channels:
  # WIP add defaults againg to test whole pipeline. needs to be removed before final merge
  sources:
    - conda-forge
    - defaults
azure:
  settings_win:
    pool:
      vmImage: windows-2019
