min_py_ver: '37'
max_py_ver: '37'

github:
  user_or_org: michellab
  repo_name: "Sire"
  branch_name: devel

channels:
  # sources selects the channels to pull packages from, in order.
  sources:
    - conda-forge
    - defaults
  targets:
    - ["conda-forge", "dev"]
