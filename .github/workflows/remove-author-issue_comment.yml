  name: 'Remove awaiting author label'

  on:
    issue_comment:
      types: [created]

  jobs:
    label-pr:
      name: Label PR
      if: github.event.sender.login == github.event.issue.user.login && contains(github.event.issue.labels.*.name, 'Awaiting author contribution')
      runs-on: ubuntu-latest
      steps:
        - name: author-commented
          uses: actions/github-script@v6
          with:
            script: |
              github.rest.issues.removeLabel({
                  issue_number: context.issue.number,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  labels: ['Awaiting author contribution']
              })
