  name: 'Add awaiting author label on comment from review'

  on:
    issue_comment:
      types: [created]

  jobs:
    label-pr:
      name: Label PR
      if: github.event.sender.login != github.event.issue.user.login && contains(github.event.issue.labels.*.name, 'review-requested') && !contains(github.event.issue.labels.*.name, 'Awaiting author contribution')
      runs-on: ubuntu-latest
      steps:
        - name: author-commented
          uses: actions/github-script@v6
          with:
            script: |
              github.rest.teams.getMembershipForUserInOrg({
                  org: 'conda-forge,
                  team_slug: 'staged-recipes',
                  username: github.event.sender.login
              });

# Get if commenter is part of staged recipes
# If yes, change label
