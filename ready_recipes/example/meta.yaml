{% set version = "1.0" %}

package:
  name: example
  version: {{ version }}

source:
  git_url: https://github.com/conda-forge/staged-recipes.git
  git_tag: 24507f6c6b2d2e03caee550e13830df15d791751

build:
  number: 0

requirements:
  run:
    - python
  build:
    - python

test:
  imports:
    - example  # [not win]
  commands:
    - python -c "import example;"  # [not win]

about:
  home: https://github.com/conda-forge/staged-recipes
  license: BSD
  summary: An example package

extra:
  recipe-maintainers:
    - HomerSimpson
    - LukeSkywalker
