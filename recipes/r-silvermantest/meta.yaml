{% set version = '0.1.0' %}
{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-silvermantest
  version: {{ version|replace("-", "_") }}

source:
  git_url: https://github.com/jenzopr/silvermantest.git
  git_rev: 1d4200a4020c2c2fb30cd9ba869ee0c9bd4d51b8
  sha256: 8ad9e5da16e1e581ddcf515204ed490ad0f94085d827be440954dfdb39c414dc

build:
  merge_build_host: True  # [win]
  number: 0
  noarch: generic
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{posix}}zip               # [win]
  host:
    - r-base
  run:
    - r-base
    - r-boot

test:
  commands:
    - $R -e "library('silvermantest')"           # [not win]
    - "\"%R%\" -e \"library('silvermantest')\""  # [win]

about:
  home: https://github.com/jenzopr/silvermantest 
  license: GPL (>= 2)
  summary: This package contains the silverman.test, a test concerning the number of
    modes of an empirical density. Furthermore functions visualizing a number of tests
    (silverman.plot), plotting a number of densities for given number of modes
    (densities.plot), calculating a critical bandwidth (h.crit) and determining the number
    of modes for a given (empirical) density (nr.modes) are available. 
  license_family: GPL2
  license_file: '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-2'

extra:
  recipe-maintainers:
    - conda-forge/r
    - khughitt

# Package: silvermantest
# Version: 0.1.0
# Date: 2018-02-07
# Title: Package to investigate the number of modes 
# Author: Florian Schwaiger, Hajo Holzmann
# Maintainer: Florian Schwaiger <schwaige@mathematik.uni-marburg.de> 
# Depends: R, boot
# Imports: splines, methods, boot
# Suggests:
# Description: This package contains the silverman.test, a test concerning the number of modes of an empirical density. Furthermore functions visualizing a number of tests (silverman.plot), plotting a number of densities for given number of modes (densities.plot), calculating a critical bandwidth (h.crit) and determining the number of modes for a given (empirical) density (nr.modes) are available. 
# License: GPL (>= 3)
# URL: http://strimmerlab.org/software/silvermantest/
# Packaged: 2018-02-07
# NeedsCompilation: no
# Repository: Github
# Date/Publication: 2018-02-07
