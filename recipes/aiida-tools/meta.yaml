{% set name = "aiida-tools" %}
{% set version = "0.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/aiida-tools-{{ version }}.tar.gz
  sha256: 429cbcf77965aab2e8098981efc232ec91993877c63c7860e9f6b5eab7db550d

build:
  skip: true  # [win]
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python
  run:
    - aiida-core >=2.0.0,<3.0.0
    - python
    - pyyaml

test:
  imports:
    - aiida_tools
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://aiida-tools.readthedocs.io
  summary: Helper tools for developing AiiDA plugins.
  license: Apache-2.0
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - chrisjsewell
