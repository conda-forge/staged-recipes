--- makefile
+++ makefile
@@ -145,6 +145,6 @@
 unrar:	clean $(OBJECTS) $(UNRAR_OBJ)
 	@rm -f unrar
-	$(LINK) -o unrar $(LDFLAGS) $(OBJECTS) $(UNRAR_OBJ) $(LIBS)	
+	$(LINK) -o unrar $(LDFLAGS) -L. -lunrar $(UNRAR_OBJ) $(LIBS)	
 	$(STRIP) unrar
 
 sfx:	WHAT=SFX_MODULE
