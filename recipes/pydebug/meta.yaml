{% set version = "0.1.0" %}

package:
  name: pydebug
  version: {{ version }}

build:
  number: 0
  track_features:
    - pydebug

about:
  home: http://www.python.org/
  license: PSF
  summary: Tracks Python and its extension compiled with --with-pydebug

extra:
  recipe-maintainers:
    - dfroger
