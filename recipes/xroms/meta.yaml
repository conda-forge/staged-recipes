{% set name = "xroms" %}
{% set version = "0.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/xroms-{{ version }}.tar.gz
  sha256: 80238b79ecdd76e8b533336fce7ac014ebbf117f3eb1529539ec15f8295e5c2e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - setuptools >=42
    - wheel
    - setuptools-scm >=3.4
    - pip
  run:
    - python >=3.6
    - cf_xarray
    - dask-core
    - netcdf4
    - numba >=0.49
    - numpy
    - xarray
    - xgcm >=0.8.1

test:
  imports:
    - xroms
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/xoceanmodel/xroms
  summary: Package for working with ROMS output in xarray
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - kthyng
