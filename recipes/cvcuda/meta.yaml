{% set version = "0.16.0" %}

package:
  name: cvcuda
  version: {{ version }}

source:
  url: https://github.com/CVCUDA/CV-CUDA/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 49d74f5958d9ce613c12ed12019a4e69f547bb53ed61658e65a4b3972446661a
  patches:
    - patches/0001-python-dynamic-libstdcxx.patch
    - patches/0003-use-conda-forge-pybind11-dlpack.patch
    - patches/0002-use-dynamic-cuda-libs.patch

build:
  number: 0
  skip: True  # [not linux or cuda_compiler_version == "None"]
  string: cuda{{ cuda_compiler_version | replace('.', '') }}_py{{ CONDA_PY }}_h{{ PKG_HASH }}_{{ PKG_BUILDNUM }}
  missing_dso_whitelist:
    - "*/libcuda.so*"
  ignore_run_exports_from:
    - cuda-cudart-dev
  ignore_run_exports:
    - cuda-version

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }}
    - {{ stdlib('c') }}
    - cmake >=3.20.1
    - ninja
  host:
    - cuda-cudart-dev
    - cuda-version {{ cuda_compiler_version }}
    - dlpack  # for global pinnings
    - dlpack >=1.0
    - libcvcuda-dev {{ version }}
    - numpy >=1.23.5
    - pybind11  # for global pinnings
    - pybind11 >=2.13.1
    - python
  run:
    - {{ pin_compatible('cuda-version', max_pin='x', min_pin='x') }}
    - cuda-cudart
    - numpy >=1.23.5
    - python

test:
  commands:
    - python -c "import importlib.util; assert importlib.util.find_spec('cvcuda') is not None, 'cvcuda package not found'"

about:
  home: https://github.com/CVCUDA/CV-CUDA
  summary: 'An open-source, GPU accelerated library for cloud-scale image processing and computer vision'
  description: |
    CV-CUDA is an open-source library of GPU-accelerated computer vision algorithms designed
    for speed and scalability. It delivers high-throughput, low-latency image/video
    processing for AI pipelines across NVIDIA cloud, desktop, and edge platforms. CV-CUDA is
    built for performance and works seamlessly with C/C++ and Python Image and AI
    frameworks.

    This package provides the Python API.
  license: Apache-2.0 and MIT
  license_file:
    - LICENSE.md
    - THIRD_PARTY_LICENSES.md
  doc_url: https://cvcuda.github.io/CV-CUDA/
  dev_url: https://github.com/CVCUDA/CV-CUDA

extra:
  recipe-maintainers:
    - conda-forge/cuda
