# copied from elsewhere, commenting out anything I don't think we need, but ??
# we did need to pin the version  on osx
c_compiler:
#  - gcc                        # [linux]
  - clang                      # [osx]
#  - vs2019                     # [win and x86_64]
#  - vs2022                     # [win and arm64]
c_compiler_version:            # [unix]
#  - 12                         # [linux]
  - 15                         # [osx]
#  - 11                         # [os.environ.get("CF_CUDA_ENABLED", "False") == "True" and linux]
