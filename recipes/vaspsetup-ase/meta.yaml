{% set name = "vaspsetup-ase" %}
{% set version = "0.2.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/mappoh/vaspsetup-ase/archive/v{{ version }}.tar.gz
  sha256: 72d890cf8e2e31e365d5a01d0a4984488358deabaad1badee5e249b91263c548

build:
  number: 0
  skip: true  # [win or osx]
  script: |
    cargo build --release
    mkdir -p ${PREFIX}/bin
    cp target/release/vaspsetup ${PREFIX}/bin/
    ${PYTHON} -m pip install . --no-deps --no-build-isolation -vv

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - rust >=1.70
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - ase >=3.22
    - numpy >=1.24

test:
  commands:
    - vaspsetup --help || true
    - python -m vaspsetup_core -c '{"command":"version","args":{}}' || true

about:
  home: https://github.com/mappoh/vaspsetup-ase
  license: MIT
  license_file: LICENSE
  summary: Terminal UI for setting up and submitting VASP calculations to an SGE cluster
  dev_url: https://github.com/mappoh/vaspsetup-ase

extra:
  recipe-maintainers:
    - mappoh
