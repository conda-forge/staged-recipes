# Hot Mic uses macOS 14 APIs (MACOSX_DEPLOYMENT_TARGET = 14.0 in the Xcode
# project), so the package must require __osx >=14.0. This file replaces the
# whole c_stdlib_version list, hence the explicit osx condition.
c_stdlib_version:
  - if: osx
    then: "14.0"
