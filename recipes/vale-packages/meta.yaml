# this file is generated from meta.j2.yml with _update_recipe.py
{% set version = "0.2.0" %}

package:
  name: vale-packages-split
  version: {{ version }}

source:
  url: https://github.com/errata-ai/packages/releases/download/v{{ version }}.tar.gz
  sha256: f104122771979a5f0981206bd85a02754e9480e4eee55313571f99506f5a6013

build:
  noarch: python
  number: 0

requirements:
  host:
    - python >=3.10
    - jinja2

outputs:
  - name: vale-packages
    build:
      noarch: generic
    requirements:
      - {{ pin_subpackage("vale-package-alex", exact=True) }}
      - {{ pin_subpackage("vale-package-google", exact=True) }}
      - {{ pin_subpackage("vale-package-hugo", exact=True) }}
      - {{ pin_subpackage("vale-package-joblint", exact=True) }}
      - {{ pin_subpackage("vale-package-microsoft", exact=True) }}
      - {{ pin_subpackage("vale-package-proselint", exact=True) }}
      - {{ pin_subpackage("vale-package-readability", exact=True) }}
      - {{ pin_subpackage("vale-package-write-good", exact=True) }}
    test:
      source_files:
        - library.json
      files:
        - test_all.py
      requires:
        - pytest
      commands:
        - pytest -vv --color=yes
  
  - name: vale-package-alex
    build:
      noarch: generic
      script:
        - mkdir -p "{{ PREFIX }}/share/vale/styles"  # [unix]
        - cp -r "{{ SRC_DIR }}/alex" "{{ PREFIX }}/share/vale/styles/"  # [unix]
        - md "{{ PREFIX }}\share\vale\styles"  # [win]
        - copy "{{ SRC_DIR }}\alex" "{{ PREFIX }}\share\vale\styles\"  # [win]
    requirements:
      run:
        - vale >=1.0.0
    test:
      files:
        - test_vale_styles.py
      requires:
        - pytest
      commands:
        - ls "${CONDA_PREFIX}/share/vale/styles/alex"  # [unix]
        - dir "%CONDA_PREFIX%\share\vale\styles\alex"  # [win]
    about:
      home: https://github.com/errata-ai/alex
      license: MIT
      license_family: MIT
      license_file: LICENSE
      summary: |-
        Catch insensitive, inconsiderate writing.
      dev_url: https://github.com/errata-ai/packages
  - name: vale-package-google
    build:
      noarch: generic
      script:
        - mkdir -p "{{ PREFIX }}/share/vale/styles"  # [unix]
        - cp -r "{{ SRC_DIR }}/Google" "{{ PREFIX }}/share/vale/styles/"  # [unix]
        - md "{{ PREFIX }}\share\vale\styles"  # [win]
        - copy "{{ SRC_DIR }}\Google" "{{ PREFIX }}\share\vale\styles\"  # [win]
    requirements:
      run:
        - vale >=1.0.0
    test:
      files:
        - test_vale_styles.py
      requires:
        - pytest
      commands:
        - ls "${CONDA_PREFIX}/share/vale/styles/Google"  # [unix]
        - dir "%CONDA_PREFIX%\share\vale\styles\Google"  # [win]
    about:
      home: https://github.com/errata-ai/Google
      license: MIT
      license_family: MIT
      license_file: LICENSE
      summary: |-
        A Vale-compatible implementation of the Google Developer Documentation Style Guide.
      dev_url: https://github.com/errata-ai/packages
  - name: vale-package-hugo
    build:
      noarch: generic
      script:
        - mkdir -p "{{ PREFIX }}/share/vale/styles"  # [unix]
        - cp -r "{{ SRC_DIR }}/Hugo" "{{ PREFIX }}/share/vale/styles/"  # [unix]
        - md "{{ PREFIX }}\share\vale\styles"  # [win]
        - copy "{{ SRC_DIR }}\Hugo" "{{ PREFIX }}\share\vale\styles\"  # [win]
    requirements:
      run:
        - vale  # no version available from meta.json
    test:
      files:
        - test_vale_styles.py
      requires:
        - pytest
      commands:
        - ls "${CONDA_PREFIX}/share/vale/styles/Hugo"  # [unix]
        - dir "%CONDA_PREFIX%\share\vale\styles\Hugo"  # [win]
    about:
      home: https://github.com/errata-ai/Hugo
      license: MIT
      license_family: MIT
      license_file: LICENSE
      summary: |-
        Adds support for Hugo shortcodes and other non-standard markup.
      dev_url: https://github.com/errata-ai/packages
  - name: vale-package-joblint
    build:
      noarch: generic
      script:
        - mkdir -p "{{ PREFIX }}/share/vale/styles"  # [unix]
        - cp -r "{{ SRC_DIR }}/Joblint" "{{ PREFIX }}/share/vale/styles/"  # [unix]
        - md "{{ PREFIX }}\share\vale\styles"  # [win]
        - copy "{{ SRC_DIR }}\Joblint" "{{ PREFIX }}\share\vale\styles\"  # [win]
    requirements:
      run:
        - vale >=1.0.0
    test:
      files:
        - test_vale_styles.py
      requires:
        - pytest
      commands:
        - ls "${CONDA_PREFIX}/share/vale/styles/Joblint"  # [unix]
        - dir "%CONDA_PREFIX%\share\vale\styles\Joblint"  # [win]
    about:
      home: https://github.com/errata-ai/Joblint
      license: MIT
      license_family: MIT
      license_file: LICENSE
      summary: |-
        Test tech job posts for issues with sexism, culture, expectations, and recruiter fails.
      dev_url: https://github.com/errata-ai/packages
  - name: vale-package-microsoft
    build:
      noarch: generic
      script:
        - mkdir -p "{{ PREFIX }}/share/vale/styles"  # [unix]
        - cp -r "{{ SRC_DIR }}/Microsoft" "{{ PREFIX }}/share/vale/styles/"  # [unix]
        - md "{{ PREFIX }}\share\vale\styles"  # [win]
        - copy "{{ SRC_DIR }}\Microsoft" "{{ PREFIX }}\share\vale\styles\"  # [win]
    requirements:
      run:
        - vale >=1.0.0
    test:
      files:
        - test_vale_styles.py
      requires:
        - pytest
      commands:
        - ls "${CONDA_PREFIX}/share/vale/styles/Microsoft"  # [unix]
        - dir "%CONDA_PREFIX%\share\vale\styles\Microsoft"  # [win]
    about:
      home: https://github.com/errata-ai/Microsoft
      license: MIT
      license_family: MIT
      license_file: LICENSE
      summary: |-
        A Vale-compatible implementation of the Microsoft Writing Style Guide.
      dev_url: https://github.com/errata-ai/packages
  - name: vale-package-proselint
    build:
      noarch: generic
      script:
        - mkdir -p "{{ PREFIX }}/share/vale/styles"  # [unix]
        - cp -r "{{ SRC_DIR }}/proselint" "{{ PREFIX }}/share/vale/styles/"  # [unix]
        - md "{{ PREFIX }}\share\vale\styles"  # [win]
        - copy "{{ SRC_DIR }}\proselint" "{{ PREFIX }}\share\vale\styles\"  # [win]
    requirements:
      run:
        - vale >=1.0.0
    test:
      files:
        - test_vale_styles.py
      requires:
        - pytest
      commands:
        - ls "${CONDA_PREFIX}/share/vale/styles/proselint"  # [unix]
        - dir "%CONDA_PREFIX%\share\vale\styles\proselint"  # [win]
    about:
      home: https://github.com/errata-ai/proselint
      license: MIT
      license_family: MIT
      license_file: LICENSE
      summary: |-
        proselint places the world’s greatest writers and editors by your side.
      dev_url: https://github.com/errata-ai/packages
  - name: vale-package-readability
    build:
      noarch: generic
      script:
        - mkdir -p "{{ PREFIX }}/share/vale/styles"  # [unix]
        - cp -r "{{ SRC_DIR }}/Readability" "{{ PREFIX }}/share/vale/styles/"  # [unix]
        - md "{{ PREFIX }}\share\vale\styles"  # [win]
        - copy "{{ SRC_DIR }}\Readability" "{{ PREFIX }}\share\vale\styles\"  # [win]
    requirements:
      run:
        - vale >=2.13.0
    test:
      files:
        - test_vale_styles.py
      requires:
        - pytest
      commands:
        - ls "${CONDA_PREFIX}/share/vale/styles/Readability"  # [unix]
        - dir "%CONDA_PREFIX%\share\vale\styles\Readability"  # [win]
    about:
      home: https://github.com/errata-ai/Readability
      license: MIT
      license_family: MIT
      license_file: LICENSE
      summary: |-
        Vale-compatible implementations of many popular readability metrics.
      dev_url: https://github.com/errata-ai/packages
  - name: vale-package-write-good
    build:
      noarch: generic
      script:
        - mkdir -p "{{ PREFIX }}/share/vale/styles"  # [unix]
        - cp -r "{{ SRC_DIR }}/write-good" "{{ PREFIX }}/share/vale/styles/"  # [unix]
        - md "{{ PREFIX }}\share\vale\styles"  # [win]
        - copy "{{ SRC_DIR }}\write-good" "{{ PREFIX }}\share\vale\styles\"  # [win]
    requirements:
      run:
        - vale >=1.0.0
    test:
      files:
        - test_vale_styles.py
      requires:
        - pytest
      commands:
        - ls "${CONDA_PREFIX}/share/vale/styles/write-good"  # [unix]
        - dir "%CONDA_PREFIX%\share\vale\styles\write-good"  # [win]
    about:
      home: https://github.com/errata-ai/write-good
      license: MIT
      license_family: MIT
      license_file: LICENSE
      summary: |-
        Naive linter for English prose for developers who can't write good.
      dev_url: https://github.com/errata-ai/packages

about:
  home: https://vale.sh/hub
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE
  summary: A collection of pre-packaged, Vale-compatible style guides and configurations.
  description: This repository contains a library of all officially supported styles for Vale.
  dev_url: https://github.com/errata-ai/packages

extra:
  feedstock-name: vale-packages
  recipe-maintainers:
    - bollwyvl
