schema_version: 1

context:
  version: "0.96"

package:
  name: practrand
  version: ${{ version }}

source:
  url: https://downloads.sourceforge.net/project/pracrand/PractRand_${{ version }}.zip
  sha256: e4caf7fda98b2c597bbda3b576753cf5a0f6047aab837c82be370ab798a672e1
  patches:
    # PractRand 0.96 ships with CRLF line endings.
    - fix-non-x86-portability.patch

build:
  number: 0
  skip:
    - not linux

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
  host:
    - testu01

tests:
  - package_contents:
      bin:
        - RNG_test
        - RNG_output
        - RNG_benchmark
        - RNG_to_TestU01
  - script:
      - RNG_test jsf64 -tlmax 1MB
      - RNG_output jsf64 1KB > /dev/null
      - RNG_to_TestU01

about:
  homepage: https://pracrand.sourceforge.net/
  license: CC0-1.0
  license_file: doc/license.txt
  summary: Statistical tests and pseudo-random number generators
  description: |
    PractRand provides command-line tools for testing, generating, and
    benchmarking random number streams. This package installs RNG_test,
    RNG_output, RNG_benchmark, and RNG_to_TestU01 for running PractRand
    generators through TestU01 batteries.
  documentation: https://pracrand.sourceforge.net/

extra:
  recipe-maintainers:
    - mikemhenry
