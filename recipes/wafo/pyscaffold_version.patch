--- pywafo-0.3.1.orig/setup.py	2017-01-15 23:19:41.000000000 -0200
+++ pywafo-0.3.1/setup.py	2022-04-06 16:03:27.358995372 -0300
@@ -98,7 +98,7 @@
 
     needs_sphinx = {'build_sphinx', 'upload_docs'}.intersection(sys.argv)
     sphinx = ['sphinx'] if needs_sphinx else []
-    setup(setup_requires=['six', 'pyscaffold>=2.4rc1,<2.5a0'] + sphinx,
+    setup(setup_requires=['six', 'pyscaffold'] + sphinx,
           tests_require=['pytest_cov', 'pytest'],
           use_pyscaffold=True,
           **config.todict())
