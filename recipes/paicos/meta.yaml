{% set name = "paicos" %}
{% set version = "0.1.12" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/paicos-{{ version }}.tar.gz
  sha256: ce29e503ec6d3e876a0481e94afd46470195288fb0f0628cc10031be48418e78

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - cython >=0.29.21
    - numpy >=1.20
    - setuptools
    - wheel
    - pip
  run:
    - python >=3.8
    - scipy
    - numpy
    - h5py
    - astropy
    - numba
    - matplotlib-base

test:
  imports:
    - paicos
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/tberlok/paicos
  summary: An object-oriented Python package for analysis of (cosmological) simulations performed with Arepo.
  license: GPL-3.0
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - tberlok
