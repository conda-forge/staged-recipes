{% set name = "yaqd-fakes" %}
{% set version = "2020.6.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 538180d6520d1ea756ae4f0aaa1e5363dd70fbc0993119783ac22e5feba01b2c

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - flit
    - python >=3.7
    - yaqd-core >=2020.05.1
  run:
    - python >=3.7
    - yaqd-core >=2020.05.1

test:
  imports:
    - yaqd_fakes
  commands:
    - pip check
    - yaqd-fake-continuous-hardware --help
    - yaqd-fake-discrete-hardware --help
  requires:
    - pip

about:
  home: https://gitlab.com/yaq/yaqd-fakes
  summary: fake yaq daemons, for testing purposes
  license: LGPL-3.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - untzag
    - ksunden 
