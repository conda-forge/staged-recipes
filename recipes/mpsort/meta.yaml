package:
  name: mpsort
  version: "0.1.11"

source:
  fn: mpsort-0.1.11.tar.gz
  url: https://pypi.python.org/packages/9c/16/ee804075ec17dd703a94d315c23d4407adc93f03adcf8c2bc6b65be25da6/mpsort-0.1.11.tar.gz
  md5: 3fa8143df1ced536d217bdd68d76d676
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpsort = mpsort:main
    #
    # Would create an entry point called mpsort that calls mpsort.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cython
    - numpy
    - mpi4py

  run:
    - python
    - cython
    - numpy
    - mpi4py

test:
  # Python imports
  imports:
    - mpsort
    - mpsort.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rainwoodman/mpsort
  license: BSD-2-Clause
  summary: 'python binding of MP-sort, a peta scale sorting routine'
  license_family: BSD

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
