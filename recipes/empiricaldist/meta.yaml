{% set name = "empiricaldist" %}
{% set version = "0.6.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/empiricaldist-{{ version }}.tar.gz
  sha256: f7b07f5877bf5317a763e84fb5e13e06ffda66aca49e35a857d793aae68dd052

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python
    - pip
  run:
    - python

test:
  imports:
    - empiricaldist
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://github.com/AllenDowney/empiricaldist
  summary: Python library that represents empirical distributions.
  license: GD
  license_file: PLEASE_ADD_LICENSE_FILE

extra:
  recipe-maintainers:
    - jdblischak
