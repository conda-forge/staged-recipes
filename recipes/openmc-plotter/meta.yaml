{% set name = "openmc-plotter" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/openmc-dev/plotter/archive/refs/tags/v{{ version }}.tar.gz
  sha256: f54644486eed1b87ae4959a464bbce0a55f625508ac99afd2b5606c7312811e8

build:
  entry_points:
    - openmc-plotter=openmc_plotter.__main__:main
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  skip: true  # [win]

requirements:
  host:
    - pip
    - python
  run:
    - matplotlib-base
    - numpy
    - openmc >0.12.2
    - pyside2
    - python  >=3.6
    - ipython

test:
  imports:
    - openmc_plotter
  source_files:
    - tests
    - pytest.ini
  commands:
    - pip check
    - export QT_QPA_PLATFORM=offscreen
    - pytest -v tests -W ignore::DeprecationWarning
  requires:
    - pip
    - pytest-qt

about:
  home: https://github.com/openmc-dev/plotter
  summary: Plotting tool for OpenMC models and tally data
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - kkiesling
    - paulromano
    - pshriwise
