{% set name = "boot-clj" %}
{% set version="2.7.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}
 



source:
  url: https://github.com/boot-clj/boot/releases/download/2.7.2/boot.jar
  sha256: 00c0d7b9ac39a68eb75fc69c1fffaeb52c839486515633d267f29e967813cfb6                   

build:
  number: 0
  skip: false  # [win]
  # the distributed binaries are already relocatable
  #binary_relocation: False

requirements:
#  build:
#    - java 
    # When setuptools is available add the `--single-version-externally-managed --record record.txt` above.
  run:
    - openjdk


test:
  commands:
    - boot -help
    
about:
  home: https://github.com/boot-clj/boot
  # Remember to specify the license variants for BSD, Apache, GPL, and LGLP.
  # Prefer the short version, e.g: GPL-2.0 instead of GNU General Public License version 2.0
  # See https://opensource.org/licenses/alphabetical
  license: OTHER
  # The license_family, i.e. "BSD" if license is "BSD-3-Clause". (optional)
  license_family: OTHER
  # It is strongly encouraged to include a license file in the package,
  # (even if the license doesn't require it) using the license_file entry.
  # See http://conda.pydata.org/docs/building/meta-yaml.html#license-file
#  license_file: LICENSE
  summary: 'Build tooling for Clojure.'

  # The remaining entries in this section are optional, but recommended
  description: |
    Build tooling for Clojure. 
  doc_url: http://boot-clj.com/
  dev_url: https://github.com/boot-clj/boot

extra:
  recipe-maintainers:
    # GitHub IDs for maintainers of the recipe.
    # Always check with the people listed below if they are OK becoming maintainers of the recipe. (There will be spam!)
    - abhi18av
