package:
  name: meson
  version: "0.43.0"

source:
  fn: meson-0.43.0.tar.gz
  url: https://pypi.python.org/packages/8f/2d/b82ad8015f68a6a261b458acc07b0252fbb0170d508844bce7fc95793a25/meson-0.43.0.tar.gz
  md5: 0d14c3b83f2674731d2aea324a841153
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch: python
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meson = meson:main
    #
    # Would create an entry point called meson that calls meson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mesonbuild
    - mesonbuild.backend
    - mesonbuild.compilers
    - mesonbuild.dependencies
    - mesonbuild.modules
    - mesonbuild.scripts
    - mesonbuild.wrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mesonbuild.com
  license: Apache Software License
  summary: 'A high performance build system'
  license_family: APACHE

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
