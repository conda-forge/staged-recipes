context:
  name: claude-agent-sdk
  version: "0.1.17"

package:
  name: claude-agent-sdk
  version: ${{ version }}

source:
  url: https://github.com/anthropics/claude-agent-sdk-python/archive/refs/tags/v${{ version }}.tar.gz
  sha256: eed1cdc8d903c643ca6da333953cae18d1dc5ff332dc2c19990a0a5b95e3059d

build:
  noarch: python
  script: ${{ PYTHON }} -m pip install . --ignore-installed --no-cache-dir --no-deps -vv
  number: 0

requirements:
  host:
    - hatchling
    - pip
    - python ${{ python_min }}.*
  run:
    - anyio >=4.0.0
    - mcp >=0.1.0
    - python >=${{ python_min }}
    - typing-extensions >=4.0.0

tests:
  - python:
      imports:
      - claude_agent_sdk
      pip_check: true

  - script: |
      python -m pytest tests/ -v
    requirements:
      run:
        - python ${{ python_min }}.*
        - pytest
        - pytest-asyncio >=0.20.0
        - trio
    files:
      source:
        - CHANGELOG.md
        - tests/

about:
  homepage: https://github.com/anthropics/claude-agent-sdk-python
  summary: 'Python SDK for Claude Agent.'
  description: Python SDK for Claude Agent.
  license: MIT
  license_file: LICENSE
  documentation: https://platform.claude.com/docs/en/agent-sdk/python
  repository: https://github.com/anthropics/claude-agent-sdk-python

extra:
  recipe-maintainers:
    - MementoRC
