{% set name = "rdatasets" %}
{% set version = "0.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 14dfeac32a5025f82f923f357484e25c443428e473a0675432df397e3327d318

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - pandas

test:
  imports:
    - rdatasets
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/holgern/pyRdatasets
  summary: 'provides over 2264 datasets as pandas dataframe from various R packages'
  license: GPL-3.0
  license_file: LICENSE
  dev_url: https://github.com/holgern/pyRdatasets

extra:
  recipe-maintainers:
    - holgern
