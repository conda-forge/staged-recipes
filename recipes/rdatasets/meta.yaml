{% set name = "rdatasets" %}
{% set version = "0.2.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: a33f2724815a1e310190c766eaca7c63fd97e266c404416d43ff7bc28d33a0e3

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
    - pandas >=1.0.0
  run:
    - python >=3.8
    - pandas >=1.0.0

test:
  imports:
    - rdatasets
  requires:
    - pip
    - pytest
  commands:
    - pip check

about:
  home: https://github.com/holgern/pyRdatasets
  summary: 'provides over 2264 datasets as pandas dataframe from various R packages'
  license: GPL-3.0-only
  license_family: GPL3
  license_file: LICENSE
  dev_url: https://github.com/holgern/pyRdatasets

extra:
  recipe-maintainers:
    - holgern
