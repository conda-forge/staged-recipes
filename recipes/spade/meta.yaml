{% set name = "spade" %}
{% set version = "0.4.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/lanl-aea/spade/archive/refs/tags/{{ version }}.tar.gz
  sha256: cabdc78e301bcb1358a82352f5ebbc7192801098fec45d8c4179d73172c3ccc4
  # Use command: curl -sL <URL above> | openssl sha256

build:
  number: 0
  noarch: python
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
  script:
    - scons install --prefix=${PREFIX}  # [unix]
    - scons install --prefix=%PREFIX%  # [win]
  entry_points:
    - spade = spade._main:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - python-build
    - scons >=4.6
    - setuptools >=77
    - setuptools_scm >=8
    - sphinx >=7.1
    - sphinx-argparse
    - sphinx-book-theme
    - sphinx-copybutton >=0.5.1
    - sphinx-design
    - sphinxcontrib-bibtex
  run:
    - python >={{ python_min }}
    - scons >=4.6
    - cxx-compiler  # [linux or win]
    - hdf5  # [linux or win]
    - __linux  # [linux]
    - __glibc >=2.28,<3  # [linux]
    - __win  # [win]
    - getopt-win32  # [win]
    - vs2022_win-64  # [win]
    - __osx  # [osx]

test:
  requires:
    - pytest
    - pytest-xdist
  imports:
    - spade

about:
  home: https://github.com/lanl-aea/spade
  summary: 'Tool for extracting data from an Abaqus output database (odb) file into a hierarchical data format (hdf5) file'
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - Prabhu-LANL
    - kbrindley
