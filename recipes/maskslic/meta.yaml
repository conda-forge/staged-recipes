{% set name = "maskslic" %}
{% set version = "0.3.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/maskslic-{{ version }}.tar.gz
  sha256: 2bcfc5521ab6e1302f7c532606954ba9273b737cf0adc9e2eba8ebfdc2d22266

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - cython
    - pip
    - python
  run:
    - cython
    - numpy >=1.7.2
    - python
    - scikit-image
    - scikit-learn
    - scipy >=0.9.0

test:
  imports:
    - maskslic

about:
  home: https://www.birving.com
  summary: Simple linear iterative clustering (SLIC) in a region of interest (ROI)
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - mcraig-ibme
