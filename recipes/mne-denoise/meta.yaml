{% set version = "0.0.1" %}

package:
  name: mne-denoise
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/m/mne-denoise/mne_denoise-{{ version }}.tar.gz
  sha256: e889979cf832b278d4fd34bcb52fc4d2852e4bc6a770bebe8d008747429e2dfe

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=64
    - wheel
    - pip
  run:
    - python >=3.10
    - mne >=1.0
    - numpy >=1.22
    - scipy >=1.8
    - matplotlib-base >=3.5
    - scikit-learn >=1.1

test:
  imports:
    - build
    - dist
    - docs
    - examples
    - mne_denoise
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/mne-tools/mne-denoise
  summary: Denoising Source Separation (DSS) and ZapLine algorithms for MNE-Python.
  doc_url: https://mne-tools.github.io/mne-denoise/
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - BabaSanfour
