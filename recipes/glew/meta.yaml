{% set name = "glew" %}
{% set version = "2.0.0" %}
{% set sha256 = "65c8b7c2bfe922b4a3175d1c15a6ef3af6d0fdf0a12f8bdeb19f9df9fee680dc" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn:  {{ name }}-{{ version }}.tgz
  url: http://downloads.sourceforge.net/project/{{ name }}/{{ name }}/{{ version }}/{{ name }}-{{ version}}.tgz
  sha256: {{ sha256 }}

build:
  number: 0
  features:
    - vc9  # [win and py27]
    - vc10  # [win and py34]
    - vc14  # [win and py35]

requirements:
  build:
    - cmake
    - perl
    - wget
    
about:
  home: http://glew.sourceforge.net/
  license: BSD
  license_file: LICENSE.txt
  summary: "The OpenGL Extension Wrangler Library"
  description: |
    The OpenGL Extension Wrangler Library (GLEW) is a cross-platform open-source C/C++ extension loading library. GLEW provides efficient run-time mechanisms for determining which OpenGL extensions are supported on the target platform. OpenGL core and extension functionality is exposed in a single header file. GLEW has been tested on a variety of operating systems, including Windows, Linux, Mac OS X, FreeBSD, Irix, and Solaris.
  doc_url: http://glew.sourceforge.net/basic.html
  dev_url: https://github.com/nigels-com/glew

extra:
  recipe-maintainers:
    - dschreij