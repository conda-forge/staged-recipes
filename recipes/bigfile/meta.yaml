package:
  name: bigfile
  version: "0.1.23"

source:
  fn: bigfile-0.1.23.tar.gz
  url: https://pypi.python.org/packages/be/5f/c762c1d543bb3150d5ba32a7669828adf1b6c93106d6ad681b14f981f1f4/bigfile-0.1.23.tar.gz
  md5: b13f76c3fb10acdc8bef89ab1fa7872a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigfile = bigfile:main
    #
    # Would create an entry point called bigfile that calls bigfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cython
    - numpy
    - mpi4py

  run:
    - python
    - cython
    - numpy
    - mpi4py

test:
  # Python imports
  imports:
    - bigfile
    - bigfile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rainwoodman/bigfile
  license: BSD-2-Clause
  summary: 'python binding of BigFile, a peta scale IO format'
  license_family: BSD

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
