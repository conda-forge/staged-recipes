{% set name = "ukbparse" %}
{% set version = "0.10.0" %}
{% set sha256 = "f51b807b1ead30ce966fdbc190a2ca3fa8cfff14edf545292cefca3adead0677" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 1
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"
  entry_points:
    - ukbparse = ukbparse.main:main

requirements:
  build:
    - python
    - pip
  run:
    - python >=3.5
    - h5py
    - numpy
    - pandas
    - pyparsing
    - six
    - tables

test:
  requires:
    - coverage
    - pytest
    - pytest-cov

  commands:
    - pytest -v --cov=ukbparse ukbparse/tests


about:
  home: https://git.fmrib.ox.ac.uk/fsl/ukbparse/
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: 'The FSL Python library'
  dev_url: https://git.fmrib.ox.ac.uk/fsl/ukbparse/

extra:
  recipe-maintainers:
    - pauldmccarthy
