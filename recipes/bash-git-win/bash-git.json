{
    "menu_name": "Anaconda${PY_VER} ${PLATFORM}",
    "menu_items":
    [
    {
        "name": "Bash (Git)",
        "script": "${PREFIX}/Library/bash-git/bin/bash.exe",
        "scriptarguments": ["--login", "-i", "--"],
        "workdir": "${PERSONALDIR}",
        "icon": "${MENU_DIR}/bash-git.ico"
    }
    ]
}
