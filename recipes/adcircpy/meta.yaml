{% set name = "adcircpy" %}
{% set version = "1.1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/adcircpy-{{ version }}.tar.gz
  sha256: ed1cdb27f751344266c732ade3326d568597719f6fba1d285f243b11f1345f82

build:
  entry_points:
    - tidal_run = adcircpy.cmd.tidal_run:main
    - best_track_run = adcircpy.cmd.best_track_run:main
    - best_track_file = adcircpy.cmd.best_track_file:main
    - plot_mesh = adcircpy.cmd.plot_mesh:main
    - plot_maxele = adcircpy.cmd.plot_maxele:main
    - plot_fort61 = adcircpy.cmd.plot_fort61:main
    - fort63 = adcircpy.cmd.fort63:main
    - tide_gen = adcircpy.cmd.tide_gen:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - dunamai
    - pip
    - python >=3.6
    - setuptools >=3.6
  run:
    - appdirs
    - geopandas
    - haversine
    - matplotlib-base
    - netcdf4
    - numpy
    - pandas
    - paramiko
    - psutil
    - pyproj >=2.6
    - python >=3.6
    - requests
    - scipy
    - shapely
    - stormevents >=1.2
    - utm

test:
  imports:
    - adcircpy
  commands:
    - pip check
    - tidal_run --help
    - best_track_run --help
    - best_track_file --help
    - plot_mesh --help
    - plot_maxele --help
    - plot_fort61 --help
    - fort63 --help
    - tide_gen --help
  requires:
    - pip

about:
  home: https://github.com/noaa-ocs-modeling/adcircpy.git
  summary: Python package for working with ADCIRC input and output files
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - zacharyburnettNOAA
