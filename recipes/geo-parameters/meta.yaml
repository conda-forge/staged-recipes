{% set name = "geo-parameters" %}
{% set version = "0.2.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/geo_parameters-{{ version }}.tar.gz
  sha256: 52b456c44066eae0a36d41daf252038c068ca4bc07b0fff2e1bf552cbf4bff64

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python
    - pip
  run:
    - python
    - numpy
    - pint

test:
  imports:
    - geo_parameters
  commands:
    - pip check
  requires:
    - pip

about:
  summary: Metadata of geophysical (especially wave) parameters
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - bjorkqvi
