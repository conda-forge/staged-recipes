From: Roland Rosenfeld <roland@debian.org>
Date: Sun, 10 May 2020 20:43:18 +0200
Subject: Fix some typos in man pages.


--- a/glimpse.1
+++ b/glimpse.1
@@ -433,7 +433,7 @@ If the file name is a directory name, th
 .B \-R \fIk\fP
 defines the maximum size (in bytes) of a record.
 The maximum value (which is the default) is 48K.
-Defining the maximum to be lower than the deafult may speed
+Defining the maximum to be lower than the default may speed
 up some searches.
 .TP
 .B \-s
@@ -897,7 +897,7 @@ and '*' (the Kleene closure).
 So, "glimpse ab.de" will match abcde, but "glimpse ab\\.de"
 will not, and "glimpse ab*de" will not match ab*de, but
 "glimpse ab\\*de" will.
-The meta character - is translated automatically to a hypen
+The meta character - is translated automatically to a hyphen
 unless it appears between [] (in which case it denotes a range of
 characters).
 .LP
--- a/glimpseindex.1
+++ b/glimpseindex.1
@@ -259,7 +259,7 @@ in runtime, so changing them can be done
 This is not really an option in the regular sense;  rather,
 it is a program by itself,
 and it is meant as a post-processing step.
-(Avaliable only from version 3.6.)
+(Available only from version 3.6.)
 .TP
 .B \-s
 supports structured queries.  This option was added to support the
