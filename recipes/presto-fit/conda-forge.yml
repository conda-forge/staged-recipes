# Only build/test the noarch package on Linux and macOS (the __unix run
# dependency keeps it unsatisfiable on Windows, but CI still attempts Windows
# unless it is removed from the build matrix here).
noarch_platforms:
  - linux_64
  - osx_64
