make libs
cp *.py ${SP_DIR}
cp *.so ${SP_DIR}
cp ns_analyse ${PREFIX}/bin
cp ns_run ${PREFIX}/bin
cp ns_profile_analyse ${PREFIX}/bin
cp ns_process_traj ${PREFIX}/bin
cp ns_T_dependent_analysis ${PREFIX}/bin
cp ns_set_analysis_weights ${PREFIX}/bin
