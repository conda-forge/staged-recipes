{% set version = '3.1.1' %}
{% set posix = 'm2-' if win else '' %}

package:
  name: r-dggridr
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/dggridR_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/dggridR/dggridR_{{ version }}.tar.gz
  sha256: 0a648e32353d599e36616cb952ca4da925bf48316c3ac85ca451f470bc3157da

build:
  number: 0
  rpaths:
    - lib/R/lib/
    - lib/
  missing_dso_whitelist:
    - '*/R.dll'        # [win]
    - '*/Rblas.dll'    # [win]
    - '*/Rlapack.dll'  # [win]

requirements:
  build:
    - {{ compiler('c') }}              # [not win]
    - {{ compiler('m2w64_c') }}        # [win]
    - {{ compiler('cxx') }}            # [not win]
    - {{ compiler('m2w64_cxx') }}      # [win]
    - {{ posix }}filesystem        # [win]
    - {{ posix }}make
    - {{ posix }}sed               # [win]
    - {{ posix }}coreutils         # [win]
    - {{ posix }}zip               # [win]
    - cross-r-base {{ r_base }}    # [build_platform != target_platform]
  host:
    - r-base
    - r-rcpp >=0.12.12
    - r-collapse >=1.8.0
    - r-s2 >=1.1
    - r-sf >=1.0
  run:
    - r-base
    - r-rcpp >=0.12.12
    - r-collapse >=1.8.0
    - r-s2 >=1.1
    - r-sf >=1.0

test:
  commands:
    - $R -e "library('dggridR')"           # [not win]
    - "\"%R%\" -e \"library('dggridR')\""  # [win]

about:
  home: https://github.com/r-barnes/dggridR/
  license: AGPL-3
  summary: Spatial analyses involving binning require that every bin have the same area, but
    this is impossible using a rectangular grid laid over the Earth or over any projection
    of the Earth. Discrete global grids use hexagons, triangles, and diamonds to overcome
    this issue, overlaying the Earth with equally-sized bins. This package provides
    utilities for working with discrete global grids, along with utilities to aid in
    plotting such data.
  license_family: AGPL
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/AGPL-3'

extra:
  recipe-maintainers:
    - conda-forge/r
    - SebKrantz

# Package: dggridR
# Type: Package
# Title: Discrete Global Grids
# Version: 3.1.1
# Date: 2026-01-18
# NeedsCompilation: yes
# Depends: R (>= 4.1.0)
# Imports: Rcpp (>= 0.12.12), collapse (>= 1.8.0), sf (>= 1.0), s2 (>= 1.1)
# LinkingTo: Rcpp
# RcppModules: dgfuncs, gridgens, gridstats
# Suggests: ggplot2, knitr, rmarkdown, dplyr, maps, R.rsp, testthat
# License: AGPL (>= 3)
# Authors@R: c(person("Richard", "Barnes",    role = "aut", email = "rijard.barnes@gmail.com"), person("Kevin",   "Sahr",      role = c("aut", "cph"), email = "sahrk@sou.edu"), person("Gerald",  "Evenden",   role = "cph"), person("Angus",   "Johnson",   role = "cph"), person("Frank",   "Warmerdam", role = "cph"), person("Even",    "Rouault",   role = "cph"), person("Lian",    "Song",      role = "ctb"), person("Edzer",   "Pebesma",   role = "ctb"), person("Sebastian", "Krantz",  role = c("ctb", "cre"), email = "sebastian.krantz@graduateinstitute.ch") )
# Description: Spatial analyses involving binning require that every bin have the same area, but this is impossible using a rectangular grid laid over the Earth or over any projection of the Earth. Discrete global grids use hexagons, triangles, and diamonds to overcome this issue, overlaying the Earth with equally-sized bins. This package provides utilities for working with discrete global grids, along with utilities to aid in plotting such data.
# URL: https://github.com/r-barnes/dggridR/
# BugReports: https://github.com/r-barnes/dggridR/issues
# RoxygenNote: 7.3.2
# Encoding: UTF-8
# VignetteBuilder: knitr, R.rsp
# Language: en-US
# Packaged: 2026-01-19 06:13:03 UTC; sebastiankrantz
# Author: Richard Barnes [aut], Kevin Sahr [aut, cph], Gerald Evenden [cph], Angus Johnson [cph], Frank Warmerdam [cph], Even Rouault [cph], Lian Song [ctb], Edzer Pebesma [ctb], Sebastian Krantz [ctb, cre]
# Maintainer: Sebastian Krantz <sebastian.krantz@graduateinstitute.ch>
# Repository: CRAN
# Date/Publication: 2026-01-20 06:10:19 UTC
