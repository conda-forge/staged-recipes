{% set name = "fleximod" %}
{% set version = "0.1.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/fleximod-{{ version }}.tar.gz
  sha256: b1c3d8f933b07b8a47fd0210ea8b91cd2c3c15b1548a4387dead81a66b38f948

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - gitpython

test:
  imports:
    - fleximod
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/jedwards4b/git-fleximod
  summary: Extended support for git-submodule and git-sparse-checkout
  license: MIT
  license_file: License

extra:
  recipe-maintainers:
    - jedwards4b
