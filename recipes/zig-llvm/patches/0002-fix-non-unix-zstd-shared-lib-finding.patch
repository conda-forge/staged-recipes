--- a/llvm/cmake/modules/Findzstd.cmake
+++ b/llvm/cmake/modules/Findzstd.cmake
@@ -47,6 +47,8 @@ if(zstd_FOUND)
       set_target_properties(zstd::libzstd_shared PROPERTIES
           INTERFACE_INCLUDE_DIRECTORIES "${zstd_INCLUDE_DIR}"
           IMPORTED_LOCATION "${zstd_DIRNAME}/${zstd_BASENAME}"
           IMPORTED_IMPLIB "${zstd_LIBRARY}")
+      # Set IMPORTED_IMPLIB for Release configuration explicitly (fixes "IMPORTED_IMPLIB not set for Release" error)
+      set_property(TARGET zstd::libzstd_shared PROPERTY IMPORTED_IMPLIB_RELEASE "${zstd_LIBRARY}")
       unset(zstd_DIRNAME)
       unset(zstd_BASENAME)
