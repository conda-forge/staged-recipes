package:
  name: vaex-core
  version: "0.1.0"

source:
  fn: vaex-core-0.1.0.tar.gz
  url: https://pypi.python.org/packages/e9/14/38728e8a2e7cb613663a2ab3d7fa7865d169fac89ad0e8282d853c2b9961/vaex-core-0.1.0.tar.gz
  md5: 4134baf9cee9c576772d10020053cd64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch: python
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vaex-core = vaex-core:main
    #
    # Would create an entry point called vaex-core that calls vaex-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.11
    - scipy
    - astropy >=1
    - aplus
    - futures >=2.2.0
    - future >=0.15.2
    - pyyaml
    - progressbar2
    - psutil >=1.2.1
    - requests
    - six

  run:
    - python
    - numpy >=1.11
    - scipy
    - astropy >=1
    - aplus
    - futures >=2.2.0
    - future >=0.15.2
    - pyyaml
    - progressbar2
    - psutil >=1.2.1
    - requests
    - six

test:
  # Python imports
  imports:
    - vaex
    - vaex.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/maartenbreddels/vaex
  license: MIT
  summary: 'Core of vaex'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
