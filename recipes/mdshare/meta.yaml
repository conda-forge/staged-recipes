{% set name = "mdshare" %}
{% set version = "0.2.1" %}
{% set sha256 = "0e163c603f231f5f0d636696532d77a45c2485e7d0c5bf9df27f64b8d63ad9fa" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  #noarch: python
  number: 0
  skip: True  # [py2k]
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip
    - setuptools
    - setuptools_scm
    - setuptools_scm_git_archive
  run:
    - python
    - setuptools

test:
  imports:
    - mdshare
    - mdshare.test

about:
  home: http://github.com/markovmodel/mdshare
  license: LGPL-3.0
  license_family: LGPL
  license_file: LICENSE
  summary: 'Easy access to shared MD data.'

extra:
  recipe-maintainers:
    - cwehmeyer
