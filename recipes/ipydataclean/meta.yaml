{% set name = "ipydataclean" %}
{% set version = "0.2.2" %}
{% set sha256 = "2f2b350b954fb6dbbe4356fc2b2496f917313e899937cd1e6436853ee9ebea1d" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - pip
    - jupyter
  run:
    - python
    - pandas
    - numpy
    - matplotlib
    - ipywidgets >=7.0.0
    - scipy
    - funcsigs  # [py2k]
    - scikit-learn
    - ipython
    - future
    - boltzmannclean

test:
  imports:
    - dataclean

about:
  home: https://faculty.ai
  license: Apache-2.0
  license_family: Apache
  license_file: '{{ environ["RECIPE_DIR"] }}/LICENSE'
  summary: 'Interactive cleaning for pandas DataFrames'
  description: |
    A Jupyter plugin for interactive cleaning of pandas DataFrames.

extra:
  recipe-maintainers:
    - shwinnn
    - zblz
    - pbugnion
