{% set name = "pyleak" %}
{% set version = "0.1.14" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pyleak-{{ version }}.tar.gz
  sha256: 51be2741542fdaa6024c31a1b8212e59485e2a91e7fdc34c151d488e9a6fb3c4

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python 3.9
    - hatchling
    - pip
  run:
    - python >=3.9
    - exceptiongroup >=1.3.0

test:
  imports:
    - pyleak
  commands:
    - pip check
  requires:
    - pip
    - python 3.9

about:
  home: https://github.com/deepankarm/pyleak
  summary: Detect leaked asyncio tasks, threads, and event loop blocking in Python. Inspired by Go's goleak
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - bgianfo
