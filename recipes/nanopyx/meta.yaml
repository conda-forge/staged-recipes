{% set name = "nanopyx" %}
{% set version = "0.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/nanopyx-{{ version }}.tar.gz
  sha256: 6633f17fe73444258edb5a1198a1321cd3d9d0c5704ed4ce23800a4fa39c5c0b

build:
  skip: true  # [py<39]
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - cython >=0.29.0
    - numpy >=1.19.0
    - pip
    - python
    - python-build >=0.10.0
    - setuptools >=42
    - wheel >=0.38.4
  run:
    - cython >=0.29.32
    - gdown >=4.6.0
    - lmfit >=1.1.0
    - matplotlib-base >=3.5
    - mastodon.py >=1.8.0
    - {{ pin_compatible('numpy') }}
    - onedrivedownloader >=1.1.3
    - opencv >=4.7.0.68
    - pandas >=1.5.3
    - pyopencl >=2022.3.1
    - python
    - pyyaml >=6.0
    - scikit-image >=0.19.2
    - scikit-learn >=1.1.0
    - scipy >=1.8
    - statsmodels >=0.14.0
    - tifffile >=2022.5.4
    - tqdm >=4.64.1

test:
  imports:
    - nanopyx
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/nanopyx/
  summary: Nanoscopy Python library (NanoPyx, the successor to NanoJ) - focused on light microscopy and super-resolution imaging
  license: EPL-2.0
  license_file: LICENSE.txt
  dev_url: https://github.com/HenriquesLab/NanoPyx

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
