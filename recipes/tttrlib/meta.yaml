{% set name = "tttrlib" %}
{% set version = "0.24.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/Fluorescence-Tools/tttrlib/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 4adf53e1304276a6f9c846f7e2381eb8df726f0f0eaa5da8ae201082f134de08

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - llvm-openmp        # [osx]
    - libgomp            # [linux]
    - pkg-config         # [not win]
    - cmake
    - ninja
    - make               # [linux]
    - doxygen
    - swig
    - python
    - numpy
  host:
    - boost-cpp
    - hdf5
    - python
    - setuptools
    - numpy
  run:
    - python
    - tqdm
    - click
    - click-didyoumean
    - {{ pin_compatible('numpy') }}
    - scikit-image
    - matplotlib
    - hdf5

test:
  imports:
    - tttrlib
  commands:
    - tttrlib --help

about:
  home: https://github.com/fluorescence-tools/tttrlib
  summary: 'A file format agnostic library for time-resolved imaging and spectroscopic data'
  description: |
    tttrlib is a simple, fast, libray to read, write and process
    time-resolved imaging and spectroscopic data. For speed, it 
    is written in C++ and wrapped for Python via SWIG.
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  doc_url: https://tttrlib.readthedocs.io/
  dev_url: https://github.com/fluorescence-tools/tttrlib

extra:
  recipe-maintainers:
    - tpeulen
