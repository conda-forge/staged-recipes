{% set name = "kronprod" %}
{% set version = "1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/Jbwasse2/{{ name }}/archive/{{ version }}.tar.gz
  sha256: 7c03a2e2be7c877e9ae8c84f9e4ddb949e5e9adf2d8160a3ab9f9b9134f65e19 

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python
    - pip
    - numpy
    - scipy
  run:
    - python

test:
  imports:
    - kronprod

about:
  home: https://github.com/Jbwasse2/kronprod 
  license: MIT
  license_family: MIT
  license_file: LICENSE.md
  summary: 'Helpful functions for using the properties of the Kronecker product, also has a memory efficient dot product.'

  dev_url: https://github.com/Jbwasse2/kronprod

extra:
  recipe-maintainers:
    - Jbwasse2
