schema_version: 1

context:
  version: "0.11.0"

package:
  name: nxswriter
  version: ${{ version }}

source:
  url: https://github.com/nexdatas/nxsblisswriter/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 4598b7d5ebdcbde3890d2b11a9deffc1d71e0a5ede3922f286e4fd83723ea258

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=61
    - pip
    - wheel
    - numpy
  run:
    - python >=${{ python_min }}
    - numpy
    - argcomplete
    - lxml
    - pytango
    - pytz
    - pninexus
    - blissdata

tests:
  - python:
      imports:
        - nxsblisswriter
      python_version:
        - ${{ python_min }}.*
        - "*"
  - script:
      - if: unix
        then:
          - test -x "$PREFIX/bin/NXSBlissWriter"
          - NXSBlissWriter --help
    requirements:
      run:
        - python ${{ python_min }}.*


about:
  homepage: https://nexdatas.github.io/nxsblisswriter
  repository: https://nexdatas.github.io/nxsblisswriter
  license: GPL-3.0-or-later
  license_file: COPYRIGHT
  summary: NeXuS BlissData Writer Server
  description: |
    NeXuS BlissData Writer Server is a Tango Server which allows to write NeXus file
    from (meta)data stored in BlissData by NXSDataWriter tango server

extra:
  recipe-maintainers:
    - jkotan
