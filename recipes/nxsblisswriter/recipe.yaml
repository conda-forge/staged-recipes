schema_version: 1

context:
  version: "0.12.1"

package:
  name: nxsblisswriter
  version: ${{ version }}

source:
  url: https://github.com/nexdatas/nxsblisswriter/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 37f02f45808d5ff0d0cff54a29dd44ac0f234258256b9a3238581276ff959fe9

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=61
    - pip
    - wheel
  run:
    - python >=${{ python_min }}
    - numpy
    - lxml
    - pytango
    - pninexus
    - blissdata

tests:
  - python:
      imports:
        - nxsblisswriter
      python_version:
        - ${{ python_min }}.*
        - "*"
  - script:
      - if: unix
        then:
          - test -x "$PREFIX/bin/NXSBlissWriter"
          - NXSBlissWriter --help
    requirements:
      run:
        - python ${{ python_min }}.*


about:
  homepage: https://nexdatas.github.io/nxsblisswriter 
  repository: https://github.com/nexdatas/nxsblisswriter
  license: GPL-3.0-or-later
  license_file: COPYRIGHT
  summary: NeXuS BlissData Writer Server
  description: |
    NeXuS BlissData Writer Server is a Tango Server which allows to write NeXus file
    from (meta)data stored in BlissData by NXSDataWriter tango server

extra:
  recipe-maintainers:
    - jkotan
