{% set name = "resnest" %}
{% set version = "0.0.6b20210414" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/resnest-{{ version }}.tar.gz
  sha256: f39ad668a1c83502c5f12297ae266e6c761d42d719f04469e52f994075d541a4

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python
  run:
    - nose
    - numpy
    - pillow
    - python
    - requests
    - scipy
    - pytorch-gpu >=1.0.0
    - tqdm

test:
  imports:
    - resnest
    - resnest.gluon
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/zhanghang1989/ResNeSt
  summary: ResNeSt
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ruben-arts
