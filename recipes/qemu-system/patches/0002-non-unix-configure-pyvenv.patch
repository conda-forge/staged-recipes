--- a/configure
+++ b/configure
@@ -993,7 +993,8 @@ get_pwd() {
     fi
 }

-meson="$(cd pyvenv/bin; get_pwd)/meson"
+# conda-forge: Use Scripts dir for Windows compatibility
+meson="$PWD/pyvenv/Scripts/meson"
 if [ -f "$meson$EXESUF" ]; then
   meson="$meson$EXESUF"
 fi
