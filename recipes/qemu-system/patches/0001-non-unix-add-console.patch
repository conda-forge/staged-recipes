--- meson.build.orig
+++ meson.build
@@ -81,6 +81,7 @@

 cc = meson.get_compiler('c')
 all_languages = ['c']
+add_project_link_arguments('-Wl,--subsystem,console,-e,mainCRTStartup', language: 'c')
 enable_cpp = host_os == 'windows' or get_option('plugins')
 if enable_cpp and add_languages('cpp', required: false, native: false)
   all_languages += ['cpp']
