{% set name = "pygeoutils" %}
{% set version = "0.1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 02463d6d9eb4ded7f97f48acbc70b00d37ba4d1718e007b0cb30519e5dc46ec2

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools_scm
  run:
    - python >=3.6
    - setuptools
    - pygeoogc
    - geopandas
    - rasterio
    - shapely
    - xarray
    - simplejson

test:
  imports:
    - pygeoutils

about:
  home: https://github.com/cheginit/pygeoutils
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'A part of Hydrodata software stack for handling geospatial data manipulations '

  description: |
      pygeoutils is a part of the Hydrodata suite that handles manipulation of
      (Geo)JSON and GeoTIFF objects and converting them to dataframes.
  doc_url: https://hydrodata.readthedocs.io
  dev_url: https://github.com/cheginit/pygeoutils

extra:
  recipe-maintainers:
    - cheginit
