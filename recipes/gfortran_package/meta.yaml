{% set name = "gfortran_package" %}
{% set version = "8.2.0" %}

package:
  name: {{ name|lower }}_{{ target_platform }}
  version: {{ version }}

build:
  number: 0
  skip: true  # [not osx]

requirements:
  build:
    - _gfortran_package_{{ target_platform }} ={{ version }}

# outputs:
# test:
#   files:
#     - hello.f90
#     - maths.f90
#   commands:
#     - gfortran --help
#     - gfortran -o hello hello.f90 && ./hello
#     - gfortran -O3 -fopenmp -ffast-math -o maths maths.f90 && ./maths

about:
  home:
  license: GPL
  summary: fortran
  description: fortran
  doc_url:
  dev_url:

extra:
  recipe-maintainers:
    - beckermr
