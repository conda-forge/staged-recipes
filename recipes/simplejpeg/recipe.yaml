context:
  name: simplejpeg
  version: "1.8.2"

package:
  name: simplejpeg
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/simplejpeg/simplejpeg-${{ version }}.tar.gz
  sha256: b06e253a896c7fc4f257e11baf96d783817cea41360d0962a70c2743ba57bc30
  patches:
    - skip_yasm_fix.patch
    - do_not_use_lto.patch
    - do_not_build_libjpeg-turbo.patch

build:
  skip:
    - "win or match(python,'<3.9')"
  number: 0

requirements:
  build:
    - ${{ compiler('c') }}
    # Unclear why CXX is needed for a C extension but simplejpeg wants it
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
    - cmake
    - yasm
    - if: unix
      then: make
  host:
    - python
    - cython
    - pip
    - libjpeg-turbo =3.1.0
    - numpy >=2.0.0
    - setuptools >=74.1.1
  run:
    - python
    - numpy >=2.0.0

tests:
  - python:
      imports:
        - simplejpeg
      pip_check: true

about:
  homepage: https://github.com/jfolz/simplejpeg
  summary: simplejpeg is a simple package based on recent versions of libturbojpeg for fast JPEG encoding and decoding.
  license: MIT
  license_file: LICENSE
  repository: https://github.com/jfolz/simplejpeg

extra:
  recipe-maintainers:
    - carlodri
