schema_version: 1

context:
  version: "1.2.4"

package:
  name: pyrage-stubs
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/p/pyrage-stubs/pyrage_stubs-${{ version }}.tar.gz
  sha256: 00d90a5daff51f9d692490ae3134dd14439ebd743a0c2d6426c36580b3a7fb8f

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=61.0.0
    - pip
  run:
    - python >=${{ python_min }}
    - pyrage >=${{ version }},<2.0

tests:
  - python:
      imports:
        - pyrage
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - '*'
  - package_contents:
      strict: true
      site_packages:
        - pyrage/*.pyi
        - pyrage/py.typed
        - pyrage_stubs-${{ version }}.dist-info/*

about:
  summary: A PEP 561 stub package for pyrage's types
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/woodruffw/pyrage
  repository: https://github.com/woodruffw/pyrage/tree/main/pyrage-stubs

extra:
  recipe-maintainers:
    - pavelzw
