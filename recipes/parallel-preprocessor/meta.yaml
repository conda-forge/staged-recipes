# manually update or generated from template
package:
    name: parallel-preprocessor
    version: 0.3
source:
    # calc check sum by command sha256sum
    sha256: 89aad3990c278c2352bacacdc0d7edc77b9922200272e50ff9591c6f957f6219
    url: https://github.com/ukaea/parallel-preprocessor/releases/download/dev/parallel-preprocessor.zip

build:
    number: '2010'  # should be generated, or manually update, YYMM pattern here
    string: oct_2020

requirements:
    build:
        - cmake
        - pybind11
        - vc >=14,<15.0a0 [win]
        - vs2015_runtime [win]
        - vs2015_win-64 [win]
        - occt >=7.4
        - tbb
        - tbb-devel
    host:
        - ca-certificates
        - openssl
        - vc >=14,<15.0a0 [win]
        - vs2015_runtime [win]
    run:  # test
        - occt >=7.4
        - tbb
        - vs2015_runtime [win]

#test:
#    commands:
#        - if not exist %LIBRARY_BIN%\pppApp%SHLIB_EXT% exit 1
about:
    home: https://github.com/ukaea/parallel-preprocessor
    license: LGPL
    license_family: OTHER
    license_file: LICENSE
    summary: 'parallel CAD preprocessor for large assembly simulation'
extra:
    copy_test_source_files: true
    final: true
    recipe-maintainers:
        - qingfengxia
