MACOSX_DEPLOYMENT_TARGET:  # [osx]
    - '10.15'               # [osx]
# some c++17 is only available in macos 10.13 or above
# PropertyContainerTest.cpp:46:22: error: 'any_cast<std::__1::shared_ptr<A> >' is unavailable: 
# conda build works on a osx 10.15 locally, but not on conda-forge build CI
# for the moment, just disable MACOSX by filter in meta.yaml