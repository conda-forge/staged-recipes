{% set name = "seqnado" %}
{% set version = "0.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/seqnado-{{ version }}.tar.gz
  sha256: 3a0bf03d2727bb56aef5316ae0637529f8fde019256c28a595d27930b73f9050

build:
  entry_points:
    - seqnado = seqnado.cli:cli_pipeline
    - seqnado-design = seqnado.cli:cli_design
    - seqnado-config = seqnado.cli:cli_config
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=61.0
    - wheel
    - setuptools-scm >=6.2
    - pip
  run:
    - python >=3.10
    - click
    - cookiecutter
    - drmaa
    - pandas
    - pulp <=2.7.0
    - pydantic
    - pyyaml
    - seaborn
    - setuptools-scm
    - snakemake-wrapper-utils
    - snakemake <8.0.0
    - tracknado
    - wget

test:
  imports:
    - seqnado
  commands:
    - pip check
    - seqnado --help
    - seqnado-design --help
    - seqnado-config --help
  requires:
    - pip

about:
  home: https://alsmith151.github.io/SeqNado/
  summary: Pipelines for genomics analysis
  license: GPL-3.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - CChahrour
