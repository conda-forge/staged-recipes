{% set name = "xforms" %}
{% set version = "0.9999.3" %}
{% set tag = "v0r9999p3" %}

package:
   name: "{{ name|lower }}"
   version: "{{ version }}"

source:
  path: /virgoDev/Xform/v0r9999p3/
#url: https://git.ligo.org/virgo/virgoapp/Cm/-/archive/{{ tag }}/Cm-{{ tag }}.tar.gz
#sha256: 6868297801946c5cc7bb3dcd5a0a6388468fa8292d59f897ae20ba0690f0176e

build:
  number: 0
  skip: True  # [win or osx]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ cdt('mesa-libgl-devel') }}  # [linux]
    - {{ cdt('mesa-dri-drivers') }}  # [linux]
    - {{ cdt('libselinux') }}  # [linux]
    - {{ cdt('libxdamage') }}  # [linux]
    - {{ cdt('libxxf86vm') }}  # [linux]
    - {{ cdt('libxext') }}     # [linux]
    - {{ cdt('expat') }}     # [linux]
    - {{ cdt('libx11') }}     # [linux]
    - meson >=0.50
    - ninja
    - pkg-config
    - jpeg
  host:
    - xorg-libxfixes  # [linux]

test:
  requires:
    - pkg-config
  commands:
    - test -x ${PREFIX}/bin/fdesign  # [unix]
    - test -f ${PREFIX}/lib/libfds.a  # [unix]
    - test -f ${PREFIX}/lib/libforms.a  # [unix]
    - pkg-config --print-errors --modversion xforms
     

about:
  home: https://git.ligo.org/virgo/virgoapp/Xform/
  dev_url: https://git.ligo.org/virgo/virgoApp/Xform/
  license: "LGPL-2.0-only"
  license_family: "GPL"
  license_file: "COPYING"
  summary: XForms is a graphical user interface toolkit for X based on the X11 Xlib library
  description: |
           This is the first Open Source distribution of the XForms Library, a
           graphical user interface toolkit for X Window Systems.  It should be
           considered "provisional".  Hence the Version 0.9999.  There should be
           several more 9's following the decimal point.


extra:
  recipe-maintainers:
      - rhyspoulton
      - fcarbogn 