{% set name = "phonopy" %}
{% set version = "1.11.2.72" %}
{% set sha256 = "300e5495d0306f472c1aa34a522273e3066b9f50d3eb25cee17cd3b6177eea54" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  #skip: True  # [win]
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - numpy x.x
    - setuptools
    - toolchain
  run:
    - python
    - numpy x.x
    - ase
    - spglib
    - scipy
    - yaml
    - pyyaml
    - future

test:
  imports:
    - phonopy
  commands:
    - cd {{ environ.SRC_DIR }}/test
    - python -m unittest discover -s phonopy -b

about:
  home: https://atztogo.github.io/phonopy/
  license: BSD-3-Clause
  license_family: BSD
  license_file: {{ environ.RECIPE_DIR }}/COPYING
  summary: 'Phonopy is an open source package for phonon calculations at harmonic and quasi-harmonic levels.'

  description: |
    Phonopy is an open source package for phonon calculations at harmonic and quasi-harmonic levels.
  doc_url: https://atztogo.github.io/phonopy/
  dev_url: https://github.com/atztogo/phonopy

extra:
  recipe-maintainers:
    - jochym
    - atztogo
