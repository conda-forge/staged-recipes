{% set name = "balance" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/facebookresearch/balance/archive/{{ version }}.tar.gz
  sha256: b975b2a21a17089b97f87d3caa996a7224a39e80449b11f6a03d4ab6dec18e3a

build:
  noarch: python
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - setuptools-scm
    - python >=3.7
  run:
    - pandas <=1.4.3
    - ipfn
    - ipython
    - matplotlib-base
    - numpy
    - patsy
    - plotly
    - python >=3.7
    - seaborn <=0.11.1
    - scikit-learn
    - scipy <=1.8.1
    - statsmodels

test:
  source_files:
    - tests
  imports:
    - balance
  commands:
    - pip check
    - pytest -vvv --capture=tee-sys tests
  requires:
    - pip
    - pytest

about:
  home: https://import-balance.org/
  dev_url: https://github.com/facebookresearch/balance
  summary: Simple workflow and methods for dealing with biased data.
  license: GPL-2.0-only
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - thewchan
