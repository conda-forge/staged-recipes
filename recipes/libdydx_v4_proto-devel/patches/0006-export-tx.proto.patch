--- v4-client-cpp/lib/proto/cosmos/tx/v1beta1/tx.proto.orig	2021-10-14 15:00:00.000000000 +0000
+++ v4-client-cpp/lib/proto/cosmos/tx/v1beta1/tx.proto	2021-10-14 15:00:00.000000000 +0000
@@ -7,6 +7,14
 import "google/protobuf/any.proto";
 import "cosmos_proto/cosmos.proto";

+import "google/protobuf/descriptor.proto";
+
+extend google.protobuf.FileOptions {
+  string cpp_file_export = 50000;
+}
+extend google.protobuf.FieldOptions {
+  string cpp_field_export = 50002;
+}
 option go_package = "github.com/cosmos/cosmos-sdk/types/tx";

 // Tx is the standard type used for broadcasting transactions.
@@ -41,16 +49,16
 message SignDoc {
   // body_bytes is protobuf serialization of a TxBody that matches the
   // representation in TxRaw.
-  bytes body_bytes = 1;
+  bytes body_bytes = 1 [(cpp_field_export) = "DYDX_V4_PROTO_EXPORT"];

   // auth_info_bytes is a protobuf serialization of an AuthInfo that matches the
   // representation in TxRaw.
-  bytes auth_info_bytes = 2;
+  bytes auth_info_bytes = 2 [(cpp_field_export) = "DYDX_V4_PROTO_EXPORT"];

   // chain_id is the unique identifier of the chain this transaction targets.
   // It prevents signed transactions from being used on another chain by an
   // attacker
-  string chain_id = 3;
+  string chain_id = 3 [(cpp_field_export) = "DYDX_V4_PROTO_EXPORT"];

   // account_number is the account number of the account in state
   uint64 account_number = 4;
@@ -236,3 +244,5
   // sig is the signature of the sign doc.
   bytes sig = 4;
 }
+
+option (cpp_file_export) = "DYDX_V4_PROTO_EXPORT";
