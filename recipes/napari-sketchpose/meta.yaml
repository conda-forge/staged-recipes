{% set name = "napari-sketchpose" %}
{% set version = "0.1.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari-sketchpose-{{ version }}.tar.gz
  sha256: 6ffaa959333609e807e71d46e5be2b06de880738c56d246f6adf23dcb0647d94

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools >=42.0.0
    - wheel
  run:
    - cellpose-omni ==0.9.1
    - light-the-torch
    - magicgui
    - matplotlib-base
    - numpy
    - omnipose ==0.4.4
    - pyqtgraph ==0.13.3
    - python >=3.8
    - qtpy

test:
  imports:
    - napari_sketchpose
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/napari-sketchpose/
  summary: A segmentation plugin to adapt Omnipose implementation to partial labelling.
  license: GPL-3.0-only
  license_file: LICENSE
  dev_url: https://bitbucket.org/koopa31/napari-sketchpose

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
