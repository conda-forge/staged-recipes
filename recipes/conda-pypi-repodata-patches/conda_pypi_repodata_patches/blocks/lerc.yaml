name: lerc
reason: mis-tagged-pure-python
details: |
  The `lerc` wheel on PyPI is tagged `py3-none-any` but ships a compiled
  native library. Newer versions renamed the library on PyPI to `pylerc` and
  fixed the tags.
