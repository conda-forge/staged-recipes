{% set name = "SSp_Detector" %}
{% set version = "2023.10.18a0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 024ff31758cd1356c1f7d33cb1f880e93badc0ab5469493572b029009cbc403b

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation"

requirements:
  host:
    - pip
    - python >=3.10

  run:
    - pip
    - python >=3.10

test:
  imports:
    - SSp_Detector
  commands:
    - pip check
  requires:
    - pip

about:
  home: "https://github.com/sjg2203/SSp_Detector"
  license: Apache-2.0
  license_family: APACHE
  license_file: LICENSE
  summary: "Sleep spindles detector"
  doc_url: https://github.com/sjg2203/SSp_Detector/blob/main/README.md
  dev_url: https://github.com/sjg2203/SSp_Detector

extra:
  recipe-maintainers:
    - sjg2203
