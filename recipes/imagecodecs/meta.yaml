{% set name = "imagecodecs" %}
{% set version = "2018.10.21" %}
{% set sha256 = "38e4852a2b890746ddb3a0399416bee69fb588d932c97e3a3513d3861f6d422f" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - setup.patch
    
build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - m2-patch  # [win]
  host:
    - python
    - pip
    - numpy
    - cython
    - msinttypes  # [win and vc<14]
    - zlib
    - bzip2
    - xz
    - zstd
    - lz4-c
    - libpng
    - libwebp<1.0
    - jxrlib
    - libjpeg-turbo
    - libjpeg-turbo12
    - openjpeg=2.3
  run:
    - python
    - {{ pin_compatible('numpy') }}
    - zlib
    - bzip2
    - xz
    - zstd
    - lz4-c
    - libpng
    - libwebp<1.0
    - jxrlib
    - libjpeg-turbo
    - libjpeg-turbo12
    - openjpeg=2.3
    
test:
  imports:
    - imagecodecs
    - imagecodecs._imagecodecs
    - imagecodecs._jpeg12

about:
  home: https://www.lfd.uci.edu/~gohlke/
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'Image transformation, compression, and decompression codecs'
  description: |
    The imagecodecs package provides various block-oriented, in-memory buffer transformation, compression, and decompression functions for use in the tiffile, czifile, and other Python imaging modules.

    Decode and/or encode functions are currently implemented for Zlib DEFLATE, ZStandard, LZMA, BZ2, LZ4, LZW, LZF, JPEG, JPEG 12-bit, JPEG 2000, JPEG XR, WebP, PackBits, Packed Integers, Delta, XOR Delta, Floating Point Predictor, and Bitorder reversal.
  doc_url: https://www.lfd.uci.edu/~gohlke/
  dev_url: https://www.lfd.uci.edu/~gohlke/

extra:
  recipe-maintainers:
    - csachs
