{% set name = "unicall" %}
{% set version = "0.1.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/unicall-{{ version }}.tar.gz
  sha256: c14816c7d8f898f766931e4fa1483178d9dca54f2d6ae7c453bacc88baa1fc18

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9,<4.0
    - poetry-core
    - pip
  run:
    - python >=3.9.0,<4.0.0
    - uniserde >=0.3.6,<0.4.0

test:
  imports:
    - unicall
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://gitlab.com/mad-moo/unicall
  summary: Easy to use RPC library based on uniserde
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
