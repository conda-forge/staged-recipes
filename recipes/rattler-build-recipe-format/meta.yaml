{% set name = "conda-recipe-v2-schema" %}
{% set version = "0.9.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/prefix-dev/recipe-format/archive/v{{ version }}.tar.gz
  sha256: 9d7fe5d5567476e936576d971d976c31d7b706d10a985f5aeeedf28b50fbbbba

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - hatchling
    - pip
  run:
    - python >=3.10
    - pydantic

test:
  imports:
    - conda_recipe_v2_schema
  commands:
    - pip check
  requires:
    - pip


about:
  home: https://github.com/prefix-dev/recipe-format
  summary: Model for the recipe used to build conda packages
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Hofer-Julian
    - wolfv
