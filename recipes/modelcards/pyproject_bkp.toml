[tool.black]
line-length = 88
target_version = ['py37', 'py38', 'py39', 'py310']
preview = true

[tool.isort]
profile = "black"

[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]

build-backend = "setuptools.build_meta"
