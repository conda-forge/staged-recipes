{% set name = "smspp-project" %}
{% set version = "0.5.8" %}

{% set highs_ver = "1.11.0" %}
{% set scip_ver = "9.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  # git_url: https://gitlab.com/smspp/smspp-project
  # git_rev: 33fda299d6adb083849947430ede87ef96eb1e4d
  url: https://gitlab.com/davide-f/{{ name }}/-/releases/{{ version }}/downloads/{{ name }}-{{ version }}.tar.gz
  sha256: 5cd52601a9922bbe52dd9a8f32ed77e1eae44efc7168729ed2878c88fec91683

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - cmake >=3.15
    - _openmp_mutex  # [linux]
    - llvm-openmp  # [osx]
    - make
    - pkgconfig  # [unix]
  host:
    - libboost-headers
    - libboost-mpi
    - netcdf-cxx4
    - bzip2  # [win]
    - zlib  # [win]
    - eigen
    - mpich  # [unix]
    - msmpi  # [win]
    - _openmp_mutex  # [linux]
    - llvm-openmp  # [osx]
    - coin-or-utils
    - coin-or-clp
    - coin-or-osi
    # - scip ={{ scip_ver }}
    - highs ={{ highs_ver }}
    - stopt
    - getopt-win32  # [win]
    - pthreads-win32  # [win]
  run:
    - libboost
    - libboost-mpi
    - netcdf-cxx4
    - bzip2  # [win]
    - zlib  # [win]
    - mpich  # [unix]
    - msmpi  # [win]
    - _openmp_mutex  # [linux]
    - llvm-openmp  # [osx]
    - coin-or-utils
    - coin-or-clp
    - coin-or-osi
    # - scip ={{ scip_ver }}
    - highs ={{ highs_ver }}
    - stopt
    - pthreads-win32  # [win]

test:
  requires:
    - git
  commands:
    - ucblock_solver --help
    - InvestmentBlock_test --help
    - |
      git clone --depth 1 https://gitlab.com/smspp/ucblock
      git clone --depth 1 -b highs-ucblocksolver-example https://gitlab.com/davide-f/tools
      cd tools/ucblock_solver/examples
      ucblock_solver Bus_Test.nc4 -S uc_solverconfig-HiGHS.txt

about:
  home: https://gitlab.com/smspp/smspp-project
  summary: 'A powerful tool to model and solve optimization problems.'
  description: |
    SMS++ project provides is a system for modeling complex, block-structured mathematical models, and solving them via sophisticated, structure-exploiting algorithms.
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: LICENSE
  doc_url: https://smspp.gitlab.io/
  dev_url: https://gitlab.com/smspp/smspp-project

extra:
  recipe-maintainers:
    - davide-f
    - frangio68
    - dmeoli
