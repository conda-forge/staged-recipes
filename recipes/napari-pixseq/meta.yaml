{% set name = "napari-pixseq" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari-PixSeq-{{ version }}.tar.gz
  sha256: e42e7c2c3d2c80a5e48f75903653428934dd1d5ea27a3376e04939b7dd6085fa

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools >=42.0.0
    - setuptools-scm
    - wheel
  run:
    - magicgui
    - matplotlib-base
    - napari
    - numpy
    - originpro
    - opencv
    - pandas
    - picassosr
    - pyqtgraph
    - python >=3.8
    - pyqt5-tools
    - qtpy
    - scipy
    - tqdm

test:
  imports:
    - napari_pixseq
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/napari-PixSeq
  summary: A Napari plugin for extracting time series traces from Single Molecule Localisation Microsocpy (SMLM) data.
  license: MIT
  license_file: LICENSE
  dev_url: https://github.com/piedrro/napari-PixSeq

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
