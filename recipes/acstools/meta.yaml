{% set name = "acstools" %}
{% set version = "3.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9a91262f2bac5e2f100d949211ebe6fd657dbf26cf07b48cab11d0653432b782

build:
  noarch: python
  number: 0
  skip: True  # [py<36]
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python
    - pip
    - setuptools_scm
  run:
    - python
    - numpy
    - astropy
    - beautifulsoup4
    - requests
    - pyyaml
    - scipy
    - scikit-image

test:
  imports:
    - acstools
  commands:
    - acs_destripe --help
    - acs_destripe_plus --help

about:
  home: https://github.com/spacetelescope/acstools
  license: BSD-3-Clause
  license_family: BSD
  # It is strongly encouraged to include a license file in the package,
  # (even if the license doesn't require it) using the license_file entry.
  # See https://docs.conda.io/projects/conda-build/en/latest/resources/define-metadata.html#license-file
  license_file: LICENSE.md
  summary: 'Python Tools for HST ACS'
  description: Python Tools for HST ACS (Advanced Camera for Surveys) Data
  doc_url: https://acstools.readthedocs.io/
  dev_url: https://github.com/spacetelescope/acstools

extra:
  recipe-maintainers:
    - pllim
