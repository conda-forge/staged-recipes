c_compiler:                    # [win]
  - vs2017                     # [win]
cxx_compiler:                  # [win]
  - vs2017                     # [win]
vc:                            # [win]
  - 14.1                       # [win]

# Doesn't build python, but specifying python is necessary
# since it is part of a zip_keys in
# https://github.com/conda-forge/conda-forge-pinning-feedstock/blob/master/recipe/conda_build_config.yaml
python:                        # [win]
  - 3.7.* *_cpython            # [win]
