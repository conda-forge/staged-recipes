{% set name = "thuner" %}
{% set version = "0.0.8" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: e4bc608f8340e569c88dca27ba1b0ffd6a7b5646077d96cd37744d73aa14caf7

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  skip: True  # [win]
    

requirements:
  host:
    - python
    - pip
    - setuptools
    - graphviz
    - python-graphviz
    - pydot
  run:
    - python
    - numpy
    - numba
    - scipy
    - scikit-image
    - pandas
    - matplotlib-base
    - xarray
    - cartopy
    - typing_extensions
    - pre-commit
    - black
    - pytest
    - pytest-cov
    - codecov
    - ipykernel
    - netcdf4
    - h5netcdf
    - requests
    - arm_pyart
    - tqdm
    - cdsapi
    - xesmf
    - opencv
    - awscli
    - pytables
    - memory_profiler
    - psutil
    - pydantic
    - zarr
    - windrose
    - pydot
    - graphviz
    - python-graphviz
    - metpy

test:
  imports:
    - thuner
  commands:
    - pip check
  requires:
    - python
    - pip

about:
  home: https://github.com/THUNER-project/THUNER
  license: BSD-3-Clause
  license_file: LICENSE.txt
  license_family: BSD
  summary: Thunderstorm tracking and analysis 
  description: The Thunderstorm Event Reconnaissance (THUNER) package is a toolkit for tracking and analyzing meteorological events, primarily convective events.
    
extra:
  recipe-maintainers:
    - eshort0401
