diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -31,3 +31,14 @@ if (NOT MSVC)
 endif ()
 
+if (DEFINED ENV{LIBRARY_PREFIX})
+    list(APPEND CMAKE_PREFIX_PATH "$ENV{LIBRARY_PREFIX}")
+    set(CMAKE_FIND_PACKAGE_PREFER_CONFIG ON)
+    list(APPEND CMAKE_IGNORE_PATH "$ENV{LIBRARY_PREFIX}/lib/cmake/sundials")
+    file(WRITE "${CMAKE_BINARY_DIR}/conda_cmake/FindBLAS.cmake"
+        "set(BLAS_LIBRARIES \"$ENV{LIBRARY_PREFIX}/lib/blas.lib\")\n"
+        "set(BLAS_LINKER_FLAGS \"\")\n"
+        "set(BLAS_FOUND TRUE)\n")
+    list(PREPEND CMAKE_MODULE_PATH "${CMAKE_BINARY_DIR}/conda_cmake")
+endif ()
+
 find_package(pybind11 CONFIG REQUIRED)
