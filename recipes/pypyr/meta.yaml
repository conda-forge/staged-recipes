{% set name = "pypyr" %}
{% set version = "5.1.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pypyr-{{ version }}.tar.gz
  sha256: 17a1376232126caf294e9c369af86d9fa6d104559ba160aa4754b83a46c03b7d

build:
  number: 0
  noarch: python
  entry_points:
    - pypyr=pypyr.cli:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - python-dateutil
    - ruamel.yaml
    - tomli
    - tomli-w

test:
  imports:
    - pypyr
    - pypyr.cache
  commands:
    - pip check
    - pypyr --help
  requires:
    - pip

about:
  home: https://pypyr.io
  summary: task-runner for automation pipelines defined in yaml. cli & api.
  dev_url: https://github.com/pypyr/pypyr/
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Sclare87
