{% set name = "symfem" %}
{% set version = "2021.7.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: c90326bbb8694465ce9801e7ccf66138c0aa8901beb5abcd43da79c6d88830af

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - python
    - pip
  run:
    - numpy
    - python >=3.7
    - pytest
    - sympy

test:
  requires:
    - pytest
  imports:
    - symfem
  commands:
    pytest test/ --speed fast


about:
  home: http://github.com/mscroggs/symfem
  license: MIT
  license_family: MIT

  license_file: LICENSE
  summary: 'A symbolic finite element definition library'

  description: |
    Symfem is a symbolic finite element definition library, that can be used to  
    symbolically evaluate the basis functions of a finite element space.
  dev_url: https://github.com/mscroggs/symfem

extra:
  recipe-maintainers:
    - mscroggs
    - symfembot
