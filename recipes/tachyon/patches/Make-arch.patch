diff --git a/unix/Make-arch b/unix/Make-arch
index 1044370..7a18e64 100644
--- a/unix/Make-arch
+++ b/unix/Make-arch
@@ -1026,12 +1026,21 @@ next:
 macosx:
 	$(MAKE) all \
 	"ARCH = macosx" \
-	"CC = cc" \
 	"CFLAGS = -Os -ffast-math -DBsd $(MISCFLAGS)" \
	"AR = ar" \
 	"ARFLAGS = r" \
 	"STRIP = strip" \
	"RANLIB = ranlib" \
	"LIBS = -L. -ltachyon $(MISCLIB)"
+
+macosx-x86_64-thr:
+	$(MAKE) all \
+	"ARCH = macosx-x86-thr" \
+	"CFLAGS = -O3 -m64 -ffast-math -DBsd $(THREADSFLAGS) $(MISCFLAGS)" \
+	"AR = ar" \
+	"ARFLAGS = r" \
+	"STRIP = strip" \
+	"RANLIB = ranlib" \
+ 	"LIBS = -L. -ltachyon $(MISCLIB) -lpthread"
 
 macosx-thr:
@@ -1216,12 +1224,11 @@ linux-64-debug:
 linux-64-thr:
 	$(MAKE) all \
 	"ARCH = linux-64-thr" \
-	"CC = gcc" \
 	"CFLAGS = -m64 -Wall -O3 -fomit-frame-pointer -ffast-math -DLinux  $(THREADSFLAGS) -D_REENTRANT $(MISCFLAGS)" \
	"AR = ar" \
 	"ARFLAGS = r" \
 	"STRIP = strip" \
	"RANLIB = ranlib" \
 	"LIBS = -L. -ltachyon $(MISCLIB) -lm -lpthread"
 
 # Linux on AMD64/EM64T, using Intel C 
