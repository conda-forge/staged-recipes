{% set name = "qastle" %}
{% set version = "0.17.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/qastle-{{ version }}.tar.gz
  sha256: 6d3a23d67825327cf5728cd84434410f7199890ca44d6abf64f7ead6001eb790

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.6,<3.13
    - pip
  run:
    - python >=3.6,<3.13
    - lark

test:
  imports:
    - qastle
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/iris-hep/qastle
  summary: Query AST Language Expressions
  license: MIT
  license_file: LICENSE
  dev_url: https://github.com/iris-hep/qastle

extra:
  recipe-maintainers:
    - masonproffitt
    - matthewfeickert
