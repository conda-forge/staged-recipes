{% set name = "bigdft-suite" %}
{% set version = "1.9.3_dev" %}
{% set build = 0 %}
{% set mpi = mpi %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://gitlab.com/l_sim/bigdft-suite/-/archive/1.9.3-RC.1/bigdft-suite-1.9.3-RC.1.tar.gz
  md5: 1864bb001ade0285b50060fcdb3827db
  patches:
    - patches/output.patch
    - patches/utils.patch
    - patches/multipole.patch
    - patches/mp_makefile.patch

build:
  number: {{ build }}
  skip: true  # [win or osx]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('fortran') }}
    - cmake
    - llvm-openmp  # [osx]
    - libgomp  # [linux]
    - autoconf
    - automake
    - binutils
    - make
    - pip
  host:
    - {{ mpi }}
    - libblas
    - liblapack
    - yaml
    - python
    - libxc <5.0
    - ntpoly
    - setuptools
  run:
    - {{ mpi }}
    - python
    - scipy
    - numpy
    - pyyaml
    - libxc <5.0
    - openssh  # [mpi == 'openmpi']

test:
  files:
    - test.py

about:
  home: https://bigdft.org
  license: GPL-3.0-only
  license_family: GPL
  license_file: LICENSE
  summary: Density Functional Theory calculations based on wavelets
  doc_url: https://l_sim.gitlab.io/bigdft-suite/
  dev_url: https://gitlab.com/l_sim/bigdft-suite

extra:
  recipe-maintainers:
    - william-dawson
    - luigigenovese
