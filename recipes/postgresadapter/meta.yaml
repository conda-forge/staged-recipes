{% set name = "PostgresAdapter" %}
{% set version = "1.0.0" %}
{% set sha256 = "22e64dcfaae12c5624cad63816e85b2e0749b3f63893b75d252c8aa0018a19bb" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name|lower }}-{{ version }}.tar.gz
  url: https://github.com/ContinuumIO/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [py>=36]
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - numpy 1.11*
    - cython
    - postgresql
    - psycopg2
    - toolchain

  run:
    - python 
    - numpy
    - pandas
    - six
    - libpq

test:
  requires:
    - nose
    - pytest
    - sqlalchemy
    - psycopg2

  imports:
    - postgresadapter
    - postgresadapter.core
    - postgresadapter.core.PostgresAdapter

about:
  home: https://www.continuum.io/
  license: BSD
  summary: 'Python interface to PostgreSQL, with support for PostGIS'

extra:
  recipe-maintainers:
    - DavidMertz
    - gbrener
