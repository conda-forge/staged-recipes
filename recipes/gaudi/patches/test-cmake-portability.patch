diff --git a/GaudiExamples/TinyExperiment/tests/refs/FullExperiment.yaml b/GaudiExamples/TinyExperiment/tests/refs/FullExperiment.yaml
index fb085cf62..ae25dbc17 100644
--- a/GaudiExamples/TinyExperiment/tests/refs/FullExperiment.yaml
+++ b/GaudiExamples/TinyExperiment/tests/refs/FullExperiment.yaml
@@ -1,130 +1,130 @@
 stdout: |-
   /***** Algorithm Gaudi::Example::TinyExperiment::GeneratorAlg/GeneratorAlg *************************
-  |-OutputLevel                = 0
-  |-Enable                     = True
-  |-ErrorMax                   = 1
-  |-ErrorCount                 = 0
-  |-AuditInitialize            = False
-  |-AuditReinitialize          = False
+  |-Asynchronous               = False
   |-AuditExecute               = False
   |-AuditFinalize              = False
-  |-ExtraInputs                = set()  (default: set())
-  |-ExtraOutputs               = set()  (default: set())
+  |-AuditInitialize            = False
+  |-AuditReinitialize          = False
   |-AuditRestart               = False
   |-AuditStart                 = False
   |-AuditStop                  = False
-  |-Timeline                   = True
-  |-MonitorService             = 'MonitorSvc'
-  |-RegisterForContextService  = False
   |-Cardinality                = 0
-  |-NeededResources            = []  (default: [])
-  |-Asynchronous               = False
+  |-Enable                     = True
+  |-ErrorCount                 = 0
+  |-ErrorMax                   = 1
+  |-ExtraInputs                = set()  (default: set())
+  |-ExtraOutputs               = set()  (default: set())
   |-FilterCircularDependencies = True
   |-MCTracksLocation           = DataHandle('/Event/MCTracks','W','std::vector<Gaudi::Example::TinyExperiment::MCTrack,std::allocator<Gaudi::Example::TinyExperiment::MCTrack> >')
-  |-RandomGenSvc               = ServiceHandle('RandomGenSvc')
+  |-MonitorService             = 'MonitorSvc'
   |-NbTracksToGenerate         = 10  (default: 10)
+  |-NeededResources            = []  (default: [])
+  |-OutputLevel                = 0
+  |-RandomGenSvc               = ServiceHandle('RandomGenSvc')
+  |-RegisterForContextService  = False
+  |-Timeline                   = True
   \----- (End of Algorithm Gaudi::Example::TinyExperiment::GeneratorAlg/GeneratorAlg) ----------------
   /***** Algorithm Gaudi::Example::TinyExperiment::SimulationAlg/SimulationAlg ***********************
-  |-OutputLevel                = 0
-  |-Enable                     = True
-  |-ErrorMax                   = 1
-  |-ErrorCount                 = 0
-  |-AuditInitialize            = False
-  |-AuditReinitialize          = False
+  |-Asynchronous               = False
   |-AuditExecute               = False
   |-AuditFinalize              = False
-  |-ExtraInputs                = set()  (default: set())
-  |-ExtraOutputs               = set()  (default: set())
+  |-AuditInitialize            = False
+  |-AuditReinitialize          = False
   |-AuditRestart               = False
   |-AuditStart                 = False
   |-AuditStop                  = False
-  |-Timeline                   = True
-  |-MonitorService             = 'MonitorSvc'
-  |-RegisterForContextService  = False
   |-Cardinality                = 0
-  |-NeededResources            = []  (default: [])
-  |-Asynchronous               = False
+  |-Enable                     = True
+  |-ErrorCount                 = 0
+  |-ErrorMax                   = 1
+  |-ExtraInputs                = set()  (default: set())
+  |-ExtraOutputs               = set()  (default: set())
   |-FilterCircularDependencies = True
-  |-MCTracksLocation           = DataHandle('/Event/MCTracks','R','std::vector<Gaudi::Example::TinyExperiment::MCTrack,std::allocator<Gaudi::Example::TinyExperiment::MCTrack> >')
   |-MCHitsLocation             = DataHandle('/Event/MCHits','W','std::vector<Gaudi::Example::TinyExperiment::MCHit,std::allocator<Gaudi::Example::TinyExperiment::MCHit> >')
+  |-MCTracksLocation           = DataHandle('/Event/MCTracks','R','std::vector<Gaudi::Example::TinyExperiment::MCTrack,std::allocator<Gaudi::Example::TinyExperiment::MCTrack> >')
+  |-MonitorService             = 'MonitorSvc'
   |-NbHitsPerTrack             = 15  (default: 10)
+  |-NeededResources            = []  (default: [])
+  |-OutputLevel                = 0
+  |-RegisterForContextService  = False
+  |-Timeline                   = True
   \----- (End of Algorithm Gaudi::Example::TinyExperiment::SimulationAlg/SimulationAlg) --------------
   /***** Algorithm Gaudi::Example::TinyExperiment::DigitizationAlg/DigitizationAlg *******************
-  |-OutputLevel                = 0
-  |-Enable                     = True
-  |-ErrorMax                   = 1
-  |-ErrorCount                 = 0
-  |-AuditInitialize            = False
-  |-AuditReinitialize          = False
+  |-Asynchronous               = False
   |-AuditExecute               = False
   |-AuditFinalize              = False
-  |-ExtraInputs                = set()  (default: set())
-  |-ExtraOutputs               = set()  (default: set())
+  |-AuditInitialize            = False
+  |-AuditReinitialize          = False
   |-AuditRestart               = False
   |-AuditStart                 = False
   |-AuditStop                  = False
-  |-Timeline                   = True
-  |-MonitorService             = 'MonitorSvc'
-  |-RegisterForContextService  = False
   |-Cardinality                = 0
-  |-NeededResources            = []  (default: [])
-  |-Asynchronous               = False
+  |-Enable                     = True
+  |-ErrorCount                 = 0
+  |-ErrorMax                   = 1
+  |-ExtraInputs                = set()  (default: set())
+  |-ExtraOutputs               = set()  (default: set())
   |-FilterCircularDependencies = True
-  |-MCHitsLocation             = DataHandle('/Event/MCHits','R','std::vector<Gaudi::Example::TinyExperiment::MCHit,std::allocator<Gaudi::Example::TinyExperiment::MCHit> >')
   |-HitsLocation               = DataHandle('/Event/Hits','W','std::vector<Gaudi::Example::TinyExperiment::Hit,std::allocator<Gaudi::Example::TinyExperiment::Hit> >')
+  |-MCHitsLocation             = DataHandle('/Event/MCHits','R','std::vector<Gaudi::Example::TinyExperiment::MCHit,std::allocator<Gaudi::Example::TinyExperiment::MCHit> >')
+  |-MonitorService             = 'MonitorSvc'
+  |-NeededResources            = []  (default: [])
+  |-OutputLevel                = 0
   |-RandomGenSvc               = ServiceHandle('RandomGenSvc')
+  |-RegisterForContextService  = False
   |-SigmaNoise                 = 0.1  (default: 1.0)
+  |-Timeline                   = True
   \----- (End of Algorithm Gaudi::Example::TinyExperiment::DigitizationAlg/DigitizationAlg) ----------
   /***** Algorithm Gaudi::Example::TinyExperiment::TrackingAlg/TrackingAlg ***************************
-  |-OutputLevel                = 0
-  |-Enable                     = True
-  |-ErrorMax                   = 1
-  |-ErrorCount                 = 0
-  |-AuditInitialize            = False
-  |-AuditReinitialize          = False
+  |-Asynchronous               = False
   |-AuditExecute               = False
   |-AuditFinalize              = False
-  |-ExtraInputs                = set()  (default: set())
-  |-ExtraOutputs               = set()  (default: set())
+  |-AuditInitialize            = False
+  |-AuditReinitialize          = False
   |-AuditRestart               = False
   |-AuditStart                 = False
   |-AuditStop                  = False
-  |-Timeline                   = True
-  |-MonitorService             = 'MonitorSvc'
-  |-RegisterForContextService  = False
   |-Cardinality                = 0
-  |-NeededResources            = []  (default: [])
-  |-Asynchronous               = False
+  |-Enable                     = True
+  |-ErrorCount                 = 0
+  |-ErrorMax                   = 1
+  |-ExtraInputs                = set()  (default: set())
+  |-ExtraOutputs               = set()  (default: set())
   |-FilterCircularDependencies = True
   |-HitsLocation               = DataHandle('/Event/Hits','R','std::vector<Gaudi::Example::TinyExperiment::Hit,std::allocator<Gaudi::Example::TinyExperiment::Hit> >')
-  |-TracksLocation             = DataHandle('/Event/Tracks','W','std::vector<Gaudi::Example::TinyExperiment::Track,std::allocator<Gaudi::Example::TinyExperiment::Track> >')
+  |-MonitorService             = 'MonitorSvc'
+  |-NeededResources            = []  (default: [])
   |-NumberBins                 = 100  (default: 180)
+  |-OutputLevel                = 0
+  |-RegisterForContextService  = False
   |-Sensibility                = 6  (default: 6)
+  |-Timeline                   = True
+  |-TracksLocation             = DataHandle('/Event/Tracks','W','std::vector<Gaudi::Example::TinyExperiment::Track,std::allocator<Gaudi::Example::TinyExperiment::Track> >')
   \----- (End of Algorithm Gaudi::Example::TinyExperiment::TrackingAlg/TrackingAlg) ------------------
   /***** Algorithm Gaudi::Example::TinyExperiment::CheckerAlg/CheckerAlg *****************************
-  |-OutputLevel                = 0
-  |-Enable                     = True
-  |-ErrorMax                   = 1
-  |-ErrorCount                 = 0
-  |-AuditInitialize            = False
-  |-AuditReinitialize          = False
+  |-Asynchronous               = False
   |-AuditExecute               = False
   |-AuditFinalize              = False
-  |-ExtraInputs                = set()  (default: set())
-  |-ExtraOutputs               = set()  (default: set())
+  |-AuditInitialize            = False
+  |-AuditReinitialize          = False
   |-AuditRestart               = False
   |-AuditStart                 = False
   |-AuditStop                  = False
-  |-Timeline                   = True
-  |-MonitorService             = 'MonitorSvc'
-  |-RegisterForContextService  = False
   |-Cardinality                = 0
-  |-NeededResources            = []  (default: [])
-  |-Asynchronous               = False
+  |-DeltaThetaMax              = 0.01  (default: 0.001)
+  |-Enable                     = True
+  |-ErrorCount                 = 0
+  |-ErrorMax                   = 1
+  |-ExtraInputs                = set()  (default: set())
+  |-ExtraOutputs               = set()  (default: set())
   |-FilterCircularDependencies = True
-  |-TracksLocation             = DataHandle('/Event/Tracks','R','std::vector<Gaudi::Example::TinyExperiment::Track,std::allocator<Gaudi::Example::TinyExperiment::Track> >')
   |-MCTracksLocation           = DataHandle('/Event/MCTracks','R','std::vector<Gaudi::Example::TinyExperiment::MCTrack,std::allocator<Gaudi::Example::TinyExperiment::MCTrack> >')
-  |-DeltaThetaMax              = 0.01  (default: 0.001)
+  |-MonitorService             = 'MonitorSvc'
+  |-NeededResources            = []  (default: [])
+  |-OutputLevel                = 0
+  |-RegisterForContextService  = False
+  |-Timeline                   = True
+  |-TracksLocation             = DataHandle('/Event/Tracks','R','std::vector<Gaudi::Example::TinyExperiment::Track,std::allocator<Gaudi::Example::TinyExperiment::Track> >')
   \----- (End of Algorithm Gaudi::Example::TinyExperiment::CheckerAlg/CheckerAlg) --------------------
   ApplicationMgr     SUCCESS
   ====================================================================================================================================
diff --git a/GaudiHive/src/PRGraph/Visitors/Validators.cpp b/GaudiHive/src/PRGraph/Visitors/Validators.cpp
index 3fa67e325..7779d83fc 100644
--- a/GaudiHive/src/PRGraph/Visitors/Validators.cpp
+++ b/GaudiHive/src/PRGraph/Visitors/Validators.cpp
@@ -1,5 +1,5 @@
 /***********************************************************************************\
-* (c) Copyright 1998-2019 CERN for the benefit of the LHCb and ATLAS collaborations *
+* (c) Copyright 1998-2026 CERN for the benefit of the LHCb and ATLAS collaborations *
 *                                                                                   *
 * This software is distributed under the terms of the Apache version 2 licence,     *
 * copied verbatim in the file "LICENSE".                                            *
@@ -286,7 +286,7 @@ namespace concurrency {
 
       for ( auto& pr : m_scc ) {
         if ( pr.second.size() > 1 ) {
-          m_status << "\n o [lowlink:" << std::to_string( pr.first ) << "] |";
+          m_status << "\n o |";
 
           // rotate SCCs to get reproducible order
           std::vector sortedSCC( pr.second.begin(), pr.second.end() );
diff --git a/GaudiHive/src/TimelineSvc.cpp b/GaudiHive/src/TimelineSvc.cpp
index 23f1f6f6d..2c247d3c5 100644
--- a/GaudiHive/src/TimelineSvc.cpp
+++ b/GaudiHive/src/TimelineSvc.cpp
@@ -1,5 +1,5 @@
 /***********************************************************************************\
-* (c) Copyright 1998-2025 CERN for the benefit of the LHCb and ATLAS collaborations *
+* (c) Copyright 1998-2026 CERN for the benefit of the LHCb and ATLAS collaborations *
 *                                                                                   *
 * This software is distributed under the terms of the Apache version 2 licence,     *
 * copied verbatim in the file "LICENSE".                                            *
@@ -13,8 +13,15 @@
 #include <GaudiKernel/StatusCode.h>
 #include <GaudiKernel/TimelineEvent.h>
 
+#include <cstdint>
 #include <fstream>
 
+namespace {
+  // pthread_t is an unsigned long on Linux but a pointer on macOS.
+  // Cast to uintptr_t for consistent decimal output across platforms.
+  std::uintptr_t thread_to_int( pthread_t t ) { return reinterpret_cast<std::uintptr_t>( t ); }
+} // namespace
+
 StatusCode TimelineSvc::initialize() {
   StatusCode sc = Service::initialize();
   if ( !sc.isSuccess() ) return sc;
@@ -66,8 +73,8 @@ ITimelineSvc::TimelineRecorder TimelineSvc::getRecorder( std::string alg, const
     out << std::chrono::duration_cast<std::chrono::nanoseconds>( newTimelineEvent.start.time_since_epoch() ).count()
         << " "
         << std::chrono::duration_cast<std::chrono::nanoseconds>( newTimelineEvent.end.time_since_epoch() ).count()
-        << " " << newTimelineEvent.algorithm << " " << newTimelineEvent.thread << " " << newTimelineEvent.slot << " "
-        << newTimelineEvent.event << std::endl;
+        << " " << newTimelineEvent.algorithm << " " << thread_to_int( newTimelineEvent.thread ) << " "
+        << newTimelineEvent.slot << " " << newTimelineEvent.event << std::endl;
 
     out.close();
   }
@@ -92,7 +99,7 @@ void TimelineSvc::outputTimeline() {
   for ( const auto& e : m_events ) {
     out << std::chrono::duration_cast<std::chrono::nanoseconds>( e.start.time_since_epoch() ).count() << " "
         << std::chrono::duration_cast<std::chrono::nanoseconds>( e.end.time_since_epoch() ).count() << " "
-        << e.algorithm << " " << e.thread << " " << e.slot << " " << e.event << std::endl;
+        << e.algorithm << " " << thread_to_int( e.thread ) << " " << e.slot << " " << e.event << std::endl;
   }
 
   out.close();
diff --git a/GaudiHive/tests/pytest/avalanche_scheduler/test_simple_test.py b/GaudiHive/tests/pytest/avalanche_scheduler/test_simple_test.py
index 6e4909533..0940025cf 100644
--- a/GaudiHive/tests/pytest/avalanche_scheduler/test_simple_test.py
+++ b/GaudiHive/tests/pytest/avalanche_scheduler/test_simple_test.py
@@ -1,5 +1,5 @@
 #####################################################################################
-# (c) Copyright 2024-2025 CERN for the benefit of the LHCb and ATLAS collaborations #
+# (c) Copyright 2024-2026 CERN for the benefit of the LHCb and ATLAS collaborations #
 #                                                                                   #
 # This software is distributed under the terms of the Apache version 2 licence,     #
 # copied verbatim in the file "LICENSE".                                            #
@@ -8,35 +8,70 @@
 # granted to it by virtue of its status as an Intergovernmental Organization        #
 # or submit itself to any jurisdiction.                                             #
 #####################################################################################
+import re
+
 from GaudiTesting import GaudiExeTest
 
 
+def normalize_graph(graph):
+    """Normalize obj_* IDs and line order for platform-independent comparison.
+
+    std::hash<std::string> produces different values on different platforms
+    (libstdc++ vs libc++), so we normalize obj_HASH to obj_N based on the
+    label they're associated with. We also sort the body lines since the
+    output order may vary.
+    """
+    # Extract mapping from obj_* IDs to their labels
+    label_pattern = re.compile(r"(obj_\d+) \[label=\"([^\"]+)\"")
+    id_to_label = {}
+    for match in label_pattern.finditer(graph):
+        id_to_label[match.group(1)] = match.group(2)
+
+    # Create normalized IDs based on sorted labels for deterministic ordering
+    sorted_labels = sorted(id_to_label.items(), key=lambda x: x[1])
+    id_mapping = {old_id: f"obj_{i}" for i, (old_id, _) in enumerate(sorted_labels)}
+
+    # Replace all obj_* IDs with normalized versions
+    result = graph
+    for old_id, new_id in id_mapping.items():
+        result = result.replace(old_id, new_id)
+
+    # Sort the body lines (between header and closing brace) for order-independence
+    lines = result.strip().split("\n")
+    header = lines[:2]  # "digraph datadeps {" and "rankdir=..."
+    footer = lines[-1:]  # "}"
+    body = sorted(lines[2:-1])
+    return "\n".join(header + body + footer)
+
+
 class Test(GaudiExeTest):
     command = ["gaudirun.py", "-v", "../../../options/AvalancheSchedulerSimpleTest.py"]
     timeout = 120
 
     def test_graph_file(self, cwd):
+        # Reference graph with normalized obj_* IDs (sorted by label)
+        # obj_0 = /Event/a1, obj_1 = /Event/a2, obj_2 = /Event/a3, obj_3 = /Event/a4
         ref = """digraph datadeps {
   rankdir="LR";
   Alg_0 [label="A1";shape=box];
-  obj_13600394293829281201 [label="/Event/a1";shape=box];
-  Alg_0 -> obj_13600394293829281201;
+  obj_0 [label="/Event/a1";shape=box];
+  Alg_0 -> obj_0;
   Alg_1 [label="A2";shape=box];
-  obj_13600394293829281201 -> Alg_1;
-  obj_1573251618719251457 [label="/Event/a2";shape=box];
-  Alg_1 -> obj_1573251618719251457;
+  obj_0 -> Alg_1;
+  obj_1 [label="/Event/a2";shape=box];
+  Alg_1 -> obj_1;
   Alg_2 [label="A3";shape=box];
-  obj_13600394293829281201 -> Alg_2;
-  obj_15861236635915656172 [label="/Event/a3";shape=box];
-  Alg_2 -> obj_15861236635915656172;
+  obj_0 -> Alg_2;
+  obj_2 [label="/Event/a3";shape=box];
+  Alg_2 -> obj_2;
   Alg_3 [label="A4";shape=box];
-  obj_15861236635915656172 -> Alg_3;
-  obj_1573251618719251457 -> Alg_3;
-  obj_9576344950949101152 [label="/Event/a4";shape=box];
-  Alg_3 -> obj_9576344950949101152;
+  obj_2 -> Alg_3;
+  obj_1 -> Alg_3;
   Alg_4 [label="CNT&";shape=box];
   Alg_5 [label="CNT*";shape=box];
+  obj_3 [label="/Event/a4";shape=box];
+  Alg_3 -> obj_3;
 }
 """
         graph = open(f"{cwd}/graphDump.dot").read()
-        assert graph == ref
+        assert normalize_graph(graph) == normalize_graph(ref)
diff --git a/GaudiHive/tests/pytest/precedence/test_detect_scc.py b/GaudiHive/tests/pytest/precedence/test_detect_scc.py
index ce93121f6..969c332d6 100644
--- a/GaudiHive/tests/pytest/precedence/test_detect_scc.py
+++ b/GaudiHive/tests/pytest/precedence/test_detect_scc.py
@@ -1,5 +1,5 @@
 #####################################################################################
-# (c) Copyright 2024-2025 CERN for the benefit of the LHCb and ATLAS collaborations #
+# (c) Copyright 2024-2026 CERN for the benefit of the LHCb and ATLAS collaborations #
 #                                                                                   #
 # This software is distributed under the terms of the Apache version 2 licence,     #
 # copied verbatim in the file "LICENSE".                                            #
@@ -19,6 +19,6 @@ class Test(GaudiExeTest):
     test_signature = GaudiExeTest.find_reference_block(
         """
         PrecedenceSvc        ERROR   Strongly connected components found in DF realm:
-         o [lowlink:3] | CycledAlg1 | CycledAlg3 | CycledAlg2 |
+         o | CycledAlg1 | CycledAlg3 | CycledAlg2 |
         """
     )
diff --git a/GaudiKernel/python/GaudiKernel/Configurable.py b/GaudiKernel/python/GaudiKernel/Configurable.py
index f5c4c45bc..25e1bf8c1 100644
--- a/GaudiKernel/python/GaudiKernel/Configurable.py
+++ b/GaudiKernel/python/GaudiKernel/Configurable.py
@@ -1,5 +1,5 @@
 #####################################################################################
-# (c) Copyright 1998-2025 CERN for the benefit of the LHCb and ATLAS collaborations #
+# (c) Copyright 1998-2026 CERN for the benefit of the LHCb and ATLAS collaborations #
 #                                                                                   #
 # This software is distributed under the terms of the Apache version 2 licence,     #
 # copied verbatim in the file "LICENSE".                                            #
@@ -995,7 +995,7 @@ class Configurable(metaclass=ConfigurableMeta.ConfigurableMeta):
             nameWidth = 0
             for p in props.keys():
                 nameWidth = max(nameWidth, len(p))
-            for p, v in props.items():
+            for p, v in sorted(props.items()):
                 # start with indent and property name
                 prefix = indentStr + "|-%-*s" % (nameWidth, p)
                 # add memory address for debugging (not for defaults)
diff --git a/GaudiKernel/tests/pytest/jira/test_gaudi_404.py b/GaudiKernel/tests/pytest/jira/test_gaudi_404.py
index dc700bcb4..6b5b9da0f 100644
--- a/GaudiKernel/tests/pytest/jira/test_gaudi_404.py
+++ b/GaudiKernel/tests/pytest/jira/test_gaudi_404.py
@@ -1,6 +1,6 @@
 #!/usr/bin/env python3
 #####################################################################################
-# (c) Copyright 1998-2024 CERN for the benefit of the LHCb and ATLAS collaborations #
+# (c) Copyright 1998-2026 CERN for the benefit of the LHCb and ATLAS collaborations #
 #                                                                                   #
 # This software is distributed under the terms of the Apache version 2 licence,     #
 # copied verbatim in the file "LICENSE".                                            #
@@ -11,6 +11,7 @@
 #####################################################################################
 import os
 import re
+import sys
 from subprocess import PIPE, run
 
 import pytest
@@ -18,7 +19,7 @@ from GaudiTesting import platform_matches
 
 
 @pytest.mark.skipif(
-    platform_matches(["win", "mac"]),
+    platform_matches(["win"]),
     reason="Unsupported platform",
 )
 def test():
@@ -27,26 +28,38 @@ def test():
     GaudiKernel.
     See https://its.cern.ch/jira/browse/GAUDI-404
     """
-    # find libGaudiKernel.so in the LD_LIBRARY_PATH
-    libname = "libGaudiKernel.so"
+    # find libGaudiKernel in the library path
+    libext = {"darwin": ".dylib"}.get(sys.platform, ".so")
+    libname = f"libGaudiKernel{libext}"
 
     searchpath = [os.path.curdir, "lib"]
-    # The day we can run the test on other platforms we can do this:
-    # varname = {'darwin': 'DYLD_LIBRARY_PATH',
-    #           'win32': 'PATH'}.get(sys.platform, 'LD_LIBRARY_PATH')
-    varname = "LD_LIBRARY_PATH"
-    searchpath.extend(os.environ.get(varname, "").split(os.pathsep))
+    # On macOS: use GAUDI_PLUGIN_PATH only (LD_LIBRARY_PATH is ignored by SIP)
+    # On Linux: use GAUDI_PLUGIN_PATH and LD_LIBRARY_PATH
+    varnames = ["GAUDI_PLUGIN_PATH"]
+    if sys.platform != "darwin":
+        varnames.append("LD_LIBRARY_PATH")
+    for varname in varnames:
+        searchpath.extend(os.environ.get(varname, "").split(os.pathsep))
 
     libs = [
         p for p in (os.path.join(n, libname) for n in searchpath) if os.path.exists(p)
     ]
     assert libs, f"Cannot find {libname!r} in {searchpath}"
 
-    nm = run(["nm", "-C", libs[0]], stdout=PIPE, text=True)
-    assert nm.returncode == 0, "nm invocation failed"
+    # Get demangled symbols: Linux nm supports -C, macOS needs c++filt
+    if sys.platform == "darwin":
+        nm = run(["nm", libs[0]], stdout=PIPE, text=True)
+        assert nm.returncode == 0, "nm invocation failed"
+        filt = run(["c++filt"], input=nm.stdout, stdout=PIPE, text=True)
+        assert filt.returncode == 0, "c++filt invocation failed"
+        symbols = filt.stdout
+    else:
+        nm = run(["nm", "-C", libs[0]], stdout=PIPE, text=True)
+        assert nm.returncode == 0, "nm invocation failed"
+        symbols = nm.stdout
 
     signature = re.compile(r"MsgStream& operator<< <char \[\d+\]>")
-    lines = [line for line in nm.stdout.splitlines() if signature.search(line)]
+    lines = [line for line in symbols.splitlines() if signature.search(line)]
 
     assert not lines, "found MsgStream operator<< specialization"
     print("SUCCESS: no MsgStream operator<< specialization found")
diff --git a/GaudiTestSuite/options/CustomAppFromOptions.py b/GaudiTestSuite/options/CustomAppFromOptions.py
index ea842684f..e59c988d5 100644
--- a/GaudiTestSuite/options/CustomAppFromOptions.py
+++ b/GaudiTestSuite/options/CustomAppFromOptions.py
@@ -1,5 +1,5 @@
 #####################################################################################
-# (c) Copyright 1998-2019 CERN for the benefit of the LHCb and ATLAS collaborations #
+# (c) Copyright 1998-2026 CERN for the benefit of the LHCb and ATLAS collaborations #
 #                                                                                   #
 # This software is distributed under the terms of the Apache version 2 licence,     #
 # copied verbatim in the file "LICENSE".                                            #
@@ -20,7 +20,7 @@ with warnings.catch_warnings():
     import cppyy
 
 # - we have to load GaudiKernel get the base class
-cppyy.gbl.gSystem.Load("libGaudiKernel.so")
+cppyy.gbl.gSystem.Load("libGaudiKernel")
 
 # - pass to ROOT interpreter our code
 cppyy.gbl.gInterpreter.Declare(
diff --git a/GaudiTestSuite/scripts/ExtendedProperties2.py b/GaudiTestSuite/scripts/ExtendedProperties2.py
index fe47b8065..28b36bce4 100755
--- a/GaudiTestSuite/scripts/ExtendedProperties2.py
+++ b/GaudiTestSuite/scripts/ExtendedProperties2.py
@@ -1,6 +1,6 @@
 #!/usr/bin/env python3
 #####################################################################################
-# (c) Copyright 1998-2023 CERN for the benefit of the LHCb and ATLAS collaborations #
+# (c) Copyright 1998-2026 CERN for the benefit of the LHCb and ATLAS collaborations #
 #                                                                                   #
 # This software is distributed under the terms of the Apache version 2 licence,     #
 # copied verbatim in the file "LICENSE".                                            #
@@ -35,6 +35,9 @@ from Configurables import Gaudi__TestSuite__ExtendedProperties2 as EP2
 # =============================================================================
 
 isWin = sys.platform.startswith("win")
+# On macOS, C++ exceptions thrown through cppyy don't propagate to Python properly
+# and instead terminate the process. See https://github.com/root-project/root/issues/7541
+isMac = sys.platform == "darwin"
 
 ep2 = EP2("xProps2", Point3D=(10, 40, 55), Points3D=[(10, 40, 55), (1, 2, 3)])
 
@@ -78,17 +81,18 @@ if "__main__" == __name__:
 
     xp2.SVector5 = [1, 2, 3, 4, 5]
 
-    try:
-        xp2.SVector5 = [1, 2, 3, 4, 5, 6]
-    except Exception as e:
-        print(" Exception: ", e)
+    if not isMac:
+        try:
+            xp2.SVector5 = [1, 2, 3, 4, 5, 6]
+        except Exception as e:
+            print(" Exception: ", e)
 
-    try:
-        xp2.Point3D = (1, 2, 3, 4)
-    except Exception as e:
-        print(" Exception: ", e)
+        try:
+            xp2.Point3D = (1, 2, 3, 4)
+        except Exception as e:
+            print(" Exception: ", e)
 
-    if not isWin:
+    if not isWin and not isMac:
         try:
             xp2.Vector4D = (1, 2, 3)
         except Exception as e:
diff --git a/GaudiUtils/src/component/SignalMonitorSvc.cpp b/GaudiUtils/src/component/SignalMonitorSvc.cpp
index de6dae661..4376c4dd4 100644
--- a/GaudiUtils/src/component/SignalMonitorSvc.cpp
+++ b/GaudiUtils/src/component/SignalMonitorSvc.cpp
@@ -1,5 +1,5 @@
 /***********************************************************************************\
-* (c) Copyright 1998-2025 CERN for the benefit of the LHCb and ATLAS collaborations *
+* (c) Copyright 1998-2026 CERN for the benefit of the LHCb and ATLAS collaborations *
 *                                                                                   *
 * This software is distributed under the terms of the Apache version 2 licence,     *
 * copied verbatim in the file "LICENSE".                                            *
@@ -20,6 +20,7 @@
 #include <csignal>
 #include <cstring>
 #include <iostream>
+#include <string>
 
 namespace Gaudi {
   namespace Utils {
@@ -148,9 +149,19 @@ namespace Gaudi {
 #include <GaudiKernel/IIncidentSvc.h>
 
 namespace {
-  const char* sig_desc( int signum ) {
-    if ( signum >= NSIG || signum < 0 ) return nullptr;
-    return strsignal( signum );
+  std::string sig_desc( int signum ) {
+    if ( signum >= NSIG || signum < 0 ) return {};
+    const char* desc = strsignal( signum );
+    if ( !desc ) return {};
+    std::string result( desc );
+    // On macOS, strsignal appends ": N" where N is the signal number
+    // Strip this suffix for consistent output across platforms
+    std::string suffix = ": " + std::to_string( signum );
+    if ( result.size() > suffix.size() &&
+         result.compare( result.size() - suffix.size(), suffix.size(), suffix ) == 0 ) {
+      result.resize( result.size() - suffix.size() );
+    }
+    return result;
   }
 
   /// Helper class to map signal names and numbers.
@@ -267,8 +278,8 @@ namespace {
     inline void i_addSignal( int signum, const char* signame ) {
       m_num2id[signum]    = signame;
       m_name2num[signame] = signum;
-      const char* desc    = sig_desc( signum );
-      if ( desc ) {
+      std::string desc    = sig_desc( signum );
+      if ( !desc.empty() ) {
         m_num2desc[signum] = desc;
         m_name2num[desc]   = signum;
       }
diff --git a/cmake/DeveloperBuildType.cmake b/cmake/DeveloperBuildType.cmake
index 6d297a8c9..6e1ccbe9b 100644
--- a/cmake/DeveloperBuildType.cmake
+++ b/cmake/DeveloperBuildType.cmake
@@ -1,5 +1,5 @@
 #####################################################################################
-# (c) Copyright 2020 CERN for the benefit of the LHCb and ATLAS collaborations      #
+# (c) Copyright 2020-2026 CERN for the benefit of the LHCb and ATLAS collaborations #
 #                                                                                   #
 # This software is distributed under the terms of the Apache version 2 licence,     #
 # copied verbatim in the file "LICENSE".                                            #
@@ -34,11 +34,13 @@ endif()
 set(CMAKE_CXX_FLAGS_${_CONFIG} "${_flags}"
     CACHE INTERNAL "Compile flags for ${_config} build type")
 
-# Set linker flags
-foreach(bin_type IN ITEMS EXE STATIC SHARED MODULE)
-    set(CMAKE_${bin_type}_LINKER_FLAGS_${_CONFIG} "-Wl,--no-undefined -Wl,-z,max-page-size=0x1000"
-        CACHE INTERNAL "Linker flags for ${bin_type} with ${_config} build type")
-endforeach()
+# Set linker flags (Linux-specific)
+if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
+    foreach(bin_type IN ITEMS EXE STATIC SHARED MODULE)
+        set(CMAKE_${bin_type}_LINKER_FLAGS_${_CONFIG} "-Wl,--no-undefined -Wl,-z,max-page-size=0x1000"
+            CACHE INTERNAL "Linker flags for ${bin_type} with ${_config} build type")
+    endforeach()
+endif()
 
 # Add the build types to the possible ones
 if(CMAKE_CONFIGURATION_TYPES)
diff --git a/cmake/GaudiToolbox.cmake b/cmake/GaudiToolbox.cmake
index f69ae7b8a..930849cd4 100644
--- a/cmake/GaudiToolbox.cmake
+++ b/cmake/GaudiToolbox.cmake
@@ -1452,21 +1452,27 @@ endfunction()
 function(_merge_files_confdb2 dependency file_to_merge)
     set(merge_target ${PROJECT_NAME}_MergeConfDB2)
     if(NOT TARGET ${merge_target})
+        set(output_file ${CMAKE_BINARY_DIR}/${PROJECT_NAME}.confdb2)
+        # On macOS, Python's shelve may create a .db file depending on the dbm backend
         if(APPLE)
-            set(output_files ${CMAKE_BINARY_DIR}/${PROJECT_NAME}.confdb2 ${CMAKE_BINARY_DIR}/${PROJECT_NAME}.confdb2.db)
-        else()
-            set(output_files ${CMAKE_BINARY_DIR}/${PROJECT_NAME}.confdb2)
+            set(byproduct_files ${output_file}.db)
         endif()
-        add_custom_command(OUTPUT ${output_files}
+        add_custom_command(OUTPUT ${output_file}
+            BYPRODUCTS ${byproduct_files}
             COMMAND run merge_confdb2_parts $<TARGET_PROPERTY:${merge_target},fragments>
-                    --output ${CMAKE_BINARY_DIR}/${PROJECT_NAME}.confdb2
+                    --output ${output_file}
             DEPENDS $<TARGET_PROPERTY:${merge_target},fragments>
             COMMENT "Merging .confdb2 files for ${PROJECT_NAME}"
             COMMAND_EXPAND_LISTS)
-        add_custom_target(${merge_target} ALL DEPENDS ${output_files})
-        install(FILES ${output_files}
+        add_custom_target(${merge_target} ALL DEPENDS ${output_file})
+        install(FILES ${output_file}
             TYPE LIB
             ${_gaudi_install_optional})
+        if(APPLE)
+            install(FILES ${output_file}.db
+                TYPE LIB
+                OPTIONAL)
+        endif()
     endif()
     add_dependencies(${merge_target} ${dependency})
     set_property(TARGET ${merge_target} APPEND PROPERTY fragments "${file_to_merge}")
diff --git a/cmake/tests/testGaudiInstallation.cmake b/cmake/tests/testGaudiInstallation.cmake
index 1437df23b..00491d49a 100644
--- a/cmake/tests/testGaudiInstallation.cmake
+++ b/cmake/tests/testGaudiInstallation.cmake
@@ -117,9 +117,15 @@ elseif(DEFINED TEST_GAUDI_INSTALL_FILES_EXIST) # check some files that should be
             " GAUDI_VERSION.h not found in include/")
     endif()
     # Check shared objects
-    if(NOT EXISTS ${GAUDI_INSTALL_DIR}/lib/libGaudiPluginService.so)
+    # Regular shared libraries use .dylib on macOS, .so on Linux
+    if(APPLE)
+        set(SHLIB_EXT ".dylib")
+    else()
+        set(SHLIB_EXT ".so")
+    endif()
+    if(NOT EXISTS ${GAUDI_INSTALL_DIR}/lib/libGaudiPluginService${SHLIB_EXT})
         message(FATAL_ERROR "Shared libraries not installed correctly,"
-            " libGaudiPluginService.so not found in lib/")
+            " libGaudiPluginService${SHLIB_EXT} not found in lib/")
     endif()
     # Gaudi.components
     if(NOT EXISTS ${GAUDI_INSTALL_DIR}/lib/Gaudi.components)
diff --git a/cmake/tests/testGaudiToolbox.cmake b/cmake/tests/testGaudiToolbox.cmake
index a9d2f3bf8..3fa2dda2f 100644
--- a/cmake/tests/testGaudiToolbox.cmake
+++ b/cmake/tests/testGaudiToolbox.cmake
@@ -1,5 +1,5 @@
 #####################################################################################
-# (c) Copyright 2020 CERN for the benefit of the LHCb and ATLAS collaborations      #
+# (c) Copyright 2020-2026 CERN for the benefit of the LHCb and ATLAS collaborations #
 #                                                                                   #
 # This software is distributed under the terms of the Apache version 2 licence,     #
 # copied verbatim in the file "LICENSE".                                            #
@@ -48,17 +48,24 @@ elseif(DEFINED TEST_INSTALLATION_LAYOUT)
     if(NOT EXISTS ${BIN_DIR}/${INSTALL_DIR})
         message(FATAL_ERROR "the install directory does not exist")
     endif()
-    if(NOT EXISTS ${BIN_DIR}/${INSTALL_DIR}/lib/libfoo.so)
-        message(FATAL_ERROR "libfoo.so shared library is not installed")
+    # Regular shared libraries use .dylib on macOS, .so on Linux
+    if(APPLE)
+        set(SHLIB_EXT ".dylib")
+    else()
+        set(SHLIB_EXT ".so")
     endif()
+    if(NOT EXISTS ${BIN_DIR}/${INSTALL_DIR}/lib/libfoo${SHLIB_EXT})
+        message(FATAL_ERROR "libfoo${SHLIB_EXT} shared library is not installed")
+    endif()
+    # Plugins always use .so (even on macOS)
     if(NOT EXISTS ${BIN_DIR}/${INSTALL_DIR}/lib/libbar.so)
         message(FATAL_ERROR "libbar.so plugin is not installed")
     endif()
     if(NOT EXISTS ${BIN_DIR}/${INSTALL_DIR}/lib/FooBar.components)
         message(FATAL_ERROR "list of all components FooBar.components is not installed")
     endif()
-    if(NOT EXISTS ${BIN_DIR}/${INSTALL_DIR}/lib/libbaz.so)
-        message(FATAL_ERROR "libbaz.so shared library is not installed")
+    if(NOT EXISTS ${BIN_DIR}/${INSTALL_DIR}/lib/libbaz${SHLIB_EXT})
+        message(FATAL_ERROR "libbaz${SHLIB_EXT} shared library is not installed")
     endif()
     if(NOT EXISTS ${BIN_DIR}/${INSTALL_DIR}/bin/qux)
         message(FATAL_ERROR "qux executable is not installed")
