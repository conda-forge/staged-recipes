{% set name = "oeel" %}
{% set version = "0.9.7.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/oeel-{{ version }}.tar.gz
  sha256: 29489ca510c58e85bc513eec206fbac0cb3863c1d526fa788db36ee283854436

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - nodejs >=16
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - earthengine-api
    - pyzmq
    - git
    - nodejs >=16
    
test:
  imports:
    - oeel
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/open-geocomputing/OpenEarthEngineLibrary/tree/oeel-python
  summary: The Open Earth Engine Library Python interface
  license: GPLv3
  license_file: LICENSE
  doc_url: https://www.open-geocomputing.org/OpenEarthEngineLibrary/
  dev_url: https://github.com/open-geocomputing/OpenEarthEngineLibrary/tree/oeel-python

extra:
  recipe-maintainers:
    - giswqs
    - mgravey
