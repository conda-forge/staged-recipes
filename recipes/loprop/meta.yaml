{% set name = "LoProp" %}
{% set version = "0.3.2" %}
{% set sha256 = "63bea6e0399f39be4f8bd40149283a30c526eef2d78b1b2ca078355714bff863" %}
{% set build = 0 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: {{ build }}
  noarch: python
  entry_points:
    - loprop=loprop.__main__:main
  script: {{ PYTHON }} -m pip install .[dalton,vlx] -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - blocked-matrix-utils
    - daltools
    - h5py * *nompi*
    - python >=3.6

test:
  imports:
    - loprop
  commands:
    - loprop --help

about:
  home: https://github.com/vahtras/loprop
  summary: LoProp implementation for Dalton
  license: GPL-2.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - robertodr
    - vahtras
