package:
  name: py-fann2
  version: "1.1.0"

source:
  git_rev: 906e285daf77fbdffffeb58b19423dde96729e10
  git_url:  git@github.com:smithsp/fann2.git

build:
  skip: True  # [win]
  number: 0
  # noarch_python: True
  # preserve_egg_dir: True
  script: 'python setup.py install --single-version-externally-managed --record=record.txt'

requirements:
  build:
    - python
    - setuptools
    - swig
    - fann2

  run:
    - python
    - swig
    - fann2

test:
  imports:
    - fann2

about:
  home: https://github.com/FutureLinkCorporation/fann2
  license: LGPL2
  summary: 'Fast Artificial Neural Network Library (fann) python bindings.'

extra:
  recipe-maintainers:
    - smithsp
