Remove deprecated 'dotenv' dependency.

The 'dotenv' package on PyPI is deprecated and recommends using 'python-dotenv'
instead, which is already listed as a dependency.

--- a/pyproject.toml
+++ b/pyproject.toml
@@ -26,7 +26,6 @@
   "pyyaml>=6.0.2",
   "ruamel.yaml>=0.18.0",
   "jinja2>=3.1.6",
-  "dotenv>=0.9.9",
   "pathspec>=0.12.1",
   "psutil>=7.0.0",
   "docstring_parser>=0.16",
