{% set name = "rpcompletion" %}
{% set version = "6.8.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brsynth/rpCompletion/archive/refs/tags/{{ version }}.tar.gz
  sha256: 841aed46c25dd12fe915449e91e88ee6d8481e623807db5265c894c252c87bba

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - pandas
    - rdkit
    - rr_cache
    - brs_utils
    - rxn_rebuild
    - rplibs

test:
  requires:
    - python {{ python_min }}
  imports:
    - rpcompletion

about:
  home: https://github.com/brsynth/rpCompletion/tree/main
  summary: "Completes mono-component reactions output by RetroPath2.0 with the appropriate cofactors."
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ramez17khaled