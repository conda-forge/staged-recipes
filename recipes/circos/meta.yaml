package:
  name: circos
  version: "1.3.5"

source:
  fn: Circos-1.3.5.tar.gz
  url: https://files.pythonhosted.org/packages/fe/30/966e9d8ca0c8bb63bcefc1147ecf16f5518087c54b7a71cdc43920d9ae67/Circos-1.3.5.tar.gz
  md5: a771f2ac0298bce6e4b1d1c20de35109
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circos = circos:main
    #
    # Would create an entry point called circos that calls circos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 0

requirements:
  build:
    - python
    - numpy
    - matplotlib
    - setuptools
    - pycodestyle

  run:
    - python
    - numpy
    - matplotlib
    - setuptools
    - pycodestyle

test:
  # Python imports
  imports:
    - circos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ericmjl/Circos
  license: MIT
  summary: 'Circos plots in Python!'

extra:
  recipe-maintainers:
    - ericmjl

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
