# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  name: django-ai-core
  version: "0.1.4"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/django_ai_core-${{ version }}.tar.gz
  sha256: 3e8ced754bd24428322584769584198d0877bce232d65cac3b0788aef47d82ca

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - uv_build >=0.8.22,<0.9.0
    - pip
  run:
    - python >=${{ python_min }},<4.0
    - any-llm-sdk ==0.20.3
    - queryish >=0.2.0

tests:
  - python:
      imports:
        - django_ai_core
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Django AI Core provides developer-focused AI features for implementing AI tooling in to Django sites.
  license: BSD-3-Clause
  license_file: LICENSE
  homepage: https://github.com/wagtail/django-ai-core

extra:
  recipe-maintainers:
    - rxm7706
