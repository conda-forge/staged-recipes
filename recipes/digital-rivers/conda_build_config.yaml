# digital-rivers requires Python >=3.11 (pyproject: requires-python = ">=3.11, <4").
# Override the global python_min so the noarch package floors at 3.11 instead of
# the conda-forge default minimum.
python_min:
  - "3.11"
