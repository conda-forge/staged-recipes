{% set name = "seekr2_openmm_plugin" %}
{% set version = "v0.1.0" %}
{% set build = 0 %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/seekrcentral/{{ name }}/archive/refs/tags/{{ version }}.tar.gz
  sha256: c5b0fd99373769fee8e462147e468177d293f65d484a72b99092d8abf37938a9

# used command: shasum -a 256 seekr2_openmm_plugin-0.1.0.tar.gz

build:
  number: {{ build }}
  skip: true  # [py<35 or win or (linux64 and cuda_compiler_version == "None")]
  missing_dso_whitelist:
    - '*/libcuda.*'  # [linux]
#  script_env:
#    - OPENMM_LIB_PATH=$PREFIX/lib
#    - OPENMM_PLUGIN_DIR=$OPENMM_LIB_PATH/plugins
#    - LD_LIBRARY_PATH=$OPENMM_LIB_PATH:$OPENMM_PLUGIN_DIR:$LD_LIBRARY_PATH

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }}  # [linux]
#    - {{ cdt('mesa-libgl-devel') }}  # [linux]
    - cmake
#    - jom  # [win]
    - libstdcxx-ng
    - libgcc-ng

  host:
    - openmm
    - python
    - fftw
    - swig
    - pthread-stubs
    - lxml
    - {{ pin_compatible('numpy') }}
    - cython
    # needed for Python wrappers
    - doxygen 1.8.14
    # OpenCL ICD
    - ocl-icd  # [linux]
    - khronos-opencl-icd-loader  # [osx or win]
    - libstdcxx-ng
    - libgcc-ng

  run:
    - python
    - fftw
    - openmm
    - {{ pin_compatible('numpy') }}
    - swig
    - pthread-stubs
    # OpenCL ICD
    - khronos-opencl-icd-loader  # [osx or win]
    - ocl_icd_wrapper_apple  # [osx]
    - ocl-icd  # [linux]
    - ocl-icd-system  # [linux]
    - libstdcxx-ng
    - libgcc-ng

test:
  requires:
    - pocl  # [unix]
  imports:
    - simtk
    - simtk.openmm
  commands:
#    - python -m simtk.testInstallation
#    # Existence tests Unix
    - test -f $PREFIX/lib/plugins/libSeekr2PluginCUDA$SHLIB_EXT  # [unix]
    - test -f $PREFIX/lib/plugins/libSeekr2PluginReference$SHLIB_EXT  # [unix]

about:
  home: https://github.com/seekrcentral/seekr2_openmm_plugin.git
  license: MIT
  license_family: MIT
  license_file:
    - LICENSE
  summary: An OpenMM plugin for SEEKR2.

  description: |
    Fast and versatile multiscale milestoning to compute molecular 
    thermodynamics and kinetics.
  doc_url: https://seekr2.readthedocs.io/en/latest/
  dev_url: https://github.com/seekrcentral/seekr2.git

extra:
  recipe-maintainers:
    - lvotapka
