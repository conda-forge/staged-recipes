# not using the obvious {cxx,fortran}_compiler naming to avoid
# clashing with the global pinning
compiler_cxx:
  - clang-cl
  - clangxx
  - gxx
  - vc
compiler_fortran:
  - flang
  - gfortran
  - ifx
