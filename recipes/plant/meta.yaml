{% set name = "plant" %}
{% set version = "0.5.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  path: https://gitlab.com/plant/plant/-/archive/v0.5.5/plant-v{{ version }}.tar.gz
  sha256: a80eeb10af5653f7200bf3da5241c59081f49209faed

build:
  noarch: python
  entry_points:
    - plant_info = plant_info:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build:
    - python >=3.9
    - setuptools
    - numpy >=2.0
    - pip
    - gdal
    - matplotlib-base
    - scipy
    - pillow
    - h5py
    - python-dateutil
    - numexpr

  host:
    - python >=3.9
    - setuptools >=64.0
    - setuptools-scm >=6.2
    - pip >=21.3

  run:
    - python >=3.9
    - setuptools
    - numpy >=2.0
    - poppler
    - gdal
    - h5py
    - ncurses
    - matplotlib-base
    - scipy
    - pillow
    - jupyter
    - python-dateutil
    - numexpr

test:
  imports:
    - plant
  commands:
    - pip check
    - plant_info --help
  requires:
    - pip

about:
  home: https://gitlab.com/plant/plant
  summary: PLANT is a collection of software tools developed at the Jet Propulsion Laboratory to support processing and analysis of Synthetic Aperture Radar (SAR) data for ecosystem and land-cover/land-use change science and applications.
  license: CC0-1.0
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - gshiroma
