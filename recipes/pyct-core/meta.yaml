{% set name = "pyct-core" %}
{% set version = "0.4.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/p/pyct/pyct-0.4.5.tar.gz

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"

requirements:
  run_constrained:
    - pyct {{ version }}
  host:
    - python
    - pip
  run:
    - python

test:
  imports:
    - pyct
    - pyct.build

about:
  home: http://github.com/simplejson/simplejson
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: 'Common tasks for package building (e.g. bundle examples)'
  doc_url: https://github.com/pyviz/pyct#pyctbuild
  dev_url: https://github.com/pyviz/pyct

extra:
  recipe-maintainers:
    - ceball
