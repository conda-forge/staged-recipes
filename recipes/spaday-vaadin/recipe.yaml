context:
  version: "0.1.0"

package:
  name: spaday-vaadin
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/spaday-vaadin/spaday_vaadin-${{ version }}.tar.gz
  sha256: afb9983f74732caaab2455223087dcda56d41755e737fc031a067cc93081a9a7

build:
  number: 0
  noarch: python
  script:
    env:
      SKIP_HATCH_JS: "1"
    content:
      - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling
    - hatch-js
  run:
    - python >=${{ python_min }}
    - spaday >=0.8.2,<1

tests:
  - python:
      imports:
        - spaday_vaadin
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  license: Apache-2.0
  license_file: LICENSE
  summary: Vaadin components and browser assets for spaday
  homepage: https://github.com/1kbgz/spaday-vaadin
  repository: https://github.com/1kbgz/spaday-vaadin

extra:
  recipe-maintainers:
    - timkpaine
