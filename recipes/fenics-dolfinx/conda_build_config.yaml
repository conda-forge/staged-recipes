mpi:
  - mpich
  # - openmpi  # add variant after merge

pin_run_as_build:
  boost-cpp:
    max_pin: x.x.x
  parmetis:
    max_pin: x.x
  pybind11:
    max_pin: x.x.x
  fenics-ufcx:
    max_pin: x.x.x
  xtensor:
    max_pin: x.x
