context:
  version: 0.5.3.3

package:
  name: graphriccicurvature
  version: ${{ version }}

source:
  url: https://github.com/AlainKadar/GraphRicciCurvature/archive/refs/tags/v${{ version }}.tar.gz
  sha256: b2d60e3deb62ad499cd62d451e3f3f0488bd94b4655dee1d79ebd8e960aadd89

build:
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - cython
    - numpy
    - networkx >=2.0
    - pot >=0.8.0
    - packaging
    - networkit >=6.1
    - python-louvain
    - tabulate

tests:
  - python:
      imports:
        - GraphRicciCurvature

about:
  summary: Compute discrete Ricci curvatures and Ricci flow on NetworkX graphs.
  license: Apache-2.0
  license_file: LICENSE.txt
  homepage: https://github.com/saibalmars/GraphRicciCurvature

extra:
  recipe-maintainers:
    - AlainKadar
