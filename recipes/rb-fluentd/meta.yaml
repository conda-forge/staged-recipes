{% set name = "fluentd" %}
{% set version = "1.7.0" %}

package:
  name: rb-{{ name|lower }}
  version: {{ version }}

source:
  url: https://rubygems.org/downloads/{{ name }}-{{ version }}.gem
  sha256: 4738ff9a4f19ec512388e2021dd388becabd0a5d862e9d6c0a8896f4c368d2df

build:
  noarch: generic
  number: 0
  script:
    - gem install -N -l -V --norc --ignore-dependencies {{ name }}-{{ version }}.gem
    - gem unpack {{ name }}-{{ version }}.gem
  skip: {{ win }}

requirements:
  host:
    - ruby
  run:
    - ruby
    - rb-http_parser.rb >=0.5.1,<0.7.0
    - rb-tzinfo >=2.0,<3
    - rb-tzinfo-data >=1.0,<2
    - rb-msgpack >=0.7.0,<2.0.0
    - rb-yajl-ruby >=1.0,<2
    - rb-sigdump >=0.2.2,<1.0.0
    - rb-cool.io >=1.4.5,<2.0.0
    - rb-strptime >=0.2.2,<1.0.0

test:
  commands:
    - ruby -r fluent -e 'exit 0'

about:
  home: https://rubygems.org/gems/{{ name }}
  license: Apache-2.0
  license_family: APACHE
  license_file: {{ name }}-{{ version }}/LICENSE
  summary: |
    Fluentd is an open source data collector designed to scale and simplify
    log management. It can collect, process and ship many kinds of data
    in near real-time.
  doc_url: https://www.rubydoc.info/gems/{{ name }}
  dev_url: https://github.com/fluent/fluentd

extra:
  recipe-maintainers:
    - oblute
    - benhuff
    - rluria14
    - sodre
