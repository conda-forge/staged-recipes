package:
  name: autograd
  version: "1.1.5"

source:
  fn: autograd-1.1.5.tar.gz
  url: https://files.pythonhosted.org/packages/e5/70/e9c8db296d6ff33a17fec0ecaf11e3efbc85722d042365d01704046befef/autograd-1.1.5.tar.gz
  md5: 2b65978109361a6ffb6986d03aebc157
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autograd = autograd:main
    #
    # Would create an entry point called autograd that calls autograd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 0

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.9
    - future >=0.15.2
    - scipy

  run:
    - python
    - numpy >=1.9
    - future >=0.15.2
    - scipy

test:
  # Python imports
  imports:
    - autograd
    - autograd.numpy
    - autograd.scipy
    - autograd.scipy.stats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HIPS/autograd
  license: MIT License
  summary: 'Efficiently computes derivatives of numpy code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
