{% set name = "topfarm" %}
{% set version = "2.4.3" %}
{% set python_min = "3.9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: ba744464ee3156c34f69667945cd88a316f22b4c78476552137ac56137b4693d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - hatch
    - hatch-vcs
    - setuptools
  run:
    - python >={{ python_min }}
    - matplotlib-base
    - numpy
    - numpy-financial
    - openmdao >=3.16
    - py-wake >=2
    - pydoe2
    - pydoe3 >=1.0.4,<2
    - scipy
    - scikit-learn
    - shapely >=2.0.6,<3

test:
  imports:
    - topfarm
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://gitlab.windenergy.dtu.dk/TOPFARM/topfarm2
  summary: Topfarm - Wind farm optimization using OpenMDAO
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - neda-dtu
    - madsmpedersen
    - simutisernestas
