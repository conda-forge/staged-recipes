package:
  name: pytorch-dev-deps
  version: 1.0

requirements:
  run:
    - python >=3.6
    - numpy !=1.20.0
    - ninja
    - pyyaml
    - mkl
    - mkl-include
    - setuptools
    - cmake
    - cffi
    - typing
    - pytest
    - compilers
    - flake8
    - psutil
    - hypothesis
    - mypy ==0.812

test:
  commands:
    - echo "Environment was successfully solved!"

build:
  script:
    - install -Dm664 {{ RECIPE_DIR }}/activate.sh {{ PREFIX }}/etc/conda/activate.d/activate-pytorch-dev-deps.sh  # [unix]

about:
  summary: Development dependencies to work on PyTorch.

extra:
  recipe-maintainers:
    - pmeier
    - pearu
