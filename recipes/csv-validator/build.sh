#!/bin/bash

# Download .zip to a temp directory
# Unzip
# Copy over .sh and csv-validator-cmd-X.Y.jar and the /lib/ to Conda environment directory
# Set up environment variables if needed (like to control JVM memory)