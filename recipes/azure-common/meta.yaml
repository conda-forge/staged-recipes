{% set version = "1.1.3" %}

package:
  name: azure-common
  version: {{ version }}

source:
  fn: azure-common-{{ version }}.zip
  url: https://pypi.python.org/packages/source/a/azure-common/azure-common-{{ version }}.zip
  md5: 22d748f056f7bad87f2627822952f8d7
  
build:
  script: pip install --no-deps .
  number: 0

requirements:
  build:
    - python
    - pip
    - setuptools
    - azure-nspkg
  run:
    - python
    - azure-nspkg

test:
  imports:
    - azure
    - azure.common

about:
  home: https://github.com/Azure/azure-sdk-for-python
  license: Apache License 2.0
  summary: 'Microsoft Azure Client Libraries for Python'

extra:
  recipe-maintainers:
    - ivoflipse
    - Korijn
    - Maxyme
