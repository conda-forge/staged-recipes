{% set name = "stylelint" %}
{% set version = "16.8.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/stylelint/stylelint/archive/{{ version }}.tar.gz
  sha256: 6a19de652fbddccaaf1479ac1f04d1617e337cc5df96af4efa8df4eba397e27f

build:
  noarch: generic
  number: 0

requirements:
  build:
    - nodejs
    - pnpm
    - pnpm-licenses
  run:
    - nodejs

test:
  commands:
    - stylelint --help

about:
  home: https://github.com/stylelint/stylelint
  license: MIT
  license_file:
    - LICENSE
    - third-party-licenses.txt
  summary: A mighty CSS linter that helps you avoid errors and enforce conventions.
  dev_url: https://github.com/stylelint/stylelint

extra:
  recipe-maintainers:
    - danielnachun
