# conda_build_config.yaml
# Configuration for WriteFreely conda build

# Use the CGO compiler which includes C compiler support
cgo_compiler:
  - go-cgo

# SQLite version pin (use conda-forge global pinning)
# sqlite is already pinned globally, but we can be explicit
# sqlite:
#   - 3
