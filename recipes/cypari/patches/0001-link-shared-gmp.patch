--- a/setup.py
+++ b/setup.py
@@ -337,7 +337,7 @@
 else:
     compile_args = []
 
-link_args += [pari_static_library, gmp_static_library]
+link_args += [pari_static_library, '-L' + gmp_library_dir, '-lgmp']
 if sys.platform.startswith('linux'):
     link_args += ['-Wl,-Bsymbolic-functions', '-Wl,-Bsymbolic']
 
