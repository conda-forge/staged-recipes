{% set name = "stompy-ocean" %}
{% set version = "0.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/stompy_ocean-{{ version }}.tar.gz
  sha256: 32edb41f5d5ff609c6f8386732eac1e2086c00df78ac91676de38d2088b66566

build:
  noarch: python
  entry_points:
    - realpython = stompy.__main__:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
  host:
    - python >=3
    - setuptools >=67.0.0
    - wheel
    - pip
    - cgal-cpp
  run:
    - python >=3
    - cmocean
    - matplotlib-base
    - netcdf4
    - pandas
    - requests
    - scipy
    - shapely
    - six
    - xarray
    - seawater
    - gdal
    - cgal
    - cgal-cpp
    - numpy

test:
  imports:
    - stompy
    - stompy.grid
    - stompy.grid.unstructured_grid
    - stompy.io
    - stompy.model
    - stompy.plot
    - stompy.spatial
  commands:
    - pip check
    - realpython --help
  requires:
    - pip

about:
  home: https://github.com/rustychris/stompy
  summary: Various python modules related to modeling and oceanographic data analysis.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - klarrieu
