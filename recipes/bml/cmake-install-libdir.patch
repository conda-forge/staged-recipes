>diff --git a/build.sh b/build.sh
index 1663202..adc5a56 100755
--- a/build.sh
+++ b/build.sh
@@ -41,6 +41,7 @@ step and the build:
 EOF
     set_defaults
     echo "CMAKE_BUILD_TYPE       {Release,Debug}             (default is ${CMAKE_BUILD_TYPE})"
+    echo "CMAKE_INSTALL_LIBDIR   {lib,lib64}                 (default is ${CMAKE_INSTALL_LIBDIR})"
     echo "BUILD_SHARED_LIBS      Build a shared library      (default is ${BUILD_SHARED_LIBS})"
     echo "CC                     Path to C compiler          (default is ${CC})"
     echo "CXX                    Path to C++ compiler        (default is ${CXX})"
@@ -71,6 +72,7 @@ EOF
 
 set_defaults() {
     : ${CMAKE_BUILD_TYPE:=Release}
+    : ${CMAKE_INSTALL_LIBDIR:=lib}
     : ${BUILD_SHARED_LIBS:=no}
     : ${CC:=gcc}
     : ${CXX:=g++}
@@ -140,6 +142,7 @@ configure() {
     fi
     ${CMAKE:=cmake} .. \
         -DCMAKE_BUILD_TYPE="${CMAKE_BUILD_TYPE}" \
+        -DCMAKE_INSTALL_LIBDIR="${CMAKE_INSTALL_LIBDIR}" \
         -DCMAKE_C_COMPILER="${CC}" \
         -DCMAKE_CXX_COMPILER="${CXX}" \
         -DCMAKE_Fortran_COMPILER="${FC}" \
