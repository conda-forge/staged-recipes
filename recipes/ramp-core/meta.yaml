{% set python_min = "3.11" %}
{% set version = "0.0.4" %}

package:
  name: ramp-core
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/r/ramp-core/ramp_core-{{ version }}.tar.gz
  sha256: b512bc6526ca9937ab71eb3fe13659cc0167a849778c4e94fb54bdc12c7a8cf4

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}.*
    - setuptools
    - setuptools-scm
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - cytoolz
    - numpy

test:
  imports:
    - ramp_core
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}.*

about:
  home: https://pypi.org/project/ramp-core/
  summary: Utilities package for the Nuclear RAMP
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - esheder
