{% set name = "catx" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/catx-{{ version }}.tar.gz
  sha256: 7b4d7e50edbccaacd9aa0f23f05b689c97f01afef18483164f6d7169cf78bbd6

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - dm-haiku
    - matplotlib-base
    - numpy
    - optax
    - python >=3.6
    - tensorflow

test:
  imports:
    - catx
  commands:
    - pytest tests
  source_files:
    - tests
  requires:
    - pytest
    - pytest-mock
    - scikit-learn

about:
  home: https://github.com/instadeepai/catx
  summary: Contextual Bandits with Continuous Actions in JAX
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - cyprienc
    - WissBe
