{% set version = '0.9.2' %}
{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-tsutils
  version: {{ version|replace("-", "_") }}

source:
  url:
    - {{ cran_mirror }}/src/contrib/tsutils_{{ version }}.tar.gz
    - {{ cran_mirror }}/src/contrib/Archive/tsutils/tsutils_{{ version }}.tar.gz
  sha256: 7d41b34e17256572da2eec53951de42f8a8124013c7634362570d8961470e83e

build:
  merge_build_host: True  # [win]
  number: 0
  noarch: generic
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - {{posix}}zip               # [win]
  host:
    - r-base
    - r-mapa
    - r-rcolorbrewer
    - r-forecast
    - r-plotrix
  run:
    - r-base
    - r-mapa
    - r-rcolorbrewer
    - r-forecast
    - r-plotrix

test:
  commands:
    - $R -e "library('tsutils')"           # [not win]
    - "\"%R%\" -e \"library('tsutils')\""  # [win]

about:
  home: https://github.com/trnnick/tsutils/
  license: GPL-3
  summary: 'Includes: (i) tests and visualisations that can help the modeller explore time series
    components and perform decomposition; (ii) modelling shortcuts, such as functions
    to construct lagmatrices and seasonal dummy variables of various forms; (iii) an
    implementation of the Theta method; (iv) tools to facilitate the design of the forecasting
    process, such as ABC-XYZ analyses; and (v) "quality of life" functions, such as
    treating time series for trailing and leading values.'
  license_family: GPL3
  license_file:
    - '{{ environ["PREFIX"] }}/lib/R/share/licenses/GPL-3'

extra:
  recipe-maintainers:
    - conda-forge/r

# Package: tsutils
# Type: Package
# Title: Time Series Exploration, Modelling and Forecasting
# Version: 0.9.2
# Authors@R: c( person("Nikolaos", "Kourentzes",, "nikolaos@kourentzes.com", c("aut", "cre")), person("Ivan", "Svetunkov",, "ivan@svetunkov.ru", c("ctb")), person("Oliver", "Schaer",, "info@oliverschaer.ch", c("ctb")))
# Description: Includes: (i) tests and visualisations that can help the modeller explore time series components and perform decomposition; (ii) modelling shortcuts, such as functions to construct lagmatrices and seasonal dummy variables of various forms; (iii) an implementation of the Theta method; (iv) tools to facilitate the design of the forecasting process, such as ABC-XYZ analyses; and (v) "quality of life" functions, such as treating time series for trailing and leading values.
# Imports: RColorBrewer, forecast, MAPA, plotrix
# Suggests: thief
# License: GPL-3
# Encoding: UTF-8
# LazyData: true
# URL: https://github.com/trnnick/tsutils/
# RoxygenNote: 6.1.1
# NeedsCompilation: no
# Packaged: 2020-02-06 05:19:16 UTC; Nikos
# Author: Nikolaos Kourentzes [aut, cre], Ivan Svetunkov [ctb], Oliver Schaer [ctb]
# Maintainer: Nikolaos Kourentzes <nikolaos@kourentzes.com>
# Repository: CRAN
# Date/Publication: 2020-02-06 10:20:05 UTC
