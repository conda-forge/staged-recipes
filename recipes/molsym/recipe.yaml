schema_version: 1

context:
  version: 1.1.0

package:
  name: molsym
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/m/molsym/molsym-${{ version }}.tar.gz
  sha256: 9028482c104595e613f40f948daf4bd428c2b0b910a4e3f37bc08ed8a8b631dc

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=64
    - setuptools-scm >=8
    - pip
  run:
    - python >=${{ python_min }}
    - numpy >=1.23.4

tests:
  - python:
      imports:
        - molsym
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: A program for molecular symmetry detection and SALC construction
  license: MIT
  license_file: LICENSE.txt
  homepage: https://github.com/NASymmetry/MolSym
  documentation: https://molsym.readthedocs.io

extra:
  recipe-maintainers:
    - nlk36701
