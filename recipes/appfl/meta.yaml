{% set name = "appfl" %}
{% set version = "0.4.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/APPFL/APPFL/archive/v{{ version }}.tar.gz
  sha256: 4d596069402b3b69329f00063a304c4f964cc16fecd069868d3d5f7b50f74d75
  patches:
    - credentials.patch

build:
  entry_points:
    - appfl-auth=appfl.login_manager.globus.cli:auth
    - appfl-install-compressor=appfl.compressor.install:install_compressor
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.6
    - setuptools >=42
    - wheel
    - pip
  run:
    - python >=3.6
    - numpy
    - pytorch
    - grpcio
    - grpcio-tools
    - omegaconf
    - mpi4py
    - boto3
    - botocore
    - globus-compute-sdk
    - zfpy
    - python-blosc
    - python-zstd
    - scipy
    - lz4
    - python-xz

test:
  imports:
    - appfl
  commands:
    - pip check
    - appfl-auth --help
    - appfl-install-compressor --help
  requires:
    - pip

about:
  home: https://github.com/APPFL/APPFL
  summary: An open-source package for privacy-preserving federated learning
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
