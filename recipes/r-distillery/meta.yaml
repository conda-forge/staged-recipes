{% set posix = 'm2-' if win else '' %}
{% set native = 'm2w64-' if win else '' %}

package:
  name: r-distillery
  # Note that conda versions cannot contain -, so any -'s in the version have
  # been replaced with _'s.
  version: "1.0_2"

source:
  fn: distillery_1.0-2.tar.gz
  url:
    - http://cran.r-project.org/src/contrib/distillery_1.0-2.tar.gz
    - http://cran.r-project.org/src/contrib/Archive/distillery/distillery_1.0-2.tar.gz


  # You can add a hash for the file here, like md5 or sha1
  # md5: 49448ba4863157652311cc5ea4fea3ea
  # sha1: 3bcfbee008276084cbb37a2b453963c61176a322
  # patches:
   # List any patch files here
   # - fix.patch

build:
  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

  # This is required to make R link correctly on Linux.
  rpaths:
    - lib/R/lib/
    - lib/


requirements:
  build:
    - r-base

  run:
    - r-base

test:
  commands:
    # You can put additional test commands to be run here.
    - $R -e "library('distillery')" # [not win]
    - "\"%R%\" -e \"library('distillery')\"" # [win]

  # You can also put a file called run_test.py, run_test.sh, or run_test.bat
  # in the recipe that will be run at test time.

  # requires:
    # Put any additional test requirements here.

about:
  home: http://www.ral.ucar.edu/staff/ericg
  license: GPL (>= 2)
  summary: Some very simple method functions for confidence interval calculation and to distill
    pertinent information from a potentially complex object; primarily used in common
    with packages extRemes and SpatialVx.
  license_family: GPL3

# The original CRAN metadata for this package was:

# Package: distillery
# Version: 1.0-2
# Date: 2015-07-23
# Title: Method Functions for Confidence Intervals and to Distill Information from an Object
# Author: Eric Gilleland
# Maintainer: Eric Gilleland <ericg@ucar.edu>
# Depends: R (>= 2.10.0)
# Description: Some very simple method functions for confidence interval calculation and to distill pertinent information from a potentially complex object; primarily used in common with packages extRemes and SpatialVx.
# License: GPL (>= 2)
# URL: http://www.ral.ucar.edu/staff/ericg
# NeedsCompilation: no
# Packaged: 2015-07-23 17:24:25 UTC; ericg
# Repository: CRAN
# Date/Publication: 2015-07-24 07:53:51

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
