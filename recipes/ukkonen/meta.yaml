{% set name = "ukkonen" %}
{% set version = "1.0.1" %}
{% set sha256 = "5f3fd0e9c1b2a7ea669382ec3928370f11882cec991c4d3757955b56d18895f6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/asottile/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - python
    - setuptools
    - pip
  run:
    - python

test:
  imports:
    - {{ name }}

about:
  home: https://github.com/asottil/{{ name }}
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Implementation of bounded Levenshtein distance (Ukkonen).
  description: Implementation of bounded Levenshtein distance (Ukkonen).
  doc_url: https://github.com/asottil/{{ name }}
  dev_url: https://github.com/asottil/{{ name }}

extra:
  recipe-maintainers:
    - tdegeus
