{% set name = "tnt" %}
{% set version = "126" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://math.nist.gov/tnt/tnt_{{ version }}.zip
  sha256: 93c7cdd116a6faeb679890a52d5b513055388c7faaac74beea570f8f2edbd7e4

build:
  number: 0

requirements:
  build:
    
  host:

  run:

test:
  commands:
    - test -e ${PREFIX}/include/tnt

about:
  home: https://math.nist.gov/tnt/
  license: NIST-PD
  license_file: LICENSE.txt
  summary: Template Numerical Toolkit
  description:

extra:
  recipe-maintainers:
    - oleg-alexandrov

