schema_version: 1

context:
  name: standard-telnetlib
  version: 3.13.0

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/standard_telnetlib-${{ version }}.tar.gz
  sha256: 243333696bf1659a558eb999c23add82c41ffc2f2d04a56fae13b61b536fb173

build:
  number: 0
  noarch: python
  script:
    - if: build_type == "regular"
      then: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - if: build_type == "regular"
      then:
        - python 3.13.*
        - pip
        - setuptools >=75.0
  run:
    - if: build_type == "regular"
      then: python >=3.13
      else: python >=${{ python_min }},<3.13

tests:
  - if: build_type == "regular"
    then:
      - python:
          imports:
            - telnetlib
          pip_check: true
          python_version: "3.*"
    else:
      - python:
          imports:
            - telnetlib
          pip_check: true
          python_version: ${{ python_min }}.*

about:
  homepage: https://github.com/youknowone/python-deadlib
  summary: Standard library telnetlib redistribution. "dead battery".
  license: PSF-2.0
  license_file: LICENSE
  repository: https://github.com/youknowone/python-deadlib

extra:
  recipe-maintainers:
    - beenje
