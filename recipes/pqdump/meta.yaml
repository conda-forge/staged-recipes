package:
  name: pqdump
  version: "0.1.0"

source:
  git_url: https://github.com/charles-turner-1/pqdump.git
  git_tag: v0.1.0

build:
  number: 0
  script: cargo build --release --locked

requirements:
  build:
    - rust
    - {{ compiler('c') }}
  host:
  run:

about:
  home: https://github.com/charles-turner-1/pqdump
  summary: CLI tool for inspecting parquet files
  license: Apache 2.0
