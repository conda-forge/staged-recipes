--- setup.py.orig
+++ setup.py
@@ -170,7 +170,7 @@ except (OSError, ValueError, subprocess.CalledProcessError):
     geos_includes = []
     geos_library_dirs = []
     if sys.platform.startswith('win'):
-        geos_libraries = ['geos']
+        geos_libraries = ['geos_c']
     else:
         geos_libraries = ['geos_c']
 else:
