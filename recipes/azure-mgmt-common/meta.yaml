{% set version = "0.20.0" %}

package:
  name: azure-mgmt-common
  version: {{ version }}

source:
  fn: azure-mgmt-common{{ version }}.zip
  url: https://pypi.python.org/packages/source/a/azure-mgmt-common/azure-mgmt-common-{{ version }}.zip
  md5: f82f47f476964140576576734436ba4d
  
build:
  script: pip install --no-deps .
  number: 0

requirements:
  build:
    - python
    - pip
    - setuptools
    - requests
    - azure-common
    - azure-mgmt-nspkg
  run:
    - python
    - requests
    - azure-common
    - azure-mgmt-nspkg

test:
  imports:
    - azure
    - azure.mgmt
    - azure.mgmt.common

about:
  home: https://github.com/Azure/azure-sdk-for-python
  license: Apache License 2.0
  summary: 'Microsoft Azure Resource Management Client Library for Python (Common)'

extra:
  recipe-maintainers:
    - ivoflipse
    - Korijn
    - Maxyme
