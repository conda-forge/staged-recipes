{% set name = "hybridcontents" %}
{% set version = "0.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: af8e3f6886533748761e41bd12690a9c4854e2a12baae2d49f5b72258f3138e4

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - pip
    - python
    - notebook[test] >=4.0
    - six >=1.9.0
    - requests >=2.7.0
  run:
    - python
    - notebook[test] >=4.0
    - six >=1.9.0
    - requests >=2.7.0

test:
  imports:
    - hybridcontents[test]
  requires:
    - pytest
    - mock
    - nose
  source_files:
    - hybridcontents/tests/*.py
  commands:
    - pytest hybridcontents/tests/

about:
  home: https://github.com/viaduct-ai/hybridcontents
  license: Apache Software
  license_family: APACHE
  license_file: LICENSE
  summary: "Hybrid Content Manager"

  # The remaining entries in this section are optional, but recommended.
  description: "A ContentsManager wrapper for using multiple ContentsManager in Jupyter"
  doc_url: https://github.com/viaduct-ai/hybridcontents
  dev_url: https://github.com/viaduct-ai/hybridcontents

extra:
  recipe-maintainers:
    - yzq89
    - devstein
