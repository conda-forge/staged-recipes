{% set name = "pyorerun" %}
{% set version = "0.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/Ipuch/pyorerun/archive/{{ version }}.tar.gz
  sha256: 5526a7a60cf4a9a247165e763700745938eaceff8deaed507ecf967664d32641

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.10
    - pip
  run:
    - python >=3.10
    - ezc3d
    - rerun-sdk
    - numpy
    - trimesh
    - biorbd

test:
  imports:
    - pyorerun
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/Ipuch/pyorerun
  summary: 'Display c3d from python and pyomeca simulation'
  description: |
    Rerun C3D is a tool to rerun the C3D file and pyomeca simulations.
  license: MIT
  license_family: MIT
  license_file: LICENSE.md
  dev_url: https://github.com/Ipuch/pyorerun

extra:
  recipe-maintainers:
    - ipuch
