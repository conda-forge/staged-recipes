{% set name = "pymsym" %}
{% set version = "0.3.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/corinwagen/pymsym/archive/v{{ version }}.tar.gz
  sha256: 3a15e1bada1991dea8e084405effff081ef4b51c86cb2d13f73b5490e5172449

build:
  skip: True  # [py<39]
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build: 
    - {{ compiler('c') }}
    - cmake
    - make  # [not win]
    - ninja  # [win]
  host:
    - python
    - scikit-build-core
    - pip
  run:
    - python

test:
  imports:
    - libmsym
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/corinwagen/pymsym
  summary: libmsym python binding
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - corinwagen
