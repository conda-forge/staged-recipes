{% set name = "optuna" %}
{% set version = "0.17.1" %}
{% set sha256 = "3d1d3547340c47f34f3a416a2e0761a0ff887ae8ce06474e84ebcc8600afd438" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: "{{ sha256 }}"

build:
  number: 0
  entry_points:
    - optuna = optuna.cli:main
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - alembic
    - cliff
    - colorlog
    - numpy
    - pip
    - python
    - scipy
    - six
    - sqlalchemy >=1.1.0
    - typing
  run:
    - alembic
    - cliff
    - colorlog
    - numpy
    - python
    - scipy
    - six
    - sqlalchemy >=1.1.0
    - typing

test:
  imports:
    - optuna
    - optuna.integration
    - optuna.pruners
    - optuna.samplers
    - optuna.samplers.tpe
    - optuna.storages
    - optuna.storages.rdb
    - optuna.testing
    - tests
    - tests.integration_tests
    - tests.pruners_tests
    - tests.samplers_tests
    - tests.storages_tests
    - tests.storages_tests.rdb_tests
  commands:
    - optuna --help
  source_files:
    - tests

about:
  home: https://optuna.org/
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A hyperparameter optimization framework
  doc_url: https://optuna.readthedocs.io/
  dev_url: https://github.com/pfnet/optuna

extra:
  recipe-maintainers:
    - g-votte
    - hvy
    - iwiwi
    - sile
    - smly
    - toshihikoyanase
