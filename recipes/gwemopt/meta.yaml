{% set name = "gwemopt" %}
{% set version = "0.1.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/gwemopt-{{ version }}.tar.gz
  sha256: b102a204d737bad8027046da1fcad7ed4c161c674158f28602726ca18c888dd2

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - setuptools >=45
    - setuptools-scm
    - pip
  run:
    - python >=3.8
    - numpy >=1.7.1
    - scipy >=1.2.1
    - matplotlib-base >=1.2.0
    - astropy >=1.1.1
    - astropy-healpix
    - python-dateutil
    - mocpy >=0.12.2
    - astroquery
    - astroplan
    - ephem
    - pyyaml
    - voeventlib
    - joblib
    - ligo-segments
    - ligo.skymap
    - lxml
    - h5py
    - healpy
    - munkres
    - pandas
    - shapely
    - pytest >=3.1
    - pytest-runner
    - freezegun
    - coveralls
    - black
    - isort
    - pre-commit
    - pytables
    - regions

test:
  imports:
    - gwemopt
  commands:
    - pip check
    - gwemopt_run --help
  requires:
    - pip

about:
  home: https://github.com/skyportal/gwemopt
  summary: A python package for GW-EM Followup Optimization
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mcoughlin
