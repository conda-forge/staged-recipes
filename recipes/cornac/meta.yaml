{% set name = "cornac" %}
{% set version = "0.1.0.post3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: d375cee57d46f01e61fbb4a0c867d14f51e1bb43bdc493649635de3c859a9be0

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cython
    - numpy 1.11.*
  host:
    - numpy >=1.11
    - pandas
    - pip
    - python
    - scipy
    - tensorflow >=1.2.1
    - pytorch >=0.4.0
  run:
    - numpy >=1.11
    - pandas
    - python
    - scipy
    - tensorflow >=1.2.1
    - pytorch >=0.4.0

test:
  imports:
    - cornac
    - cornac.data
    - cornac.datasets
    - cornac.eval_strategies
    - cornac.experiment
    - cornac.metrics
    - cornac.models
    - cornac.utils

about:
  home: https://cornac.preferred.ai/
  license: Apache 2.0 
  summary: A collection of recommendation algorithms and comparisons

extra:
  recipe-maintainers:
    - tqtg 
