diff --git a/cpp/utils/semver/semver.h b/cpp/utils/semver/semver.h
index 7a873a3..1fb5147 100644
--- a/cpp/utils/semver/semver.h
+++ b/cpp/utils/semver/semver.h
@@ -3,6 +3,17 @@
 #include <string>
 #include <ostream>
 
+// glibc < 2.28 (including the conda-forge cos7 sysroot) defines `major()` and
+// `minor()` as macros in <sys/types.h> (pulled in transitively by <string>),
+// which would otherwise clobber the Semver::major / Semver::minor member names
+// declared below.
+#ifdef major
+#undef major
+#endif
+#ifdef minor
+#undef minor
+#endif
+
 namespace runai::llm::streamer::utils
 {
 
