diff --git a/MEGnet/megnet_qc_plots.py b/MEGnet/megnet_qc_plots.py
index 052b458..96addca 100755
--- a/MEGnet/megnet_qc_plots.py
+++ b/MEGnet/megnet_qc_plots.py
@@ -11,6 +11,7 @@ USAGE:
     * Check as: python megnet_qc_plots.py --help
 """
 import mne
+import argparse
 from numpy import arange
 from os import path, makedirs, cpu_count
 from glob import glob
@@ -116,9 +117,9 @@ def plot_all(results_dir=None, ica_file=None, data_file=None, apply_filter=False
         raw.save(new_file, overwrite=True, verbose=None)
         print(f'ICA was checked/applied manually and saved to: {new_file}.\n')
 
-if __name__ == '__main__':
+def main():
+    """Run the MEGnet quality-control plotting command."""
     plt.ioff()
-    import argparse
     parser = argparse.ArgumentParser(description='An extension tool to plot ica components for post-operation quality check.')
     parser.add_argument('-dir',    '--results_dir',  default=None, type=str, help='Path where ICA results are saved.')
     parser.add_argument('-ica',    '--ica_file',     default=None, type=str, help='ica file.')
@@ -134,4 +135,7 @@ if __name__ == '__main__':
              apply_filter=args.apply_filter, lfreq=args.lfreq, hfreq=args.hfreq, block=args.block,
              apply_ica=args.apply_ica)
     print('...DONE.')
-    
+
+
+if __name__ == '__main__':
+    main()
diff --git a/pyproject.toml b/pyproject.toml
index e027a18..0b81753 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -20,7 +20,9 @@ classifiers=[
         "Operating System :: POSIX",
     ]
 dependencies = [
-    'mne>1.10', 'numpy', 'scipy', 'pandas', 'munch', 'nibabel', 'joblib',  'torch', 'keras>3.0', 'scikit-learn', 'huggingface_hub>=0.24.0'
+    'mne>1.10', 'numpy', 'scipy', 'pandas', 'munch', 'nibabel', 'joblib',
+    'torch', 'keras>3.0', 'scikit-learn', 'huggingface_hub>=0.24.0',
+    'matplotlib', 'pillow'
 ]
 version = "0.3.5"
 
@@ -38,4 +40,8 @@ testing = ['datalad','pytest','pygit2']
 
 [tool.setuptools.packages.find]
 include = ["MEG*"]
-exclude = ["extras*", "conda*"]
+exclude = [
+    "MEGnet.tests*",
+    "MEGnet.prep_inputs.tests*",
+    "MEGnet.prep_inputs.extras*",
+]
