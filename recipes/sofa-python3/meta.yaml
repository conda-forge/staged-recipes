{% set name = "sofa-python3" %}
{% set version = "23.06.00" %}
{% set major_minor = ".".join(version.split(".")[:2]) %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  - url: https://github.com/sofa-framework/SofaPython3/archive/refs/tags/release-v{{ major_minor }}.tar.gz
    sha256: 27289a0bcb7b96fcf57aaab5dc38489227d256b27628ac4e968782b15001d4fa

build:
  number: 0
  # v23.06 not released on MacOS yet due to broken CI
  skip: true  # [py<38] or [osx]
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
    - make                                   # [not win]
    - ninja                                  # [win]
    - {{ cdt('mesa-libgl-devel') }}          # [linux]
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]

  host:
    - sofa
    - eigen
    - pybind11
    - python
    - boost-cpp
    - qt-main
    - glew
    - numpy

  run:
    - python
    - numpy

test:
  commands:
    - test -f $PREFIX/lib/libSofaPython3${SHLIB_EXT}  # [not win]

about:
  home: https://github.com/sofa-framework/SofaPython3
  summary: A python plugin for Sofa offering a pythonic interface and python3 support.
  license: LGPL-2.1-or-later
  license_family: LGPL
  license_file: License.md
  doc_url: https://sofapython3.readthedocs.io/
  dev_url: https://github.com/sofa-framework/SofaPython3

extra:
  recipe-maintainers:
    - olivier-roussel
    - hugtalbot
