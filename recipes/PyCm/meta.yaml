{% set name = "pycm" %}
{% set version = "7.5.1" %}
{% set tag = "v7r5p1" %}



package:
   name: "{{ name|lower }}"
   version: "{{ version }}"

source:
   url: https://git.ligo.org/virgo/virgoapp/PyCm/-/archive/{{ tag }}/PyCm-{{ tag }}.tar.gz
   sha256: 5fb86f1d09db7b24b945adb29a7acc26cc36ce69bf82da2fab80e949c6dbd24c

build:
  number: 0
  skip: true  # [win or osx]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake >=3.12.0
    - make  # [unix]
    - pkg-config  # [unix]
    - python
    - setuptools
  host:  
    - cm >=9.0.0
    - python
    - setuptools
  run: 
    - cm >=9.0.0
    - python

test:
  requires:
    - pkg-config
  commands:
    - test -r ${PREFIX}/lib/libpycm.so  # [unix]
    - test "$(pkg-config --print-errors --modversion pycm)" == "${PKG_VERSION}"  # [unix]
  imports:
    - PyCm

about:
  home: "https://git.ligo.org/virgo/virgoapp/PyCm"
  license: "GPL-3.0-or-later"
  license_family: "GPL"
  license_file: "LICENSE"
  summary: "Python binding for the Cm Library"

extra:
  recipe-maintainers:
      - rhyspoulton
      - fcarbogn 
