{% set name = "sphinxcontrib-redoc" %}
{% set version = "1.5.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: "628ef9d504dd29d3c5620e4bed70f01e73c1f456773f8d69efc208e1e1c1465f"

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  host:
    - setuptools_scm
    - pip
    - python

  run:
    - jinja2 >=2.4
    - python
    - six >=1.5
    - sphinx >=1.5

test:
  imports:
    - sphinxcontrib

about:
  home: "https://github.com/ikalnytskyi/sphinxcontrib-redoc"
  license: "BSD-2-Clause"
  license_family: "BSD"
  license_file: "LICENSE"
  summary: "ReDoc powered OpenAPI (fka Swagger) spec renderer for Sphinx"
  doc_url: "https://sphinxcontrib-redoc.readthedocs.io"
  dev_url: "https://github.com/ikalnytskyi/sphinxcontrib-redoc"

extra:
  recipe-maintainers:
    - agcopenhaver
