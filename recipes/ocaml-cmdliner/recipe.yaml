context:
  version: "2.1.1"

package:
  name: ocaml-cmdliner
  version: ${{ version }}

source:
  url: https://erratique.ch/software/cmdliner/releases/cmdliner-${{ version }}.tbz
  sha256: 05b938d1def4f54c478178f1982822834510431ed98f2ac67cb4d90aa120d6b6

build:
  number: 0
  skip:
    - win
  script:
    # Temporary workaround for conda-forge/ocaml-feedstock#101.
    - if: osx
      then: export CONDA_OCAML_CC="${CC}"
    - if: osx
      then: export CONDA_OCAML_MKEXE="${CC} -mmacosx-version-min=${MACOSX_DEPLOYMENT_TARGET} -Wl,-headerpad_max_install_names -Wl,-rpath,@executable_path/../lib"
    - if: osx
      then: export CONDA_OCAML_MKDLL="${CC} -mmacosx-version-min=${MACOSX_DEPLOYMENT_TARGET} -shared -Wl,-headerpad_max_install_names -undefined dynamic_lookup"
    # cmdliner bootstraps itself with `ocaml build.ml`; it has no build-system dependency.
    - make all PREFIX="${PREFIX}"
    - make install
        BINDIR="${PREFIX}/bin"
        LIBDIR="${PREFIX}/lib/ocaml/cmdliner"
        SHAREDIR="${PREFIX}/share"
        MANDIR="${PREFIX}/share/man"

requirements:
  build:
    - ${{ compiler("c") }}
    - ${{ stdlib("c") }}
    - ${{ compiler("ocaml") }}
    - make
  host:
    - ocaml >=4.08.0

tests:
  - package_contents:
      strict: true
      bin:
        - cmdliner
      files:
        - lib/ocaml/cmdliner/*
        - share/bash-completion/completions/*
        - share/zsh/site-functions/*
        - share/powershell/*

about:
  homepage: https://erratique.ch/software/cmdliner
  repository: https://github.com/dbuenzli/cmdliner
  documentation: https://erratique.ch/software/cmdliner/doc
  summary: Declarative definition of command line interfaces for OCaml
  license: ISC
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - ManuelLerchner
    - MementoRC
