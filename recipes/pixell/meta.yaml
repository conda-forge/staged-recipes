{% set name = "pixell" %}
{% set version = "0.17.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pixell-{{ version }}.tar.gz
  sha256: 725a1593850abe04de17a64386d5b9250096b37cf5e8bb8a51121009ff71fb43

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('fortran') }}
    - setuptools >=39
    - make
    - automake # libsharp2
    - cython >=0.28
    - llvm-openmp  # [osx]
    - libgomp      # [linux]
  host:
    - numpy
    - pip
    - python
  run:
    - python
    - astropy >=2.0
    - h5py >=2.7
    - scipy >=1.0
    - python-dateutil >=2.7
    - healpy >=1.13
    - matplotlib-base >=2.0
    - pyyaml >=5.0
    - pillow >=5.3.0
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - pixell
  commands:
    - pip check
  requires:
    - pip
    - pytest-cov >=2.6
    - coveralls >=1.5
    - pytest >=4.6

about:
  home: https://github.com/simonsobs/pixell
  summary: pixell
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ickc
