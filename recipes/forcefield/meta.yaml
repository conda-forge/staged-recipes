{% set name = "forcefield" %}
{% set version = "0.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: f96574e76883f9d72649441957b05eb57d28b43d9c7d79a2ad28f232aa418357

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  entry_points:
    - forcefield = forcefield.cli:main

requirements:
  host:
    - python >=3.9
    - pip
    - setuptools >=68.0
    - wheel
  run:
    - python >=3.9

test:
  imports:
    - forcefield
  commands:
    - forcefield --help
    - python -c "import forcefield; g = forcefield.Guard(); r = g.selftest(); assert r.detected > 0"

about:
  home: https://datasciencetech.ca/en/python-sdk
  summary: Zero-dependency AI security library -- prompt-injection detection, PII redaction, content safety, rate limiting, abuse detection, and tool governance for LLMs.
  description: |
    ForceField is a lightweight AI security scanner for Python. Detect prompt
    injection, PII leaks, LLM attacks, abuse, and more in 3 lines of code.
    Zero runtime dependencies for the base install. Optional ML extras
    available via pip install forcefield[ml].
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  doc_url: https://datasciencetech.ca/en/python-sdk
  dev_url: https://github.com/Data-ScienceTech/forcefield

extra:
  recipe-maintainers:
    - carlosdenner
