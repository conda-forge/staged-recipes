{% set name = "tfds-nightly" %}
{% set version = "4.3.0.dev202107280110" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/tfds-nightly-{{ version }}.tar.gz
  sha256: 04cea137c5ab4466bd0bb23ececc20b8022494dcabbb2934af4c55440e597b67

build:
  number: 0
  skip: true   # [py2k]
  entry_points:
    - tfds = tensorflow_datasets.scripts.cli.main:launch_cli
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python

  run:
    - absl-py
    - dataclasses  # [py<37]
    - dill
    - future
    - importlib_resources  # [py<39]
    - numpy
    - promise
    - protobuf >=3.12.2
    - python
    - requests >=2.19.0
    - six
    - tensorflow
    - tensorflow-metadata
    - termcolor
    - tqdm
    - typing-extensions  # [py<38]

test:
  imports:
    - tensorflow_datasets
    - tensorflow_datasets.audio
  commands:
    - pip check
    - tfds --help
  requires:
    - pip

about:
  home: https://github.com/tensorflow/datasets
  summary: tensorflow/datasets is a library of datasets ready to use with TensorFlow.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - admax18
    - mxr-conda
