{% set name = "RPi.GPIO" %}
{% set version = "0.7.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/RPi.GPIO-{{ version }}.tar.gz
  sha256: 7424bc6c205466764f30f666c18187a0824077daf20b295c42f08aea2cb87d3f

build:
  skip: True  # [not linux64 or not aarch64]
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip  # [linux64 or aarch64]
    - python  # [linux64 or aarch64]
    - gcc_linux-aarch64  # [linux64 or aarch64]
    - gcc_linux-64  # [linux64 or aarch64]
  run:
    - python  # [linux64 or aarch64]

test:
  imports:
    - RPi
    - RPi.GPIO
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://sourceforge.net/projects/raspberry-gpio-python/
  summary: A module to control Raspberry Pi GPIO channels
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - marcomoerz
