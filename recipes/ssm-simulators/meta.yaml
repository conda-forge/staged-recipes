{% set name = "ssm-simulators" %}
{% set version = "0.7.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ssm-simulators-{{ version }}.tar.gz
  sha256: 0d08b4a8747fc39d48db863211eccb3608d2731e961de5afbcaee16c7f242ef2

build:
  skip: true  # [py>=312 or py2k]
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
  host:
    - python
    - setuptools
    - wheel
    - cython >=0.29.23
    - numpy
    - pip
  run:
    - python
    - scipy >=1.6.3
    - pandas >=1.0.0
    - scikit-learn >=0.24.0
    - psutil >=5.0.0
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - ssms
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/ssm-simulators
  summary: SSMS is a package collecting simulators and training data generators for a bunch of generative models of interest in the cognitive science / neuroscience and approximate bayesian computation communities
  dev_url: https://github.com/AlexanderFengler/ssm-simulators
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - AlexanderFengler
    - digicosmos86
