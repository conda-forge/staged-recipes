{% set name = "flowsom" %}
{% set version = "0.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/saeyslab/FlowSOM_Python/archive/v{{ version }}.tar.gz
  sha256: 1f1b61a0d85fb285549cb0221935747153bd494185d1233a9be6c39e039d8f71

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - hatchling
    - pip
  run:
    - python >=3.9
    - mudata
    - numpy
    - matplotlib-base
    - pandas
    - scipy
    - readfcs
    - scikit-learn
    - python-igraph
    - pytest
    - session-info
    - numba
    - scanpy
    - seaborn
    - anndata
    - loguru

test:
  imports:
    - flowsom
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/saeyslab/FlowSOM_Python
  summary: The complete FlowSOM package known from R, now available in Python!
  license: GPL-3.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - berombau
