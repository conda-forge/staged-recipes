{% set name = "dgl" %}
{% set version = "0.7.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  - git_url: https://github.com/dmlc/dgl.git
    sha256: 66b980a5093903642b8fe5eea23ad50b77d85644df348242c54c8de976b15b50

build:
  noarch: python
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - python >=3.6
    - setuptools
    - cmake
    - git
    - cython

  run:
    - python >=3.6
    - numpy >=1.14.0
    - scipy >=1.1.0
    - networkx >=2.1
    - requests >=2.19.0
    - pytorch
    - tensorflow
    - tqdm

test:
  imports:
    - dgl
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/dmlc/dgl
  license: Apache-2.0
  license_file: LICENSE
  summary: A deep graph library
  description: |
    DGL is an easy-to-use, high performance and scalable Python package for deep
     learning on graphs. DGL is framework agnostic, meaning if a deep graph
     model is a component of an end-to-end application, the rest of the logics
     can be implemented in any major frameworks, such as PyTorch, Apache MXNet
     or TensorFlow.
  doc_url: https://docs.dgl.ai/en/latest/

extra:
  recipe-maintainers:
    - knc6
