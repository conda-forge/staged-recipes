{% set name = "gwtc_analysis" %}
{% set version = "0.2.8" %}
{% set python_min = "3.10" %}
package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/g/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: c28ff30565eff279386ab73dee04911cc1d2e993e37c913ac205e06fe6b48ede 

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
    - wheel
  run:
    - python {{ python_min }}
    - numpy
    - pandas
    - matplotlib-base
    - requests
    - astropy-base
    - gwpy
    - gwosc
    - ligo.skymap
    - pesummary
    - minio
    - pyyaml

test:
  requires:
    - python {{ python_min }}
  imports:
    - gwtc_analysis
  commands:
    - gwtc_analysis --help
    - python -m pip check

about:
  summary: "GWTC analysis tools for catalogs, skymaps, and parameter estimation"
  home: https://github.com/danielsentenac/gwtc_analysis
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - danielsentenac
