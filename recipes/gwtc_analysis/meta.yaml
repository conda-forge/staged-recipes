{% set name = "gwtc_analysis" %}
{% set version = "0.3.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/g/gwtc_analysis/gwtc_analysis-{{ version }}.tar.gz
  sha256: 66886234e5e5aafdbbee94f972de720eba84d6a517ce32804bdca7781825e0d4

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python 3.11
    - pip
    - setuptools
  run:
    - python >=3.10,<3.13
    - numpy
    - scipy
    - pandas
    - matplotlib
    - astropy
    - h5py
    - healpy
    - reproject
    - requests
    - tqdm
    - pyyaml
    - gwpy
    - gwosc
    - ligo.skymap
    - pesummary
    - pycbc
    - pyseobnr
    - python-lalsimulation
    - minio

test:
  imports:
    - gwtc_analysis
  commands:
    - python -m pip check
    - python -m gwtc_analysis.cli -h
    - python -m gwtc_analysis.cli search_skymaps -h
    - python -m gwtc_analysis.cli event_selection -h
    - python -m gwtc_analysis.cli catalog_statistics -h
    - python -m gwtc_analysis.cli parameters_estimation -h
    - python -m gwtc_analysis.cli build_unofficial_pe -h
  requires:
    - pip

about:
  home: https://github.com/danielsentenac/gwtc_analysis
  summary: "GWTC Analysis: search skymaps, event selection, catalog statistics, and parameter estimation plotting"
  dev_url: https://github.com/danielsentenac/gwtc_analysis
  doc_url: https://pypi.org/project/gwtc-analysis/
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - danielsentenac
