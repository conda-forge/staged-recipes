{% set name = "dpm360-lightsaber" %}
{% set version = "0.2.3" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/dpm360-lightsaber-{{ version }}.tar.gz
  sha256: acb9c3e87d79770accff381f0c66007c8bed9897dd30c3cbf40804f93e75346b

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - mlflow >=1.2.0
    - pytorch-lightning ==0.8.5
    - scikit-learn ==0.23.*
    - tabulate
    - toolz
    - python >=3.6

test:
  imports:
    - lightsaber
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/IBM/DPM360
  doc_url: https://ibm.github.io/DPM360/Lightsaber/  
  summary: Package to handle model training for dpm tasks
  license: Apache-2.0
  license_file: LICENSE.txt


extra:
  recipe-maintainers:
    - rachita-c
