{% set name = "htgettoken" %}
{% set version = "1.20" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/fermitools/htgettoken/archive/v{{ version }}.tar.gz
  sha256: 11d53046276986403c9e9d3eacc10fb4baa3d385d453111ecf3054438957dc36

build:
  # not noarch since uses scripts
  skip: True  # [win]
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python
    - pip
  run:
    - python
    - python-gssapi
    - paramiko
    - urllib3

test:
  commands:
    - htgettoken --help
    - htdecodetoken --help
    - httokendecode --help
    - htdestroytoken --help
    - httokensh --help

about:
  home: https://github.com/fermitools/htgettoken
  summary: 'Gets OIDC authentication tokens for High Throughput Computing via a Hashicorp vault server'
  license: BSD-3-Clause
  license_family: BSD
  license_file: COPYING

extra:
  recipe-maintainers:
    - beckermr
