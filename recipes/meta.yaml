package:
  name: paprika
  version: "0.1.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - paprika

about:
  home: add_url_here
  license: BSD-3-Clause License
