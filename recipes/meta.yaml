package:
  name: aiostream
  version: "0.2.6"

source:
  fn: aiostream-0.2.6.tar.gz
  url: https://pypi.python.org/packages/3e/76/da1f15b1c8f0222059957b5049487ed5b11184e5fe56cb5219a07d717747/aiostream-0.2.6.tar.gz
  md5: 6ef1f777a016fbdf4e5ba81c41de59b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch: python
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiostream = aiostream:main
    #
    # Would create an entry point called aiostream that calls aiostream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aiostream
    - aiostream.stream

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - pytest
    - pytest-asyncio
    - pytest-cov
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vxgmichel/aiostream
  license: GPLv3
  summary: 'Generator-based operators for asynchronous iteration'
  license_family: GPL3
