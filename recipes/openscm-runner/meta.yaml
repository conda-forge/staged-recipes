{% set name = "openscm-runner" %}
{% set version = "0.7.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 8a4677cd32d09821a8f647e69700742dfc0f86738f7759001948a6a3e7afdcc0

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vvv"

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - click
    - fair
    - pyam
    - pymagicc
    - python-dotenv
    - scmdata >=0.7.4
    - tqdm

test:
  imports:
    - openscm_runner
  requires:
    - pip
  commands:
    - pip check
    - python -c "import openscm_runner; assert openscm_runner.__version__ == '{{ version }}'"

about:
  home: https://github.com/openscm/openscm-runner
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Thin wrapper to run simple climate models (emissions driven runs only)
  doc_url: https://openscm-runner.readthedocs.io/en/stable/
  dev_url: https://github.com/openscm/openscm-runner

extra:
  recipe-maintainers:
    - znicholls
