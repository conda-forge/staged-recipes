{% set name = "pypardiso" %}
{% set version = "0.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  # uncomment the line below, and modify as needed. Use releases if available:
  # url: https://github.com/simplejson/simplejson/releases/download/{{ version }}/simplejson-{{ version }}.tar.gz
  # and otherwise fall back to archive:
  url: https://github.com/ma-sadeghi/PyPardisoProject/archive/v{{ version }}.tar.gz
  sha256: b1d3a139f8f04c74f8b2b5c4b55434e1f58b00ae3fe0ca975115971906d93eac
  # sha256 is the preferred checksum -- you can get it for a file with:
  #  `openssl sha256 <file name>`.

build:
  noarch: python
  number: 0
  # If the installation is complex, or different between Unix and Windows, use separate bld.bat and build.sh files instead of this key.
  # By default, the package will be built for the Python versions supported by conda-forge and for all major OSs.
  # Add the line "skip: True  # [py<35]" (for example) to limit to Python 3.5 and newer, or "skip: True  # [not win]" to limit to Windows.
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - python
    - setuptools
  host:
    - python
    - pip
  run:
    - python
    - mkl
    - numpy
    - scipy
    - mkl-service

test:
    imports:
    - pypardiso

about:
  home: https://github.com/ma-sadeghi/PyPardisoProject
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: 'Python interface to the Intel MKL Pardiso library to solve large sparse linear systems of equations'

  # The remaining entries in this section are optional, but recommended.
  # description: |
  #   simplejson is a simple, fast, complete, correct and extensible
  #   JSON <https://json.org> encoder and decoder for Python 2.5+ and
  #   Python 3.3+. It is pure Python code with no dependencies, but includes
  #   an optional C extension for a serious speed boost.
  # doc_url: https://pypardiso.readthedocs.io/
  dev_url: https://github.com/ma-sadeghi/PyPardisoProject

extra:
  recipe-maintainers:
    - ma-sadeghi
