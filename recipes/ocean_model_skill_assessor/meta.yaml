{% set name = "ocean_model_skill_assessor" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/axiom-data-science/extract_model/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 0019dfc4b32d63c1392aa264aed2253c1e0c2fb09216f8e2cc269bbfb8bb49b5

build:
  noarch: python
  number: 0
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools-scm
  run:
    - cartopy
    - extract_model
    - jupyter
    - jupyterlab
    - matplotlib
    - netcdf4
    - numpy
    - ocean_data_gateway
    - pandas
    - python >=3.6
    - pytest
    - setuptools
    - scipy
    - xarray

test:
  imports:
    - ocean_model_skill_assessor
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/axiom-data-science/ocean_model_skill_assessor
  license: MIT
  license_file: LICENSE.txt
  summary: 'A package to fully run the comparison between data and model to assess model skill.'
  doc_url: https://ocean-model-skill-assessor.readthedocs.io/
  dev_url: https://github.com/axiom-data-science/ocean_model_skill_assessor

extra:
  recipe-maintainers:
    - kthyng
