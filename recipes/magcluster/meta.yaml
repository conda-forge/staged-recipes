{% set name = "magcluster" %}
{% set version = "0.1.2" %}

package:
  name: "{{ name }}"
  version: "{{ version }}"

build:
  noarch: python

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - prokka >=1.14.6
    - clinker-py
    - pandas
    - python >=3.6
    - openpyxl

about:
  home: "https://github.com/RunJiaJi/magcluster"
  license: GNU GPLv3
  summary: "A Python package for magnetosome gene cluster screening and mapping"
  dev_url: https://github.com/RunJiaJi/magcluster

extra:
  recipe-maintainers:
    - runjiaji