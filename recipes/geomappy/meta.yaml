{% set name = "geomappy" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/geomappy-{{ version }}.tar.gz
  sha256: 788fa992ab5cf4e69ff53119657781e12aaea3500b9e53c3efbee1f4bd0c94ac

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=42
    - wheel
    - pip
  run:
    - python >=3.10
    - cartopy >=0.22
    - rioxarray
    - geopandas
    - matplotlib-base >=3.10

test:
  imports:
    - geomappy
  commands:
    - pip check
  requires:
    - pip

about:
  summary: Plotting geospatial vector and raster data on a basemap using matplotlib and cartopy
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jasperroebroek
