schema_version: 1

context:
  version: 1.5.0

package:
  name: sagemaker-mlops
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/sagemaker-mlops/sagemaker_mlops-${{ version }}.tar.gz
  sha256: acc09157aca78d3621e8381dbf40b161924620d3e820832ed709e3c2cb83ddbc

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=64
    - wheel
    - pip
  run:
    - python >=${{ python_min }}
    - sagemaker-core >=2.5.0
    - sagemaker-train >=1.5.0
    - sagemaker-serve >=1.5.0
    - boto3 >=1.42.2,<2.0
    - botocore >=1.42.2,<2.0

tests:
  - python:
      imports:
        - sagemaker.mlops
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: SageMaker MLOps package for workflow orchestration and model building
  homepage: https://github.com/aws/sagemaker-python-sdk
  license: Apache-2.0
  license_file: LICENSE
  documentation: https://sagemaker.readthedocs.io/en/stable/
  repository: https://github.com/aws/sagemaker-python-sdk

extra:
  recipe-maintainers:
    - mollyheamazon
    - mufaddal-rohawala
    - nargokul
