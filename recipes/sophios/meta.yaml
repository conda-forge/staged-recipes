{% set name = "sophios" %}
{% set version = "0.1.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/PolusAI/workflow-inference-compiler/archive/refs/tags/v{{ version }}.tar.gz
  sha256: c3718d9103aea4c60964bd9aa4009da46a4164aacc526cdf8b53d3f9a4361d9d
build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - setuptools >=42
    - pip 
    - wheel 
    - versioneer[toml] ==0.29
    - tomli
  run:
    - python >=3.6
    - graphviz
    - jsonschema
    - pyyaml
    - requests
    - mergedeep
    - networkx
    - cwl-utils >=0.32
    - typeguard
    - pydantic >=2.6
    - pydantic-settings
    - pydantic [email]
    - docker
    - podman
    - toil[cwl]
    - fastapi
    - python-jose
    - uvicorn
    - orjson

test:
  imports:
    - sophios
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/sophios/
  summary: DSL for inferring the edges of a CWL workflow DAG
  dev_url: https://github.com/PolusAI/workflow-inference-compiler
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - sameeul
