{% set name = "dropkick" %}
{% set version = "1.2.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/dropkick-{{ version }}.tar.gz
  sha256: f1a64e7c6bbf1a9f7dcb8a1198c91dd4f495a1995f1e9c57cd8fcd2cdf25b130

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - numpy
    - pip
  run:
    - python >=3.6
    - numpy >=1.9.2
    - scipy >=0.14.1
    - pandas
    - scikit-learn >=0.18.0
    - scikit-image
    - matplotlib-base >=3.0.3
    - scanpy

test:
  imports:
    - dropkick
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/KenLauLab/dropkick
  summary: Automated scRNA-seq filtering
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mfansler
