{% set name = "cupy" %}
{% set version = "6.0.0rc1" %}
{% set sha256 = "bc5b99fa5efe8465648a133262e32e501104f46b9d84f0c9649254e2c4417c5d" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [not linux64]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('nvcc') }}

  host:
    - cudnn
    - nccl
    - python
    - numpy
    - setuptools
    - cython >=0.24.0
    - fastrlock >=0.3

  run:
    - {{ pin_compatible('cudnn') }}
    - python
    - setuptools
    - {{ pin_compatible('numpy') }}
    - six >=1.9
    - fastrlock >=0.3

test:
  imports:
    - cupy
  # TODO: run full test suite
  #requires:
  #  - pytest
  #  - mock
  #source_files:
  #  - tests
  #commands:
  #  - CUPY_TEST_GPU_LIMIT=1 python -m pytest tests/cupy_tests

about:
  home: https://cupy.chainer.org/
  license: MIT
  license_family: MIT
  summary: CuPy is an implementation of a NumPy-compatible multi-dimensional array on CUDA.

extra:
  recipe_maintainers:
    - jakirkham
