diff --git a/cmake/ga-linalg.cmake b/cmake/ga-linalg.cmake
index ee07b1e..61f4a22 100644
--- a/cmake/ga-linalg.cmake
+++ b/cmake/ga-linalg.cmake
@@ -187,6 +187,7 @@ if (ENABLE_BLAS)
     endif()
     include(FetchContent)
     set( gpu_backend "none" CACHE STRING "GPU backend to use" FORCE)
+    find_package(blaspp)
     if(NOT TARGET blaspp)
       if(ENABLE_OFFLINE_BUILD)
       FetchContent_Declare(
@@ -204,6 +205,7 @@ if (ENABLE_BLAS)
       FetchContent_MakeAvailable( blaspp )
     endif()
 
+    find_package(lapackpp)
     if(NOT TARGET lapackpp)
     if(ENABLE_OFFLINE_BUILD)
       FetchContent_Declare(
