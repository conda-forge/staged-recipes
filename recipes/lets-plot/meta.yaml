{% set name = "lets-plot" %}
{% set version = "4.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/JetBrains/lets-plot/archive/v{{ version }}.tar.gz
  sha256: fb433b110fb229a1d042553d8ec14d528dbab6eef1bdcf8783c44fb071c1bd5a
  patches:
   - patches/0001-Remove-static-link-of-zlib-for-Linux.patch

build:
  skip: true  # [win]
  number: 0
  script_env:
   - PROJECT_VERSION=v{{ version }}

requirements:
  build:
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}  # [linux]
    - openjdk =11
  host:
    - zlib 
    - python
    - pip
    - setuptools
    - unzip  # [linux]
  run:
    - python
    - pypng
    - palettable

test:
  imports:
    - lets_plot
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://lets-plot.org
  summary: 'Lets-Plot is a multiplatform plotting library built on the principles of the Grammar of Graphics.'
  license: MIT
  license_file:
    - LICENSE
    - plot-base/src/commonMain/kotlin/org/jetbrains/letsPlot/core/plot/base/stat/math3/NOTICE
  dev_url: https://github.com/JetBrains/lets-plot
  doc_url: https://lets-plot.org/python/index.html#python-index-user-guide

extra:
  recipe-maintainers:
    - VDovidaytis-HORIS
