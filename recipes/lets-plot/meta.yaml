{% set name = "lets-plot" %}
{% set version = "4.6.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/JetBrains/lets-plot/archive/v{{ version }}.tar.gz
  sha256: f22fe7ae852b18ddb0088b61b1ccee9ce02b526778d5d942e2cca671e4652245

build:
  skip: true  # [win]
  number: 0
  script_env:
   - PROJECT_VERSION=v{{ version }}

requirements:
  build:
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}  # [linux]
    - openjdk =11
  host:
    - zlib 
    - python
    - pip
    - setuptools
    - unzip  # [linux]
  run:
    - python
    - pypng
    - palettable

test:
  imports:
    - lets_plot
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://lets-plot.org
  summary: 'Lets-Plot is a multiplatform plotting library built on the principles of the Grammar of Graphics.'
  license: MIT
  license_file:
    - LICENSE
    - license-files/
    - plot-base/src/commonMain/kotlin/org/jetbrains/letsPlot/core/plot/base/stat/math3/NOTICE
  dev_url: https://github.com/JetBrains/lets-plot
  doc_url: https://lets-plot.org/python/index.html#python-index-user-guide

extra:
  recipe-maintainers:
    - VDovidaytis-HORIS
