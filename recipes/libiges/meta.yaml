package:
  name: libiges
  version:  1.0

source:  
  git_url: https://github.com/cbernardo/libIGES.git
build:
requirements:
  build:
    - {{ compiler('cxx') }}
    - ninja
    - cmake  
  host:

  run:

about:
  license: LGPL2.1