@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/LuaTargets.cmake")

set(LUA_FOUND TRUE)
set(Lua_FOUND TRUE)

get_target_property(_lua_inc Lua::lua INTERFACE_INCLUDE_DIRECTORIES)
if(_lua_inc)
    set(LUA_INCLUDE_DIR "${_lua_inc}")
endif()
unset(_lua_inc)
