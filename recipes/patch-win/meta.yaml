
package:
  name: patch-win
  version: 0.1

source:
  path: ./src
  patches:
    - test.patch

build:
  skip: true  # [not py27]
  number: 0

requirements:
  build:
    - toolchain
  run:

test:
  commands:
    - true

about:
  home: http://example.com
  license: MIT
  summary: 'Testing a simple patch'

extra:
  recipe-maintainers:
    - dolfim