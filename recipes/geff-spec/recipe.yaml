context:
  name: geff-spec
  version: "1.1.0.post0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/geff_spec-${{ version }}.tar.gz
  sha256: 1b265c7849d4937307f83e12cbebabdd20af02967a065603ce7b8c6dc11e0290

build:
  noarch: python
  script: python -m pip install . -vv
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - pydantic >=2.11
    - numpy
    - annotated_types

tests:
  - python:
      imports:
      - geff_spec
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://liveimagetrackingtools.org/geff/
  summary: 'Specification of the Graph Exchange File Format'
  license: MIT
  license_file: LICENSE.txt
  documentation: https://liveimagetrackingtools.org/geff/
  repository: https://github.com/live-image-tracking-tools/geff

extra:
  recipe-maintainers:
    - tlambert03
    - cmalinmayor
    - msschwartz21
