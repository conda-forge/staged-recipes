package:
  name: pytest-html
  version: "1.14.2"

source:
  fn: pytest-html-1.14.2.tar.gz
  url: https://pypi.python.org/packages/94/a6/b9a67092c59d4c9e1f55b47c873cbb774ed91f6fe44f7a7ed38fb982b417/pytest-html-1.14.2.tar.gz
  md5: 5f0e1d42afb170b41ba7af3f878a9dbb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pytest-html = pytest-html:main
    #
    # Would create an entry point called pytest-html that calls pytest-html.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools-scm
    - pytest >=2.3
    - pytest-metadata

  run:
    - python
    - setuptools
    - pytest >=2.3
    - pytest-metadata

test:
  # Python imports
  imports:
    - pytest_html

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pytest-dev/pytest-html
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'pytest plugin for generating HTML reports'
  license_family: OTHER

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
