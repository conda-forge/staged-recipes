# lgpio includes <linux/gpio.h> (GPIO character-device uapi, added in kernel 4.8);
# the default sysroot (glibc 2.17 / CentOS 7) ships 3.10 kernel headers, so a
# newer stdlib/sysroot with 4.18 kernel headers is required.
c_stdlib_version:  # [linux]
  - "2.28"         # [linux]
