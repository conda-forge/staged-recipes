diff --git a/src/av/CMakeLists.txt b/src/av/CMakeLists.txt
index 5c1e4860..4c15c54f 100644
--- a/src/av/CMakeLists.txt
+++ b/src/av/CMakeLists.txt
@@ -20,11 +20,13 @@ if(TARGET av)
   elseif(APPLE)
     find_library(COREAUDIO_LIBRARY CoreAudio)
     find_library(COREFOUNDATION_LIBRARY CoreFoundation)
+    find_library(FOUNDATION_LIBRARY Foundation)
     find_library(AVFOUNDATION_LIBRARY AVFoundation)
     find_library(COREMEDIA_LIBRARY CoreMedia)
     target_link_libraries(av PUBLIC
       ${COREAUDIO_LIBRARY}
       ${COREFOUNDATION_LIBRARY}
+      ${FOUNDATION_LIBRARY}
       ${AVFOUNDATION_LIBRARY}
       ${COREMEDIA_LIBRARY})
   endif()
diff --git a/src/av/include/icy/av/devicemanager.h b/src/av/include/icy/av/devicemanager.h
index e15f6ece..cc9f4eb3 100644
--- a/src/av/include/icy/av/devicemanager.h
+++ b/src/av/include/icy/av/devicemanager.h
@@ -25,17 +25,7 @@
 #include "icy/signal.h"
 
 #ifdef HAVE_FFMPEG
-
-#include "icy/av/ffmpeg.h"
-
-extern "C" {
-#include <libavcodec/avcodec.h>
-#include <libavformat/avformat.h>
-#ifdef HAVE_FFMPEG_AVDEVICE
-#include <libavdevice/avdevice.h>
-#endif
-}
-
+struct AVInputFormat;
 #endif
 
 
diff --git a/src/av/src/apple/avfoundation_mm b/src/av/src/apple/avfoundation_mm
index c686596d..45ea3ae5 100644
--- a/src/av/src/apple/avfoundation_mm
+++ b/src/av/src/apple/avfoundation_mm
@@ -3,8 +3,8 @@
 #define __has_feature(x) 0
 #endif
 
-#include "icy/logger.h"
 #include "icy/av/devicemanager.h"
+#include "icy/logger.h"
 
 #import <assert.h>
 #import <AVFoundation/AVFoundation.h>
@@ -49,14 +49,17 @@ bool GetAVFoundationVideoDevices(Device::Type type, std::vector<Device>* devices
   {
     // Use AVCaptureDeviceDiscoverySession (replaces deprecated [AVCaptureDevice devices])
     NSArray<AVCaptureDeviceType>* deviceTypes;
-    AVMediaType mediaType;
+    NSString* mediaType = nil;
 
     // AVCaptureDeviceTypeExternal (macOS 14+) replaces
     // AVCaptureDeviceTypeExternalUnknown (macOS 10.15-13.x)
-    AVCaptureDeviceType externalType;
+    AVCaptureDeviceType externalType = nil;
+#if defined(__MAC_OS_X_VERSION_MAX_ALLOWED) && __MAC_OS_X_VERSION_MAX_ALLOWED >= 140000
     if (@available(macOS 14.0, *)) {
         externalType = AVCaptureDeviceTypeExternal;
-    } else {
+    } else
+#endif
+    {
 #pragma clang diagnostic push
 #pragma clang diagnostic ignored "-Wdeprecated-declarations"
         externalType = AVCaptureDeviceTypeExternalUnknown;
@@ -86,7 +89,7 @@ bool GetAVFoundationVideoDevices(Device::Type type, std::vector<Device>* devices
             position:AVCaptureDevicePositionUnspecified];
 
     NSArray<AVCaptureDevice*>* capture_devices = discoverySession.devices;
-    LDebug([capture_devices count], " capture device(s) found:")
+    LDebug([capture_devices count], " capture device(s) found:");
 
     for (AVCaptureDevice* capture_device in capture_devices) {
         static NSString* const kFormat = @"localizedName: \"%@\", "
@@ -99,7 +102,7 @@ bool GetAVFoundationVideoDevices(Device::Type type, std::vector<Device>* devices
                              [capture_device uniqueID],
                              [capture_device isConnected],
                              [capture_device isInUseByAnotherApplication]];
-        LDebug('\t', [info UTF8String])
+        LDebug('\t', [info UTF8String]);
 
         std::string uniqueID([[capture_device uniqueID] UTF8String]);
         std::string name([[capture_device localizedName] UTF8String]);
diff --git a/src/av/src/devicemanager.cpp b/src/av/src/devicemanager.cpp
index d3542bff..df65b554 100644
--- a/src/av/src/devicemanager.cpp
+++ b/src/av/src/devicemanager.cpp
@@ -12,6 +12,18 @@
 #include "icy/av/devicemanager.h"
 #include "icy/logger.h"
 
+#ifdef HAVE_FFMPEG
+#include "icy/av/ffmpeg.h"
+
+extern "C" {
+#include <libavcodec/avcodec.h>
+#include <libavformat/avformat.h>
+#ifdef HAVE_FFMPEG_AVDEVICE
+#include <libavdevice/avdevice.h>
+#endif
+}
+#endif
+
 
 #if defined(ICY_WIN)
 #include "icy/av/win32/mediafoundation.h"
