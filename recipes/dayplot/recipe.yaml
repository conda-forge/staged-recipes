context:
  name: dayplot
  version: "0.2.1"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: 4a7f7d0207ea0f4e9fe67c1dd158c7cea5cb70ee6a10332e4a2f8d213407951c

build:
  noarch: python
  script: python -m pip install . -vv
  number: 0

requirements:
  build:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - matplotlib-base

tests:
  - python:
      imports:
      - dayplot
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: https://github.com/JosephBARBIERDARNAL/dayplot
  summary: 'Build calendar heatmaps with ease'
  description: |
    A simple-to-use Python library to build calendar heatmaps with ease.
    It's built on top of matplotlib and leverages it to access high customization possibilities.
  license: MIT
  license_file: LICENSE
  documentation: https://josephbarbierdarnal.github.io/dayplot/

extra:
  recipe-maintainers:
    - claudiushaag
