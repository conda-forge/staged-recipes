# Deactivation script for the bash-completion package
#
# Currently no sensible way to fully remove the bash completion facillity from the users
# shell. The following will at least allow for a reload.
unset CONDA_BASH_COMPLETION_LOADED
