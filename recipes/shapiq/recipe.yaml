schema_version: 1

context:
  name: shapiq
  version: 1.3.1

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/shapiq-${{ version }}.tar.gz
  sha256: 95cb2eb1c3a757bf5c6dd38cf40d2149b3d4acd477a24308120ff2e844cbb51c

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=61.0
    - wheel
    - scikit-learn
    - numpy
    - xgboost
    - sparse-transform
    - pip
  run:
    - python >=${{ python_min }}
    - numpy
    - scipy
    - pandas
    - scikit-learn
    - tqdm
    - requests
    - sparse-transform
    - galois
    - matplotlib-base
    - networkx
    - colour
    - pillow
    - overrides

tests:
  - python:
      imports:
        - shapiq
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Shapley Interactions for Machine Learning
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/mmschlk/shapiq

extra:
  recipe-maintainers:
    - janjagusch
