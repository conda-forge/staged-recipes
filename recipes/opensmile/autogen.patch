--- autogen.sh	2016-10-29 06:16:37.000000000 +1100
+++ autogen.sh	2018-01-15 13:34:00.000000000 +1100
@@ -21,10 +21,11 @@
 automake -a
 
 # on Mac libtoolize is called glibtoolize
+# but not in conda
 LIBTOOLIZE=libtoolize
-if [ `uname -s` = Darwin ]; then
-    LIBTOOLIZE=glibtoolize
-fi
+#if [ `uname -s` = Darwin ]; then
+#    LIBTOOLIZE=glibtoolize
+#fi
 echo "- $LIBTOOLIZE"
 $LIBTOOLIZE
 
