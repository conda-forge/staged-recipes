# Note: there are many handy hints in comments in this example -- remove them when you've finalized your recipe

# Jinja variables help maintain the recipe as you'll update the version only here.
# Using the name variable with the URL in line 13 is convenient
# when copying and pasting from another recipe, but not really needed.
{% set name = "segregation" %}
{% set version = "1.0.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  git_url: https://github.com/pysal/segregation.git
  git_rev: 1.0.4
  sha256: 844b5c474656f4a137c115c3f25128418f3284b57addf622958e05b512c144de
 
build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vv"

requirements:
  host:
    - python >=3
    - pip
  run:
    - python>=3
    - pandas
    - geopandas
    - matplotlib
    - scikit-learn
    - seaborn
    - numpy
    - scipy
    - libpysal

test:
  imports:
    - segregation

about:
  home: http://github.com/spatialucr/segregation
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: 'Segregation Measures Framework in PySAL'

  description: |
    The PySAL segregation module allow users to estimate several segregation measures and perform 
    inference for single measures and comparative inference in a concise way.
  doc_url: http://segregation.readthedocs.io/
  dev_url: https://github.com/pysal/segregation

extra:
  recipe-maintainers:
    # GitHub IDs for maintainers of the recipe.
    - renanxcortes
    - sjsrey
    - knaaptime
