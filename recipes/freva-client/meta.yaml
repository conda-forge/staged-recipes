{% set name = "freva-client" %}
{% set version = "2506.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/freva_client-{{ version }}.tar.gz
  sha256: 546e2617035205ee48fe23ce9c22211b0617f9263b543bca46db020628629013

build:
  entry_points:
    - freva-client = freva_client:cli.app
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - flit-core >=3.2
    - appdirs
    - pip
  run:
    - python >=3.8
    - appdirs
    - pyyaml
    - requests
    - intake-esm
    - rich
    - setuptools
    - tomli
    - typer

test:
  imports:
    - freva_client
  commands:
    - pip check
    - freva-client --help
  requires:
    - pip

about:
  summary: Search for climate data based on key-value pairs
  dev_url: https://github.com/FREVA-CLINT/freva-nextgen/
  license: BSD-3-Clause
  license_file:
    - LICENSE
    - LICENSE

extra:
  recipe-maintainers:
    - antarcticrainforest
    - mo-dkrz
