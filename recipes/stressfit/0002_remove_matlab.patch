diff --git a/setup.py b/setup.py
index 82c6782..3550261 100644
--- a/setup.py
+++ b/setup.py
@@ -21,6 +21,7 @@ def get_version():
 
 stressfit_data = ['resources/*/*']
 
+tests_data = ['*']
 other_data = ['stressfit_example1.py', 
               'LICENSE', 
               'README.md', 
@@ -59,7 +60,12 @@ setup(
     keywords = 'residual stress, neutron, pseudo-strain, surface effect', 
     python_requires = '>=3.8',
     install_requires = requires,
-    packages = find_packages(),
-    package_data = {'stressfit': stressfit_data}, 
-    data_files = [('.',other_data)]
+    packages = find_packages(exclude=['matlab', 'matlab.*']),
+    package_data = {
+        'stressfit': stressfit_data,
+        'tests': tests_data
+    },
+    data_files = [('.',other_data)],
+    include_package_data = True,
+    test_suite = 'tests'
 )
