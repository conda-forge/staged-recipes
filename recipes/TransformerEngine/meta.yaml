{% set name = "TransformerEngine" %}
{% set version = "1.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/NVIDIA/TransformerEngine/archive/v{{ version }}.tar.gz
  sha256: 0e4617f6f6724ea6104584086f81d6fa371bd4921d065ef5faae1a9d4f0e2008

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  script_env:
    - NVTE_FRAMEWORK=pytorch
  skip: true           # [cuda_compiler_version in (undefined, "None")]
  skip: true           # [not linux]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }}
    - {{ stdlib('c') }}
  host:
    - pip
    - python
    - pytorch
  run:
    - flash-attn >=2.0.6,<=2.4.2,!=2.0.9,!=2.1.0
    - pydantic
    - python
    - pytorch

test:
  imports:
    - transformer_engine
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/NVIDIA/TransformerEngine
  summary: Transformer acceleration library
  doc_url: https://docs.nvidia.com/deeplearning/transformer-engine/user-guide/index.html
  dev_url: https://github.com/NVIDIA/TransformerEngine
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - weiji14
