{% set name = "TransformerEngine" %}
{% set version = "1.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/NVIDIA/TransformerEngine/archive/v{{ version }}.tar.gz
  sha256: 1afab78ba6c7eac2260d7cb682c77ac5d6b52e62b43bf4387b01b91e675c281b

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  script_env:
    - NVTE_FRAMEWORK=pytorch
  skip: true           # [cuda_compiler_version in (undefined, "None")]
  skip: true           # [not linux]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }}
    - {{ stdlib('c') }}
  host:
    - pip
    - python
    - pytorch
  run:
    - flash-attn >=2.0.6,<=2.4.2,!=2.0.9,!=2.1.0
    - pydantic
    - python
    - pytorch

test:
  imports:
    - transformer_engine
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/NVIDIA/TransformerEngine
  summary: Transformer acceleration library
  doc_url: https://docs.nvidia.com/deeplearning/transformer-engine/user-guide/index.html
  dev_url: https://github.com/NVIDIA/TransformerEngine
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - weiji14
