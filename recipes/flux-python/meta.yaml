{% set name = "flux-python" %}
{% set version = "0.59.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/flux-python-{{ version }}.tar.gz
  sha256: 4d1fc785b6345a55e58706f71fc62a21d176ba6fe219d949e62388e3057a3c01

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  skip: True  # [not linux] 

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
  host:
    - python
    - pip
    - setuptools
    - flux-core {{ version }}
  run:
    - python
    - cffi >=1.1
    - pyyaml

test:
  imports:
    - flux
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/flux-framework/flux-python
  summary: Python bindings for the flux resource manager API
  license: LGPL-3.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
