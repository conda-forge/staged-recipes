schema_version: 1

context:
  version: "0.1.0"

package:
  name: jupyverse-frontend
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/j/jupyverse-frontend/jupyverse_frontend-${{ version }}.tar.gz
    sha256: a57fc93006d5848aedc67a74812ea711f8d18a4763e847cafb3173cd9c1818ec

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - uv-build
  run:
    - python >=${{ python_min }}
    - jupyverse-api >=0.15.0,<0.16.0

tests:
  - python:
      imports: jupyverse_frontend
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: BSD-3-Clause
  license_file: COPYING.md
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/api/frontend
  summary: The API for Jupyverse's frontend.

extra:
  recipe-maintainers:
    - davidbrochart
