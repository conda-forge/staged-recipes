context:
  version: "3.0.0"

package:
  name: ocaml-yojson
  version: ${{ version }}

source:
  url: https://github.com/ocaml-community/yojson/releases/download/${{ version }}/yojson-${{ version }}.tbz
  sha256: 99414da7609b92a02474ef4b49ecda15edc8cbba5229341b124e7e4695c39610

build:
  number: 0
  skip:
    - win
  script:
    # Temporary workaround for conda-forge/ocaml-feedstock#101.
    - if: osx
      then: export CONDA_OCAML_CC="${CC}"
    - if: osx
      then: export CONDA_OCAML_MKEXE="${CC} -mmacosx-version-min=${MACOSX_DEPLOYMENT_TARGET} -Wl,-headerpad_max_install_names -Wl,-rpath,@executable_path/../lib"
    - if: osx
      then: export CONDA_OCAML_MKDLL="${CC} -mmacosx-version-min=${MACOSX_DEPLOYMENT_TARGET} -shared -Wl,-headerpad_max_install_names -undefined dynamic_lookup"
    - dune build --profile=release -p yojson @install
    - dune install --prefix="${PREFIX}" --libdir="${PREFIX}/lib/ocaml" yojson

requirements:
  build:
    - ${{ compiler("c") }}
    - ${{ stdlib("c") }}
    - ${{ compiler("ocaml") }}
    - ocaml-dune >=2.7
  host:
    - ocaml >=4.08

tests:
  - package_contents:
      strict: true
      bin:
        - ydump
      files:
        - lib/ocaml/yojson/*
        - doc/yojson/*
        - doc/yojson/odoc-pages/*

about:
  homepage: https://github.com/ocaml-community/yojson
  repository: https://github.com/ocaml-community/yojson
  summary: Low-level JSON parsing and printing library for OCaml
  license: BSD-3-Clause
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - ManuelLerchner
    - MementoRC
