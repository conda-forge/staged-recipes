{% set name = "polaris-lib" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/polaris-lib-{{ version }}.tar.gz
  sha256: 4be9ecd4171dfd5d9e4af1af1b2cd409b395099e8243a38ef203a07cb117154a

build:
  entry_points:
    - polaris = polaris.cli:app
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - setuptools
    - setuptools-scm
    - pip
  run:
    - python >=3.9
    - tqdm
    - loguru
    - typer
    - pyyaml
    - pydantic >=2
    - pydantic-settings >=2
    - authlib
    - httpx
    - tenacity
    - filelock
    - numpy
    - pandas
    - scipy
    - scikit-learn
    - seaborn
    - datamol >=0.12
    - zarr
    - pyarrow

test:
  imports:
    - polaris
  commands:
    - pip check
    - polaris --help
  requires:
    - pip

about:
  summary: Client for the Polaris Hub.
  license: Apache-2.0
  license_file: LICENSE
  home: https://github.com/polaris-hub/polaris

extra:
  recipe-maintainers:
    - hadim
    - cwognum
