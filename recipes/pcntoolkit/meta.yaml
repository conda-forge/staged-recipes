{% set name = "pcntoolkit" %}
{% set version = "0.31.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/amarquand/PCNtoolkit/archive/refs/tags/v0.31.tar.gz
  sha256: 20338d66fc56b33abc93a102aa30bd827a279a2e538a3780390ce9c21aa00a51

build:
  number: 0
  skip: true  # [py<310]
  script: {{ PYTHON }} -m pip install . --no-deps

requirements:
  host:
    - python
    - pip
    - poetry
    - setuptools
  run:
    - python
    - nibabel >=5.3.1
    - pymc >=5.17.0
    - scikit-learn >=1.5.2
    - seaborn >=0.13.2
    - six >=1.16.0
    - bspline
    - scipy >=1.12.3
    - pytorch >=2.1.1  # [not win]
    - pytorch-cpu >=2.1.1  # [not win]
    - libtorch >=2.1.1  # [not win]
    - pytorch-cpu >=1.1.0  # [win]
    - numba >=0.60.0
    - nutpie >=0.13.2


test:
  imports:
    - pcntoolkit
  requires:
    - python
    
about:
  home: http://github.com/amarquand/PCNtoolkit
  summary: Predictive Clinical Neuroscience toolkit
  license: GPL-3.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - amarquand
    - AuguB
