# Global conda-forge pin still has fortran_compiler_version=5 on win-64,
# which resolves the 2018 flang 5.0 package (rejects modern meson flags).
# Override only on Windows; unix keeps gfortran from the global pins.
fortran_compiler_version:  # [win]
  - "19"  # [win]
