--- a/meson.build
+++ b/meson.build
@@ -164,13 +164,18 @@
     'with_rpc_client_only',
     )
 )
-    eigen_dep = dependency('eigen3', required: false)
+    # conda-forge eigen ships CMake package config as Eigen3.
+    eigen_dep = dependency(
+        'Eigen3',
+        method: 'cmake',
+        modules: ['Eigen3::Eigen'],
+        required: false,
+    )
+    if not eigen_dep.found()
+        eigen_dep = dependency('eigen3', required: false)
+    endif
     if not eigen_dep.found()
-        eigen_dep = dependency(
-            'eigen3',
-            subproject: subproject('eigen'),
-            required: get_option('with_eigen'),
-        )
+        error('eigen3 / Eigen3 not found (install conda-forge eigen)')
     endif
     if eigen_dep.found()
         _deps += eigen_dep
