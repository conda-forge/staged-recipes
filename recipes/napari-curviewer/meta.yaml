{% set name = "napari-curviewer" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari-curviewer-{{ version }}.tar.gz
  sha256: e04bbb1df5e316abd32fb7857e65f357fd72a45658ca25810b5b6f20a5cd03e0

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - setuptools >=42.0.0
    - wheel
    - setuptools-scm
    - pip
  run:
    - python >=3.8
    - numpy
    - magicgui
    - qtpy
    - scikit-image
    - vedo
    - vtk
    - scipy

test:
  imports:
    - napari_curviewer
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Rocsg/napari-curviewer
  summary: A plugin to unroll organs along their curved central line
  license: LGPL-3.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
