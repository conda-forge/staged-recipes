{% set name = "pynumero_libraries" %}
{% set version = "0.0" %}
{% set variant = "openblas" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ version }}.tar.gz
  url: https://github.com/santiagoropb/pynumero_libraries/archive/v{{ version }}.tar.gz
  sha256: 9933cef09fc4e2d9fa03a842428760e2180eb84581c6d58f47cec720f220ac14
  
  
build:
  number: 0
  skip: true  # [win]
  run_exports:
    strong:
      - libgcc
      - libstdc++
      
requirements:
  build:
    - cmake
    - {{ compiler('fortran') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - ampl-mp
    - python
    - numpy
  run:
    - python
    - ampl-mp
    - {{ pin_compatible('numpy') }}

test:
  files:
    - test
    - simple_nlp.nl
    
about:
  home: https://github.com/santiagoropb/pynumero_libraries.git
  license: BSD
  summary: 'Libraries for PyNumero, a python package for numerical optimization'

extra:
  recipe-maintainers:
    - santiagoropb
