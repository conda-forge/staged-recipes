context:
  version: "0.2.0"
  python_min: "3.11"

package:
  name: spaday-perspective
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/spaday-perspective/spaday_perspective-${{ version }}.tar.gz
  sha256: a4648cbf15cdd6163fac1096846fe2fd47fbcac2779e0fca26add2f666d303be

build:
  number: 0
  noarch: python
  script:
    env:
      SKIP_HATCH_JS: "1"
    content:
      - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling
    - hatch-js
  run:
    - python >=${{ python_min }}
    - spaday >=0.6.0

tests:
  - python:
      imports:
        - spaday_perspective
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  license: Apache-2.0
  license_file: LICENSE
  summary: Perspective for spaday
  homepage: https://github.com/1kbgz/spaday-perspective
  repository: https://github.com/1kbgz/spaday-perspective

extra:
  recipe-maintainers:
    - timkpaine
