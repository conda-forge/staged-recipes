# This file contains build configuration for the okd-install recipe
# Only essential build variant configurations should be here

# Go compiler version (since this is a Go project)
go_compiler_version:
  - "1.23"

# Target platforms
target_platform:
  - linux-64
