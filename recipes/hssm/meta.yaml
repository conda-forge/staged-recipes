{% set name = "hssm" %}
{% set version = "0.2.0" %}
{% set wfpt_version = "0.1.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  - url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/hssm-{{ version }}.tar.gz
    sha256: a4b874ff970cdef38ca1d6ec738edb1708b6a9b5e5243b7e4217d4a428be4842
    folder: hssm
  - url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/hddm_wfpt-{{ version }}.tar.gz
    sha256: b16d318ce47f4691b90e341f29918fd0f7ca0cbce120f794d2e7c8198a40fb6d
    folder: hddm_wfpt

build:
  number: 0

outputs:
  - name: hddm-wfpt
    version: {{ wfpt_version }}
    build:
      noarch: python
      script: python -m pip install hddm_wfpt -vv --no-deps --no-build-isolation
    requirements:
      build:
        - {{ compiler('cxx') }}
        - {{ compiler('c') }}
        - cython
      host:
        - python >=3.10,<3.13
        - setuptools
        - wheel
        - cython
        - numpy
        - pip
      run:
        - python >=3.10,<3.13
        - {{ pin_compatible('numpy') }}
        - scipy >=1.9.1
    test:
      imports:
        - hddm_wfpt
      commands:
        - pip check
      requires:
        - pip

  - name: hssm
    version: {{ version }}
    build:
      noarch: python
      script: python -m pip install hssm -vv --no-deps --no-build-isolation
    requirements:
      host:
        - python >=3.10,<3.12
        - pip
      run:
        - python >=3.10,<3.12
        - pymc =5.9
        - scipy
        - arviz >=0.14.0,<0.15.0
        - numpy >=1.23.4,<1.26
        - onnx >=1.12.0,<2.0.0
        - jax >=0.4.0,<0.5.0
        - jaxlib >=0.4.0,<0.5.0
        - ssm-simulators >=0.7.2,<0.8.0
        - huggingface_hub >=0.15.1,<0.16.0
        - onnxruntime >=1.15.0,<2.0.0
        - bambi >=0.12.0,<0.13.0
        - numpyro >=0.12.1,<0.13.0
        - seaborn >=0.13.0,<0.14.0
        - pytensor <2.17.4
    test:
      imports:
        - hssm
      commands:
        - pip check
      requires:
        - pip

about:
  home: https://lnccbrown.github.io/HSSM/
  summary: Bayesian inference for hierarchical sequential sampling models.
  dev_url: https://github.com/lnccbrown/HSSM/
  license: "SPDX-License-Identifier: LicenseRef-Brown-University-2023"
  license_file: LICENSE

extra:
  recipe-maintainers:
    - digicosmos86
    - AlexanderFengler
