{% set name = "cytoplay" %}
{% set version = "0.0.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 6f325e6ae6010354f47920d36df05992c0a4e4b8df5db90c59dfb6776ca7c434
build:
  skip: true  # [win]
  number: 1
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation 
  

requirements:
  build:
    - cmake >=3.14
    - make 
    - {{ compiler('cxx') }}
    - libblas
    - liblapack
    - pybind11
    - freeglut # [not osx]
    - libglx # [not osx]
    - xorg-libxi
    - xorg-libxmu
    - glew
    - mesalib
    - mesa-libgl-cos7-x86_64
    
  host:
    - setuptools
    - python
    - libblas
    - liblapack
    - pybind11
    - freeglut # [not osx]
    - libglx # [not osx]
    - xorg-libxi
    - xorg-libxmu
    - glew
    - mesalib
    - mesa-libgl-cos7-x86_64
  run:
    - python
    - libblas
    - liblapack
    - pybind11
    - freeglut # [not osx]
    - libglx # [not osx]
    - xorg-libxi
    - xorg-libxmu
    - glew
    - mesalib

test:
  imports:
    - cytosim
about:
  home: http://cytosim.org/
  license: GPL-3.0-or-later
  license_family: GPL3
  license_file: LICENSE.txt
  summary: "Cytosim: Langevin dynamics of active polymer networks"
  doc_url: https://cytosim.gitlab.io/cytosim/doc/code/doxygen/index.html
  dev_url: https://gitlab.com/f-nedelec/cytosim

extra:
  recipe-maintainers:
    - SergeDmi
