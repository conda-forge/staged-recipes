{% set name = "gotranx" %}
{% set name_underscore = "gotranx" %}
{% set version = "1.3.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name_underscore }}-{{ version }}.tar.gz"
  sha256: 619d5f843d3d56acd2ca557b1022defc25785dceb28e5eba383bf996d93f67b3

build:
  number: 0
  noarch: python
  entry_points:
    - "gotranx=gotranx.cli:app"
  script: python -m pip install -v --no-deps .

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - attrs
    - lark
    - pint
    - rich-click
    - structlog
    - sympy
    - typer
    - toml
test:
  requires:
    - python {{ python_min }}
    - pip
    - pytest
    - pytest-cov

  imports:
    - gotranx

  source_files:
    - tests/
    - pyproject.toml

  commands:
    - gotranx --help
    - pytest -m "not example and not benchmark" --log-cli-level=debug -vvs --maxfail 4 tests

about:
  home: https://github.com/finsberg/gotranx
  summary: A declarative language describing ordinary differential equations
  license: MIT
  license_file: LICENSE
  doc_url: https://finsberg.github.io/gotranx/
  dev_url: https://github.com/finsberg/gotranx

extra:
  recipe-maintainers:
    - finsberg
