{% set name = "google-weather-tools" %}
{% set version = "vconda.candidate.v0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://github.com/google/weather-tools/archive/v{{ version }}.tar.gz
  sha256: dadc84ede790c36aa404e8b5eb22153070bc7edfac2407bf9e80db062f9122a9

build:
  skip: true  # [win]
  number: 0
  script: >
    {{ PYTHON }} -m pip install . -vv && 
    {{ PYTHON }} -m pip install ./weather_dl -vv && 
    {{ PYTHON }} -m pip install ./weather_mv -vv && 
    {{ PYTHON }} -m pip install ./weather_sp -vv 

requirements:
  host:
    - pip
    - python =3.8
    - gdal =3.5.1
    - setuptools_scm
  run:
    - apache-beam[gcp] ==2.40.0  # [unix]
    - earthengine-api >=0.1.263
    - gdal =3.5.1
    - metview-batch
    - python =3.8
    - absl-py
    - cdsapi
    - cfgrib
    - dataclasses
    - ecmwf-api-client
    - geojson
    - google-apitools
    - google-cloud-bigquery
    - google-cloud-datastore
    - google-cloud-firestore
    - google-cloud-storage
    - metview-batch  # [unix]
    - netcdf4
    - numpy
    - pandas
    - pygrib
    - pyproj
    - pytype
    - rasterio
    - requests
    - rioxarray
    - simplejson  # [unix]
    - urllib3
    - xarray
    - xarray-beam  # [unix]

test:
  imports:
    - weather_dl  # [unix]
    - weather_mv  # [unix]
    - weather_mv.loader_pipeline  # [unix]
    - weather_sp  # [unix]
    - weather_sp.splitter_pipeline  # [unix]
  requires:
    - pytest
    - pytest-subtests
    - flake8

about:
  home: "https://weather-tools.readthedocs.io/"
  license: Apache Software
  license_family: APACHE
  license_file: LICENSE
  summary: "Apache Beam pipelines to make weather data accessible and useful."
  doc_url: https://github.com/google/weather-tools/tree/main/docs
  dev_url: https://github.com/google/weather-tools/blob/main/LICENSE

extra:
  recipe-maintainers:
    - mahrsee1997
    - alxmrs
