{% set name = "multigifview" %}
{% set version = "0.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: b53769a3f7a55be6008bd5588d7efd0adf3142b2547c490073d1b9899b2f21fc

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"
  entry_points:
    - multigifview = multigifview:main

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - qt.py >=1.2
    - pyqt >=5.12

test:
  requires:
    - pyqt
  commands:
    - multigifview -h

about:
  home: https://github.com/johnomotani/multigifview
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: LICENCE
  summary: Simple gif viewer
  dev_url: https://github.com/johnomotani/multigifview

extra:
  recipe-maintainers:
    - johnomotani
