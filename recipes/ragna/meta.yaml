{% set name = "ragna" %}
{% set version = "0.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/Ragna-{{ version }}.tar.gz
  sha256: 148a695e2d3ba801fdc4c4c6742b1dbb24cc709336c6fcae8f4b26759b3edec2

build:
  skip: true  # [py<39]
  entry_points:
    - ragna = ragna._cli:app
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - setuptools >=45
    - setuptools-scm >=6.2
    - pip
  run:
    - python >=3.9
    - aiofiles
    - emoji
    - fastapi
    - huey
    - httpx
    - importlib-metadata >=4.6  # [py<310]
    - packaging
    - panel >=1.3,<1.4
    - pydantic >=2
    - pydantic-core
    - pydantic-settings >=2
    - pyjwt
    - python-multipart
    - redis-py
    - questionary
    - rich
    - sqlalchemy >=2
    - tomlkit
    - typer
    - uvicorn

test:
  imports:
    - ragna
  commands:
    - pip check
    - ragna --help
  requires:
    - pip

about:
  summary: RAG orchestration framework
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - aktech
