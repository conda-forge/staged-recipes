{% set name = "mono" %}
{% set version = "4.6.0.245" %}
{% set sha256 = "2981f518cace63bfe10953fecd57c7fef040077a1ac617967dde5466062c1327" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: http://download.mono-project.com/sources/{{ name }}/{{ name }}-{{ version }}.tar.bz2
  sha256: {{ sha256 }}

build:
  number: 0

requirements:
  build:
    - gcc       # [unix]
    - perl
  run:
    - libgcc    # [unix]
test:
  imports:
    - mono --version
    - mono --help

about:
  home: http://www.mono-project.com
  license: MIT
  license_file: LICENSE.txt
  summary: |
    Mono is a software platform designed to allow developers to easily create cross platform applications.
  description: |
    Mono is a software platform designed to allow developers to easily create cross platform applications.
    It is an open source implementation of Microsoft's .NET Framework based on the ECMA standards for C# and the Common Language Runtime.
  doc_url: http://www.mono-project.com/docs/
  dev_url: https://github.com/mono/mono

extra:
  recipe-maintainers:
    - bgruening
