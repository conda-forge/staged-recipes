{% set name = "amazon-sagemaker-hyperpod" %}
{% set version = "0.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/amazon_sagemaker_hyperpod-{{ version }}.tar.gz
  sha256: 19a34099a43482344d06c93ecc15a3273ccb2dbc0551ac8f80b90f04f40f7592

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:    
    - python >=3.8
    - hatchling >=1.5.0
    - jupyterlab >=4.0.0,<4.2
    - hatch-nodejs-version >=0.3.2
    - hatch-jupyter-builder
    - pip
  run:
    - python >=3.8
    - jupyter_server >=2.0.1,<3
    - boto3
    - botocore
    - jupyterlab >=4.0,<5

test:
  imports:
    - amazon_sagemaker_hyperpod
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://aws.amazon.com/sagemaker/
  summary: 'Amazon SageMaker Hyperpod Jupyter Extension'
  license: Apache-2.0
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - bhanderig
