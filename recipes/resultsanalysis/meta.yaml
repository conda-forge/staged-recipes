{% set name = "resultsanlysis" %}
{% set version = "2.0.0" %}
{% set sha256 = "fc889b5b716f8823264217da82a85f4096ac161f5ef9e54566765c23efc7c8d4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://gitlab.developers.cam.ac.uk/ch/hunter/ssiptools/utils/resultsanlysis/-/archive/{{ version }}/{{ name }}-{{ version }}.tar.gz
  sha256:  {{ sha256 }}

build:
  noarch: python
  number: 0
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools
    - setuptools_scm
  run:
    - python >=3.6
    - rdkit
    - matplotlib

test:
  imports:
    - resultsanlysis
  requires:
    - pytest
  commands:
    - pytest --pyargs resultsanlysis

about:
  home: https://gitlab.developers.cam.ac.uk/ch/hunter/ssiptools/utils/resultsanlysis
  license: AGPL-3.0-only
  license_family: AGPL
  license_file: COPYING
  summary: 'Results analysis utility methods.'
  description: |
    Results analysis utility methods module part of the SSIPTools suite of tools.
  doc_url: https://gitlab.developers.cam.ac.uk/ch/hunter/ssiptools/utils/resultsanlysis
  dev_url: https://gitlab.developers.cam.ac.uk/ch/hunter/ssiptools/utils/resultsanalysis

extra:
  recipe-maintainers:
    - mjw99
    - mdd31
