{% set name = "pc_rasterize" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/UM-RMRS/pc-rasterize/releases/download/v{{ version }}/pc_rasterize-{{ version }}-sdist.tar.gz
  sha256: 2d4ff956f5422ad360ac45969db497d802ca367246ca0089c3bb9226b8c3184b

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - pip
    - setuptools
  run:
    - python >=3.10
    - dask-core
    - geopandas
    - numba
    - numpy
    - odc-geo
    - pandas
    - python-pdal
    - rasterio
    - rioxarray
    - shapely >=2
    - xarray

test:
  imports:
    - pc_rasterize
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/UM-RMRS/pc-rasterize
  summary: Rasterize point cloud data in parallel
  dev_url: https://github.com/UM-RMRS/pc-rasterize
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - fbunt
