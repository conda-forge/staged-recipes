{% set name = "tkinterweb" %}
{% set version = "3.18.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/tkinterweb-{{ version }}.tar.gz
  sha256: 0c39f74b9c056a1c635f841cbe952f88a77f1c9081e6114fa6fa9812f2315aaf

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=2.7
    - pip
  run:
    - python >=2.7
    - pillow >=7.1

test:
  imports:
    - tkinterweb
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Andereoo/TkinterWeb
  summary: HTML/CSS viewer for Tkinter - bindings for Tkhtml3
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - Andereoo
