
{% set name = "mljar-supervised" %}
{% set version = "0.10.3" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/mljar-supervised-{{ version }}.tar.gz
  sha256: f1bf61e841127cc4418f2b865f1d5a2c99a190a6abfe522b4e862d20ee4249e0

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - catboost ==0.24.4
    - category_encoders ==2.2.2
    - cloudpickle ==1.3.0
    - dtreeviz ==1.0
    - joblib ==1.0.1
    - lightgbm ==3.0.0
    - matplotlib-base >=3.2.2
    - numpy <1.20.0
    - optuna ==2.6.0
    - pandas ==1.2.0
    - pyarrow >=2.0.0
    - python
    - scikit-learn ==0.24.1
    - scipy ==1.6.1
    - seaborn ==0.10.1
    - shap ==0.36.0
    - tabulate ==0.8.7
    - wordcloud ==1.8.1
    - xgboost ==1.3.3

test:
  imports:
    - supervised
    - supervised.algorithms
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/mljar/mljar-supervised
  summary: Automated Machine Learning for Humans
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - pplonski
