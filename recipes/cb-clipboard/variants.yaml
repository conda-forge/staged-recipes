c_stdlib_version:
  - if: osx and x86_64
    then:
      - "10.15"

c_stdlib:
  - if: osx
    then:
      - macosx_deployment_target

MACOSX_DEPLOYMENT_TARGET:
  - if: osx and x86_64
    then:
      - "10.15"
