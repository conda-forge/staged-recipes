# See https://docs.conda.io/projects/conda-build/en/stable/resources/variants.html
VDF_MODE:
  -
  - pandas
  - dask
# modin + dask_modin => BUG
  - modin
  - dask_modin
###  - ray_modin
### GPU dependencies
# Not compatible with conda-forge at this time because it's impossible add some channel
# I need -c nvidia -c rapidsai -c conda-forge
#  - dask_cudf
#  - cudf
#  - all
