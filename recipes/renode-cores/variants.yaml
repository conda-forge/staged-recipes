c_stdlib:
    - if: linux
      then: sysroot
    - if: osx
      then: macosx_deployment_target

m2w64_c_compiler:
  - m2w64-toolchain
m2w64_cxx_compiler:
  - m2w64-toolchain
m2w64_c_stdlib:
  - m2w64-toolchain
