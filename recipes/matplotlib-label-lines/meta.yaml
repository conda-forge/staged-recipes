{% set name = "matplotlib-label-lines" %}
{% set version = "0.4.0" %}

package:
  name: {{ name }}
  version: {{ version }}

requirements:
  host:
    - pip
    - python

  run:
    - python
    - numpy
    - matplotlib

test:
  imports:
    - labellines

about:
  home: https://github.com/cphyc/matplotlib-label-lines
  license: MIT

