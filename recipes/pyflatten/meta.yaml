package:
  name: pyflatten
  version: "1.0"

source:
  fn: pyflatten-1.0.tar.gz
  url: https://files.pythonhosted.org/packages/80/65/f95ef354584a7210e1baf4401e833477c7cea3f50dcd00b684f98f3c7406/pyflatten-1.0.tar.gz
  md5: efc7ee4a8e5945e680ade35882d24ebc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyflatten = pyflatten:main
    #
    # Would create an entry point called pyflatten that calls pyflatten.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 0

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  imports:
    - pyflatten
    - pyflatten.flatten

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ericmjl/pyflatten
  license: MIT
  summary: 'A utility for flattening nested data structures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
