{% set version = "0.1.0a2" %}
{% set python_min = "3.11" %}

package:
  name: yohou
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/y/yohou/yohou-{{ version }}.tar.gz
  sha256: b54498eaaa6f425bf13bb39d0dbab6ee6cdfca87f1409a758dbef7e4d40864fe

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - hatch-vcs
    - pip
  run:
    - python >={{ python_min }}
    - scikit-learn >=1.6.0
    - sklearn-wrap
    - scipy >=1.13
    - polars >=0.20
    - plotly >=5.19
    - pydantic >=2.6
    - pyarrow >=14.0.0

test:
  imports:
    - yohou
  commands:
    - pip check
    - pytest --help
  requires:
    - pip
    - pytest
    - python {{ python_min }}

about:
  home: https://github.com/stateful-y/yohou
  summary: A time series forecasting package based on Scikit-Learn and Polars
  doc_url: https://yohou.readthedocs.io
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - gtauzin
