context:
  version: "0.1.2"

package:
  name: gwframe
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/g/gwframe/gwframe-${{ version }}.tar.gz
  sha256: b713401fcf8bbc716a5d74d4a4ca8178694d9ca0d946316135b4e0b597143af3

build:
  skip:
    - win
  script: python -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ${{ compiler('cxx') }}
    - cmake >=3.15
    - make
    - pkg-config
    - if: build_platform != target_platform
      then:
        - cross-python_${{ target_platform }}
  host:
    - python
    - pip
    - scikit-build-core >=0.10
    - nanobind >=2.0.0
    - setuptools-scm >=8.0
    - numpy >=1.26
    - libboost-devel
    - ldas-tools-al
    - ldas-tools-cmake
    - ldas-tools-framecpp
    - openssl
    - zlib
    - zstd
  run:
    - python
    - numpy >=1.26
    - ldas-tools-framecpp
    - typer >=0.10.0
    - rich >=13.0.0

tests:
  - python:
      imports:
        - gwframe
        - gwframe.read
        - gwframe.write
        - gwframe.types
      pip_check: true
  - script:
      - gwframe --help

about:
  homepage: https://git.ligo.org/olivia.godwin/gwframe
  summary: High-level Python library to work with gravitational-wave frame (GWF) files
  description: |
    gwframe is a high-level Python library for reading and writing
    gravitational-wave frame (GWF) files, built on top of frameCPP.
  license: GPL-2.0-or-later
  license_file: LICENSE
  documentation: https://git.ligo.org/olivia.godwin/gwframe
  repository: https://git.ligo.org/olivia.godwin/gwframe

extra:
  recipe-maintainers:
    - nothisisolivia
