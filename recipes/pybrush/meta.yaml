{% set name = "pybrush" %}
{% set version = "0.1.1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/cavalab/brush/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 6fba5bc52a4d0d8b82d1b65d631481d1e4dd3614ee648675add6e627adda946f

build:
  skip: True  # [not linux]
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
    - make  # [not win]
    - ninja  # [win]
    - eigen 3.4.*
  host:
    - nlohmann_json
    - pybind11_json 0.2.13
    - python 3.8.2
    - eigen
    - fmt
    - gcc >=12.0
    - gxx >=12.0
    - ninja
    - ceres-solver
    - pybind11 2.6.2
    - pytest
    - pydot
    - pip
  run:
    - python 3.8
    - scikit-learn
    - pandas
    - graphviz
    - deap
    - pmlb

test:
  imports:
    - pybrush
  requires:
    - pytest
  commands:
    - pytest

about:
  home: https://github.com/cavalab/brush
  summary: 'An interpretable machine learning library for training symbolic models'
  description: |
    Brush is a strongly-typed genetic programming language. It is designed for
    **b**ackpropagations and **r**ecursion **u**sing **s**earch **h**euristics.
  license: GPL-3.0-only
  license_family: GPL
  license_file: LICENSE
  doc_url: http://cavalab.org/brush/
  dev_url: https://github.com/cavalab/brush

extra:
  recipe-maintainers:
    - lacava
    - JDRomano2
