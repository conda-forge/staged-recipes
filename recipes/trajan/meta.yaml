{% set name = "trajan" %}
{% set version = "0.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: a7b44ccd9898f45ac04033e46158f01748962ba9a943329ba0c0e27b59743296

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - numpy
    - scipy
    - xarray
    - matplotlib-base
    - cartopy
    - pyproj

test:
  imports:
    - trajan
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/OpenDrift/trajan
  summary: 'Simple, fast, extensible JSON encoder/decoder for Python'
  description: Trajectory analysis package for simulated and observed trajectories
  license: GPL-2.0-or-later
  license_family: GPL
  license_file: LICENSE
  doc_url: https://trajan.readthedocs.io/
  dev_url: https://github.com/OpenDrift/trajan

extra:
  recipe-maintainers:
    - gauteh
    - knutfrode
