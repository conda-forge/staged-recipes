{% set name = "caf.ntem" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/caf_ntem-{{ version }}.tar.gz
  sha256: 49609827c24966cdb28f7eff16d27024f05efdbc47cb701d57d0655fcfc5b902

build:
  entry_points:
    - caf.ntem = caf.ntem.__main__:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=64.0.0
    - versioningit
    - pip
  run:
    - python >=3.10
    - pandas >=2.2.3
    - tqdm >=4.67.1
    - caf.toolkit >=0.9.0
    - sqlalchemy >=2.0.37
    - caf.base >=0.2.0

test:
  imports:
    - caf
  commands:
    - pip check
    - caf.ntem --help
  requires:
    - pip
    - python

about:
  home: https://github.com/transport-for-the-north/caf.ntem
  summary: CAF package for extracting and analysing NTEM data.
  dev_url: https://github.com/transport-for-the-north/caf.ntem
  doc_url: https://cafntem.readthedocs.io/en/stable/
  license: LicenseRef-TfN-License
  license_file: LICENSE

extra:
  recipe-maintainers:
    - MattBuckley-TfN
    - BenTaylor-TfN
    - isaac-tfn
    - Kieran-Fishwick-TfN
