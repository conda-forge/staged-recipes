{% set name = "geogravl3" %}
{% set version = "1.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 07c9610bb310fe73477c3e1e0be6bc6e57033adec033ef91b916b532a9980425

build:
  entry_points:
    - geogravL3=geogravl3.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - pip
  run:
    - python >={{ python_min }}
    - netCDF4
    - numpy
    - pandas
    - pydantic
    - scipy
    - shapely >=2
    - xmltodict

test:
  imports:
    - geogravl3
  commands:
    - pip check
    - geogravL3 --help
    - geogravL3 run_pipeline --help
    - geogravL3 download_resources --help
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://git.gfz-potsdam.de/grace_l3/geogravl3
  summary: Grace Level 3 Python package.
  license: GPL-3.0-or-later
  license_file: LICENSES/GPL-3.0-or-later.txt

extra:
  recipe-maintainers:
    - eboergens
    - DaRa42
    - danschef
