{% set name = "argoverse" %}
{% set version = "1.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/argoai/{{ name }}-api/archive/master.tar.gz
  sha256: 8f46ba59cd28128c3a2ba35111b855e826c66e2911ea1ac779590665d6db897c

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - python {{ python }}                    # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - numpy                                  # [build_platform != target_platform]
    - pybind11                               # [build_platform != target_platform]
  host:
    - python
    - pip
  run:
    - python
    - pip
    - colour
    - descartes
    - imageio
    - h5py
    - hydra-core
    - lap
    - matplotlib
    - motmetrics
    - omegaconf
    - opencv
    - pandas
    - pillow
    - imageio
    - pyntcloud
    - scipy
    - shapely
    - scikit-learn
    - typing_extensions

    - {{ pin_compatible('numpy') }}

test:
  imports:
    - {{ name }}
    - {{ name }}.config
    - {{ name }}.data_loading
    - {{ name }}.evaluation
    - {{ name }}.evaluation.detection
    - {{ name }}.evaluation.stereo
    - {{ name }}.map_representation
    - {{ name }}.utils
    - {{ name }}.visualization

  requires:
    - pip
    - flake8
    - flake8-import-order
    - pytest
    - pytest-benchmark

about:
  home: https://github.com/argoai/{{ name }}-api
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'The official API for the Argoverse dataset.'
  description: |
    The official API for the Argoverse dataset: https://www.argoverse.org/.
  doc_url: https://argoai.github.io/{{ name }}-api
  dev_url: https://github.com/{{ name }}/{{ name }}

extra:
  recipe-maintainers:
    - benjaminrwilson
