diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5ca3065..f0b7425 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -57,9 +57,10 @@ add_library(decord SHARED ${DECORD_CORE_SRCS} ${VIDEOTOOLBOX_SRCS})
 # Set the include directories needed to compile the library
 target_include_directories(decord
         PUBLIC
-        "${CMAKE_CURRENT_SOURCE_DIR}/include"
-        "${CMAKE_CURRENT_SOURCE_DIR}/3rdparty/dlpack/include"
-        "${CMAKE_CURRENT_SOURCE_DIR}/3rdparty/dmlc-core/include"
+        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/include>
+        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/3rdparty/dlpack/include>
+        $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/3rdparty/dmlc-core/include>
+        $<INSTALL_INTERFACE:${CMAKE_INSTALL_INCLUDEDIR}>
         PRIVATE
         "${CMAKE_CURRENT_SOURCE_DIR}/src"
 )
@@ -202,4 +203,11 @@ install(TARGETS decord
         ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR})
 
 install(DIRECTORY include/decord DESTINATION ${CMAKE_INSTALL_INCLUDEDIR})
-export(EXPORT decordTargets FILE "${CMAKE_CURRENT_BINARY_DIR}/decord-config.cmake")
\ No newline at end of file
+export(EXPORT decordTargets FILE "${CMAKE_CURRENT_BINARY_DIR}/decord-config.cmake")
+
+# Install the export set so downstream projects can locate the library with
+# find_package(decord CONFIG). Upstream only exports it into the build tree.
+install(EXPORT decordTargets
+        FILE decord-config.cmake
+        NAMESPACE decord::
+        DESTINATION ${CMAKE_INSTALL_LIBDIR}/cmake/decord)
\ No newline at end of file
