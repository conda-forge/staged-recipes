package:
  name: _sqlite_variant
  version: {{ version }}

build:
  number: 0
  noarch: generic
  string: {{ build_str }}

about:
  summary: "A package to specify the default sqlite variant."

extras:
  recipe-maintainers:
    - hmaarrfk
