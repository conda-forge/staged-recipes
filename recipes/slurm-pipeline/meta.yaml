{% set name = "slurm-pipeline" %}
{% set version = "4.0.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/slurm-pipeline-{{ version }}.tar.gz
  sha256: fa1ec041ff420b3728fbb929ef5f39ed38b5a4789ef1f7c585845ebb899547d4

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python
  run:
    - pytest >=6.2.2
    - python

test:
  imports:
    - slurm_pipeline
  commands:
    - pip check
    - pytest test
    - test $(bin/slurm-pipeline-version.py) = {{ version }}
  requires:
    - pip
    - pytest

about:
  home: https://github.com/acorg/slurm-pipeline
  summary: A Python class and utility script for scheduling SLURM jobs
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - terrycojones
