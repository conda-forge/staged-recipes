schema_version: 1

context:
  python_min: "3.10"
  version: "0.9.3"

package:
  name: ihmt
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/i/ihmt/ihmt-${{ version }}.tar.gz
  sha256: 24a84428df152d067cb805f18ad7c708d21a48f8fc554df46d8d43b1cf7f9790

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling >=1.26
    - pip
  run:
    - python >=${{ python_min }}
    - numpy
    - scipy
    - pyyaml

tests:
  - python:
      imports:
        - ihmt
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Inhomogeneous magnetization transfer simulator for magnetic resonance research.
  license: GPL-3.0-only
  license_file: LICENSE
  homepage: https://github.com/SirTwinkleberry/ihMT

extra:
  recipe-maintainers:
    - SirTwinkleberry
