schema_version: 1

context:
  version: "0.1.0"

package:
  name: jupyverse-kernel
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/j/jupyverse-kernel/jupyverse_kernel-${{ version }}.tar.gz
    sha256: f82fe2de5283e0d00c1d3cccc68f34c07691a325937338903083bc1f21cdd299

build:
  noarch: python
  number: 0
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - uv-build
  run:
    - python >=${{ python_min }}
    - anyio

tests:
  - python:
      imports: jupyverse_kernel
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: BSD-3-Clause
  license_file: COPYING.md
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/api/kernel
  summary: The API for Jupyverse's kernel.

extra:
  recipe-maintainers:
    - davidbrochart
