{% set version = "1.4.1" %}

package:
  name: stock-pandas
  version: {{ version }}

source:
  url: https://github.com/kaelzhang/stock-pandas/archive/refs/tags/{{ version }}.tar.gz
  sha256: fa2689e55d0143442d26ff6d9e2ccfa9d84e603e626e9b9ad4b32f2102144371

build:
  script: make build
  number: 0

requirements:
  host:
    - python >=3.9
    - pip
    - setuptools
  build:
    - python >=3.9
    - Cython >=3.0.5,<4.0.0
    - numpy >=2.0.0rc1
    - setuptools
    - make
  run:
    - python >=3.9

test:
  imports:
    - stock-pandas
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/kaelzhang/stock-pandas
  summary: 'The production-ready subclass of `pandas.DataFrame` to support stock statistics and indicators.'
  license: MIT
  license_family: MIT
  license_file: LICENSE
  dev_url: https://github.com/kaelzhang/stock-pandas

extra:
  recipe-maintainers:
    - kaelzhang
