{% set name = "tango-controls-tui" %}
{% set version = "0.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/SKAJohanVenter/tango-controls-tui/archive/refs/tags/{{ version }}.tar.gz
  sha256: 7a01e85dd9ed27840b5762e058b515a5ba729f216690b93a9b46c9edda091ba2

build:
  number: 0
  skip: true  # [not linux]

requirements:
  build:
    - {{ compiler('rust') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - cpptango
    - cppzmq
    - omniorb
  run:
    - {{ pin_compatible('omniorb', max_pin='x.x') }}

test:
  commands:
    - tango-controls-tui --help

about:
  home: https://github.com/SKAJohanVenter/tango-controls-tui
  license: MIT
  license_file: LICENSE
  summary: 'TUI to explore tango devices'
  description: |
    Text-based user interfaces to explore tango devices.
    Tango-Controls is a software toolkit for building control systems.
  dev_url: https://github.com/SKAJohanVenter/tango-controls-tui

extra:
  recipe-maintainers:
    - beenje
    - SKAJohanVenter
