{% set name = "desktop3" %}
{% set version = "0.5.1" %}
{% set hash_type = "sha256" %}
{% set hash = "98ea7b60db8bdbb2df6acbc0d7f36e300e780d39f340e3eb6f334f995aab48bb" %}
{% set ext = "zip" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.{{ ext }}
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.{{ ext }}
  {{ hash_type }}: {{ hash }}

build:
  number: 0
  skip: true  # [py2k]
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  imports:
    - desktop

about:
  home: https://github.com/eight04/pyDesktop3
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Simple desktop integration for Python.'
  license_family: LGPL
  description: |
    The desktop package provides desktop environment detection and
    resource opening support for a selection of common and standardised
    desktop environments.
  dev_url: https://github.com/eight04/pyDesktop3

extra:
  recipe-maintainers:
    - eight04
    - tadeu
