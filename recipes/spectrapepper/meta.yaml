{% set name = "spectrapepper" %}
{% set version = "0.0.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 8ed432d3ad4cc7102c48ee7e9d42342ad62894c11bdf3926a7d01894b8fc3cd6

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - pip
    - python >=3.6

  run:
    - numpy
    - pandas
    - scipy 
    - matplotlib
    - python >=3.6

test:
  imports:
    - spectrapepper

about:
  home: "https://github.com/enricgrau/spectrapepper"
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "A Python package to simplify and accelerate analysis of spectroscopy data"
  doc_url: https://enricgrau.github.io/spectrapepper/
  dev_url: https://github.com/enricgrau/spectrapepper

extra:
  recipe-maintainers:
    - enricgrau
