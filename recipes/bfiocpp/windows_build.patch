diff --git a/setup.py b/setup.py
index 2996b62..2422e06 100644
--- a/setup.py
+++ b/setup.py
@@ -55,11 +55,13 @@ class CMakeBuild(build_ext):
         if platform.system() == "Windows":
             print("-----------------Windows...")
             cmake_args += [
-                "-DCMAKE_LIBRARY_OUTPUT_DIRECTORY_{}={}".format(cfg.upper(), extdir)
+                "-DCMAKE_LIBRARY_OUTPUT_DIRECTORY_{}={}".format(cfg.upper(), extdir),
+                "-G Ninja"
             ]
-            if sys.maxsize > 2**32:
-                cmake_args += ["-A", "x64"]
-            build_args += ["--", "/m"]
+            # if sys.maxsize > 2**32:
+            #     cmake_args += ["-A", "x64"]
+            # build_args += ["--", "/m"]
+            self.build_temp = "C:/"+str(hex(hash(self.build_temp)))[1:5]
         else:
             cmake_args += ["-DCMAKE_BUILD_TYPE=" + cfg]
             build_args += ["--", "-j4"]
