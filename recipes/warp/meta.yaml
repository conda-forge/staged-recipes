{% set name = "Warp" %}
{% set version = "2.0.0dev0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/alisterburt/pandas/releases/download/v1.2.0.dev0/warp-main.tar.gz  # temporary
  # url: https://github.com/simplejson/simplejson/releases/download/{{ version }}/simplejson-{{ version }}.tar.gz
  sha256: ab378719b670d6af66f62ec944cfce252d3f4de60f42040493eeba5588033014

build:
  skip: true  # [win or osx or cuda_compiler_version == "None"]
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('cuda') }}
    - cmake
  host:
    - python
    - dotnet
    - fftw
    - libtiff
    - pytorch
    - pytorch-cuda
    - torchvision
    - cudnn
  run:
    - python

test:
  commands:
    - WarpTools --help

about:
  home: http://www.warpem.com
  summary: 'Advanced cryo-EM data processing'
  description: |
    placeholder
  # Remember to specify the license variants for BSD, Apache, GPL, and LGPL.
  # Use the SPDX identifier, e.g: GPL-2.0-only instead of GNU General Public License version 2.0
  # See https://spdx.org/licenses/
  license: MIT
  # The license_family, i.e. "BSD" if license is "BSD-3-Clause". 
  # Optional
  license_family: MIT
  # It is required to include a license file in the package,
  # (even if the license doesn't require it) using the license_file entry.
  # Please also note that some projects have multiple license files which all need to be added using a valid yaml list.
  # See https://docs.conda.io/projects/conda-build/en/latest/resources/define-metadata.html#license-file
  license_file: LICENSE.txt
#  doc_url: https://simplejson.readthedocs.io/
#  dev_url: https://github.com/simplejson/simplejson

extra:
  recipe-maintainers:
    - dtegunov
    - alisterburt
