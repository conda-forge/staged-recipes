{% set name = "injective-py" %}
{% set version = "0.7.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: a03431b321afbedf07cb8fae76c6f01311716279cc44c4dc93e5fd73c0549b8c
  patches:
    - update_reqs_0.7.2.patch

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . --ignore-installed --no-cache-dir -vvv
  number: 0

requirements:
  host:
    - python >=3.8.0
    - coincurve
    - pip
  run:
    - aiocron
    - aiohttp
    - bech32
    - bip32
    - coincurve
    - ecdsa
    - eip712
    - grpcio
    - grpcio-tools
    - hdwallets
    - mnemonic
    - protobuf
    - python >=3.8.0
    - requests
    - safe-pysha3
    - urllib3
    - websockets

test:
  imports:
    - pyinjective
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/InjectiveLabs/sdk-python
  summary: 'Injective Python SDK, with Exchange API client'
  license: Apache-2.0
  license_file:
    - LICENSE.md
    - NOTICE.md

extra:
  recipe-maintainers:
    - https://github.com/MementoRC
