{% set name = "fury" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: cbe48366de47074d65491a1c8d6cc3efd917c1013925b7652733538e3db932cd

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"

requirements:
  host:
    - python
    - pip
    - numpy
    - scipy
    - vtk

  run:
    - python
    - {{ pin_compatible('numpy') }}
    - scipy
    - matplotlib
    - vtk
    - dipy

test:
  imports:
    - fury
    # - fury.tests

about:
  home: http://github.com/fury-gl/fury
  license: "BSD-3-Clause"
  license_family: BSD
  license_file: LICENSE
  summary: 'Free Unified Rendering for Python'

  description: |
    Free Unified Rendering for Python (FURY) is a library that offers
    a collection of new widgets, functionnality to vtk. It works under 
    Python 2.7 and Python 3.4+

  doc_url: http://fury.gl/
  dev_url: https://github.com/fury-gl/fury

extra:
  recipe-maintainers:
    - skoudoro
    - garyfallidis
