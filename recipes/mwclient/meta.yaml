package:
  name: mwclient
  version: "0.8.4"

source:
  fn: mwclient-0.8.4.tar.gz
  url: https://pypi.python.org/packages/66/84/e99c39f3ab25a18eea9371d4e47ba79866d08f2e0ef3d70926f3ebd05c1b/mwclient-0.8.4.tar.gz
  md5: 8532c9e0e0cbda4e1b6495c361d0310c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mwclient = mwclient:main
    #
    # Would create an entry point called mwclient that calls mwclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests_oauthlib
    - six

  run:
    - python
    - requests_oauthlib
    - six

test:
  # Python imports
  imports:
    - mwclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - mock
    - pytest
    - pytest-cache
    - pytest-cov
    - pytest-pep8
    - responses >=0.3.0
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/btongminh/mwclient
  license: MIT
  summary: 'MediaWiki API client'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
