{% set name = "sparse_dot_mkl" %}
{% set version = "0.2.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 342582d5a0d08a406f882e1af6ded1a2708a5f8f10c758f94689453dfae3690d

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - numpy
    - pip
    - python
    - scipy
  run:
    - numpy
    - python
    - scipy

test:
  imports:
    - sparse_dot_mkl
    - sparse_dot_mkl.tests
  requires:
    - coverage
    - nose

about:
  home: "https://github.com/flatironinstitute/sparse_dot"
  license: MIT
  license_family: MIT
  license_file: 
  summary: "Intel MKL wrapper for sparse matrix multiplication"
  doc_url: 
  dev_url: 

extra:
  recipe-maintainers:
    - your-github-id-here
