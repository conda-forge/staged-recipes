schema_version: 1

context:
  version: 4.1.1

package:
  name: kreuzberg
  version: ${{ version }}

source:
  url: https://github.com/kreuzberg-dev/kreuzberg/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 0f44a073270ceaad3997a769760d1a02ac84de5219c3d27a8d03130fe4ff5262

build:
  number: 0
  script: 
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - kreuzberg = kreuzberg.__main__:main

requirements:
  build:
    - ${{ stdlib('c') }}
    - ${{ compiler('c') }}
    - ${{ compiler('rust') }}
    - cargo-bundle-licenses
  host:
    - python
    - maturin >=1,<2
    - pip
  run:
    - python

tests:
  - python:
      imports:
        - kreuzberg
      pip_check: true
  - script:
      - kreuzberg --help

about:
  summary: High-performance document intelligence library for Python. Extract text, metadata, and structured data from PDFs, Office documents, images, and 50+ formats. Powered by Rust core for 10-50x speed improvements.
  license: MIT
  license_file:
    - crates/kreuzberg-tesseract/LICENSE
    - THIRDPARTY.yml
  homepage: https://kreuzberg.dev
  repository: https://github.com/kreuzberg-dev/kreuzberg
  documentation: https://kreuzberg.dev

extra:
  recipe-maintainers:
    - thewchan
