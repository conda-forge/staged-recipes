schema_version: 1

context:
  name: aiodataloader
  version: 0.4.2

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/aiodataloader-${{ version }}.tar.gz
  sha256: c92f6f2fb7ee13939ffd68274895aca44ccc0294a1275179bfb8af2b29b788e1

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - typing_extensions >=4.1.1

tests:
  - python:
      imports:
        - aiodataloader
      python_version:
        - ${{ python_min }}.*
        - 3.*
      pip_check: true

about:
  summary: Asyncio DataLoader implementation for Python
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/syrusakbary/aiodataloader
  repository: https://github.com/syrusakbary/aiodataloader

extra:
  recipe-maintainers:
    - beenje
