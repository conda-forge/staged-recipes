package:
  name: pyvisa-py
  version: "0.2"

source:
  fn: PyVISA-py-0.2.tar.gz
  url: https://pypi.python.org/packages/59/ef/c79cd2c221dba12628242e917afe53aa08bb221016f7bee76ea1ff130a22/PyVISA-py-0.2.tar.gz
  md5: 1f5433d4f0b8f42bd8507fbc0a77f66b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvisa-py = pyvisa-py:main
    #
    # Would create an entry point called pyvisa-py that calls pyvisa-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyvisa >=1.8

  run:
    - python
    - pyvisa >=1.8

test:
  # Python imports
  imports:
    - pyvisa-py
    - pyvisa-py.protocols
    - pyvisa-py.testsuite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hgrecco/pyvisa-py
  license: MIT License
  summary: 'Python VISA bindings for GPIB, RS232, and USB instruments'
  license_family: MIT

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
