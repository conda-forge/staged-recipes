{% set name = "pyhunspell" %}
{% set version = "0.5.2" %}
{% set sha256 = "13f4c98b61166cf420d769971cce8f5ee499d9b0ad7fed0a7ad40d1b6656a27a" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://github.com/blatinier/{{ name }}/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - toolchain
  run:
    - python
    - hunspell

test:
  imports:
    - hunspell

about:
  home: http://github.com/blatinier/pyhunspell
  license: GPL-3.0
  license_family: GPL
  license_file: COPYING
  summary: 'Python binding for hunspell'

  description: |
    Python binding for Hunspell usage.
  doc_url: https://github.com/blatinier/pyhunspell/wiki/Documentation
  dev_url: https://github.com/blatinier/pyhunspell

extra:
  recipe-maintainers:
    - blatinier
