{% set name = "smopy" %}
{% set version = "0.0.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/smopy-{{ version }}.tar.gz
  sha256: b85202617b9cc576436169c495a45730a831301d9a208067c11ad4ab3c7f5ce5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - ipython
    - matplotlib-base
    - numpy
    - pillow
    - python >=3.6
    - six

test:
  imports:
    - smopy
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/rossant/smopy
  summary: OpenStreetMap image tiles in Python
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - julian-belina
