{% set name = "jupyterhub-ldapauthenticator" %}
{% set version = "1.2.1" %}
{% set sha256 = "192406a8872727fdf4651aaa0d47cc91401c173399db1b835084ef86dbba49e2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  skip: True  # [win or py<35]
  # noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - jupyterhub
    - ldap3
    - tornado
    - traitlets

test:
  imports:
    - ldapauthenticator

about:
  home: https://github.com/jupyterhub/ldapauthenticator
  license: BSD-3
  license_family: BSD
  license_file: LICENSE
  summary: 'LDAP Authenticator for JupyterHub'

extra:
  recipe-maintainers:
    - dhirschfeld
    - minrk
