{% set name = "geohexgrid" %}
{% set version = "2.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/geohexgrid-{{ version }}.tar.gz
  sha256: 5fda7b4934aa1c125d2407b565070803460eb9b8c63b066cbe354d5363fcfbff

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - hatchling
    - pip
  run:
    - python >=3.9
    - geopandas >=0.11.1
    - rtree >=1.0.0

test:
  imports:
    - geohexgrid
  commands:
    - pip check
  requires:
    - pip

about:
  summary: A Python library for making geographic flat-top hexagon grids like QGIS's `create grid` function
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - christophfink
