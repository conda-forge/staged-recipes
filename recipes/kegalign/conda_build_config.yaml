c_compiler_version:
  - 12          # [linux and (cuda_compiler_version or "").startswith("12")]
  - 11          # [linux and (cuda_compiler_version or "").startswith("11")]

c_stdlib_version:
  - 2.17        # [linux and (cuda_compiler_version or "").startswith("12")]
  - 2.17        # [linux and (cuda_compiler_version or "").startswith("11")]

cxx_compiler_version:
  - 12          # [linux and (cuda_compiler_version or "").startswith("12")]
  - 11          # [linux and (cuda_compiler_version or "").startswith("11")]

cuda_compiler:
  - cuda-nvcc   # [linux and (cuda_compiler_version or "").startswith("12")]
  - nvcc        # [linux and (cuda_compiler_version or "").startswith("11")]

cdt_name:
  - cos7        # [linux and (cuda_compiler_version or "").startswith("12")]
  - cos7        # [linux and (cuda_compiler_version or "").startswith("11")]

docker_image:
  - quay.io/condaforge/linux-anvil-cuda:11.8    # [linux and (cuda_compiler_version or "").startswith("12")]
  - quay.io/condaforge/linux-anvil-cuda:11.8    # [linux and (cuda_compiler_version or "").startswith("11")]

cuda_compiler_version:
  - 12.0
  - 11.8
