{% set version = '1.0.1' %}
{% set sha256 = '7abe69c9d02374385e23d0e7d6279d5fa03899989d25b381d837fa46cf294831' %}
{% set name = 'xxhash' %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  #fn: {{ name }}-{{ version }}.zip
  sha256: {{ sha256 }}
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.zip

build:
  number: 0
  script:
    - python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - python
    - toolchain
    - msinttypes  # [win]
    - vc 9        # [win and py27]
    - vc 10       # [win and py34]
    - vc 14       # [win and py>=35]
    - pip
  run:
    - python
    - vc 9    # [win and py27]
    - vc 10   # [win and py34]
    - vc 14   # [win and py>=35]

test:
  # Python imports
  imports:
    - xxhash
  commands:
    - conda inspect linkages {{ name|lower }}  # [linux]
    - conda inspect objects {{ name|lower }}   # [osx]
  requires:
    - nose >1.3.0

about:
  home: https://github.com/ifduyue/python-xxhash
  license: BSD-2-Clause
  license_file: LICENSE
  summary: 'Python binding for xxHash'
  license_family: BSD

extra:
  recipe-maintainers:
    - mariusvniekerk
