diff --git a/CMakeLists.txt b/CMakeLists.txt
index 3107eb3..8c99a1e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -7,7 +7,13 @@ if(CMAKE_VERSION VERSION_GREATER_EQUAL 3.15)
   cmake_policy(SET CMP0092 NEW)
 endif()
 
-project(libsecp256k1
+if(NOT BUILD_SHARED_LIBS)
+  set(MY_PROJECT_NAME libsecp256k1-static)
+else ()
+  set(MY_PROJECT_NAME libsecp256k1)
+endif()
+
+project(${MY_PROJECT_NAME}
   # The package (a.k.a. release) version is based on semantic versioning 2.0.0 of
   # the API. All changes in experimental modules are treated as
   # backwards-compatible and therefore at most increase the minor version.
@@ -51,6 +57,8 @@ endif()
 
 option(SECP256K1_INSTALL "Enable installation." ${PROJECT_IS_TOP_LEVEL})
 
+option(SECP256K1_INSTALL_HEADERS "Enable headers installation." ${PROJECT_IS_TOP_LEVEL})
+
 option(SECP256K1_ENABLE_MODULE_ECDH "Enable ECDH module." ON)
 if(SECP256K1_ENABLE_MODULE_ECDH)
   add_compile_definitions(ENABLE_MODULE_ECDH=1)
