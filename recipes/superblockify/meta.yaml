{% set name = "superblockify" %}
{% set version = "1.0.0rc3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/superblockify-{{ version }}.tar.gz
  sha256: 7aa300bb7c973406f74104442026039873a5aa23a8ec3b6ac259752b971af99e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=61.2
    - pip
  run:
    - python >=3.10
    - osmnx >=1.6.0
    - geopandas
    - rasterio
    - shapely
    - pyrosm
    - numba
    - tqdm
    - contextily
    - ruamel.yaml
    - pyarrow
    - seaborn

test:
  imports:
    - superblockify
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/NERDSITU/superblockify
  summary: Automated Generation, Visualization, and Analysis of potential Superblocks in Cities
  license: AGPL-3.0-or-later
  license_file: LICENSE
  doc_url: https://nerdsitu.github.io/superblockify/
  dev_url: https://github.com/NERDSITU/superblockify

extra:
  recipe-maintainers:
    - cbueth
