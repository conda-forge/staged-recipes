{% set name = "konnektor" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/OpenFreeEnergy/{{ name }}/archive/refs/tags/v{{ version }}.tar.gz
  sha256: a77bcf03a8b7801887508a39223009f4035683600e9c81d46b69c070e3402666

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - setuptools >=61.0
    - versioningit
    - pip
  run:
    - python >=3.9
    - gufe
    - numpy
    - rdkit
    - networkx
    - scikit-mol
    - scikit-learn
    - ipycytoscape

test:
  imports:
    - konnektor
  commands:
    - pytest --pyargs konnektor
  requires:
    - pytest

about:
  home: https://github.com/OpenFreeEnergy/konnektor
  summary: Konnektor is a package for calculating networks.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mikemhenry
