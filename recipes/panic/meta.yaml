{% set name = "panic" %}
{% set version = "9.4.1" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/panic-{{ version }}.tar.gz
  sha256: 5f601f9496d1f2f71ea443d6adb39d4fcae5805fb5f193b0b7af8e8b45dd9499

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  entry_points:
    - panic = panic.gui.gui:main
    - PyAlarm = panic.ds.PyAlarm:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - fandango
    - pytango
    - taurus    

test:
  imports:
    - panic
  commands:
    - pip check
    - PyAlarm --help
  requires:
    - python >={{ python_min }}  
    - pip
    - fandango
    - pytango

about:
  home: https://gitlab.com/tango-controls/panic
  summary: PANIC, a python Alarm System for TANGO
  doc_url: https://pythonhosted.org/panic/
  license: LGPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - sergirubio
