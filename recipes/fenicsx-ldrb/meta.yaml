{% set name = "fenicsx-ldrb" %}
{% set python_min = "3.10" %}
{% set version = "0.1.9" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/fenicsx_ldrb-{{ version }}.tar.gz
  sha256: 2db3d62d414b84f249d42f0b4ba3dcbd391deee0b8cb7b7d1ef2e561dad3aec3

build:
  number: 0
  noarch: python
  entry_points:
    - "ldrb=ldrb.cli:main"
  script: python -m pip install -v --no-deps .

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - numpy
    - scipy
    - adios4dolfinx
    - numba
    - fenics-dolfinx

test:
  requires:
    - python {{ python_min }}
    - pytest
    - pytest-cov
    - pip
  imports:
    - ldrb
  source_files:
    - tests/
    - pyproject.toml
  commands:
    - pip check
    - ldrb --help
    - pytest --log-cli-level=debug -vvs --maxfail 4 tests


about:
  home: https://github.com/finsberg/fenicsx-ldrb
  summary: Laplace-Dirichlet Rule-based algorithm for assigning myocardial fiber orientations
  license: MIT
  license_file: LICENSE
  doc_url: https://finsberg.github.io/fenicsx-ldrb/
  dev_url: https://github.com/finsberg/fenicsx-ldrb

extra:
  recipe-maintainers:
    - finsberg
