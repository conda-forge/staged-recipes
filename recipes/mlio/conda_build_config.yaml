# The default deployment target used by conda-forge is 10.9 which is not
# compatible with pybind11 due to lack of sized C++ deallocation.
MACOSX_DEPLOYMENT_TARGET:  # [osx]
  - 10.12                  # [osx]

arrow_cpp:
  - 0.14.1
