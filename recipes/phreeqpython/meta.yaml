{% set name = "phreeqpython" %}
{% set version = "1.5.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/phreeqpython-{{ version }}.tar.gz
  sha256: 68bfa3f0f39bca47e6525079cb82da08f5509f6c56e90ffafc4cf55472f091d8

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python
    - setuptools >=42
    - wheel
    - pip
  run:
    - python
    - periodictable
    - scipy

test:
  imports:
    - phreeqpython
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Vitens/phreeqpython
  summary: Vitens viphreeqc wrapper and utilities
  license: Apache-2.0
  license_file: LICENCE

extra:
  recipe-maintainers:
    - jan-janssen
