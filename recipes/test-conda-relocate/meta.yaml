
package:
  name: test-conda-relocate
  version: 0.1

source:
  path: ./src

build:
  skip: true  # [not py27]
  number: 0
  detect_binary_files_with_prefix: True

requirements:
  build:
    - toolchain
    - cmake
  run:

test:
  commands:
    - test-conda-relocate

about:
  home: http://example.com
  license: GPLv3
  summary: 'Example trying to relocate binary containing prefix'

extra:
  recipe-maintainers:
    - dolfim