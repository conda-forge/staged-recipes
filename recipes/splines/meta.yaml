{% set name = "splines" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/splines-{{ version }}.tar.gz
  sha256: c7d2fa8c217a468753307d489bd1fba59532c77aec07ef1e0caa83d1f378f06d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=40.8.0
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - numpy

test:
  imports:
    - splines
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://splines.readthedocs.io/
  summary: Splines in Euclidean Space and Beyond
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
