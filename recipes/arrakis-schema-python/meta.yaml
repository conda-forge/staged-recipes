{% set name = "arrakis-schema-python" %}
{% set pypi_name = "arrakis-schema" %}
{% set version = "0.1.1" %}

{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ pypi_name[0] }}/{{ pypi_name }}/arrakis_schema-{{ version }}.tar.gz
  sha256: ff821d535e776707a155aa1786ea699f9babfd605ebf00c41ebb508449241620

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - hatchling
  run:
    - importlib_resources # [py<312]
    - python >={{ python_min }}

test:
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - python -m pip check
    - python -m pip show {{ pypi_name }}

about:
  home: https://git.ligo.org/ngdd/arrakis-schema
  dev_url: https://git.ligo.org/ngdd/arrakis-schema
  doc_url: https://docs.ligo.org/ngdd/arrakis-schema
  summary: Schemas for the Arrakis API
  license: LGPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jrollins
    - myNameIsPatrick
