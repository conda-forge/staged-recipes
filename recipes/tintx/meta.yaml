{% set name = "tintx" %}
{% set version = "2022.9.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/tintx-{{ version }}.tar.gz
  sha256: 8630fb3690a6be0032c40a96b0934ee54719a6de233985fb6d96dd2d297172c6

build:
  entry_points:
    - tintx = tintx.cli:tintx
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - cartopy
    - click
    - cftime
    - dask-core
    - pandas
    - pytables
    - numpy
    - scipy
    - netcdf4
    - matplotlib-base
    - xarray
    - tqdm
    - typing-extensions

test:
  imports:
    - tintx
  commands:
    - pip check
    - tintx --help
  requires:
    - pip

about:
  home: https://github.com/antarcticrainforest/tintX
  summary: Tracking facility to track rainfall and other non-continous data.
  dev_url: https://github.com/antarcticrainforest/tintX
  license: BSD-3-Clause
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - antarcticrainforest
