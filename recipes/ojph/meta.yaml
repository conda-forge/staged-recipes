{% set version = "0.0.1" %}

package:
  name: ojph
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/o/ojph/ojph-{{ version }}.tar.gz
  sha256: 4a91a4dc70f13c93c174f412abe8e25abe358a30fead417eab759a25e236c0cf
  patches:
    - bring_back_python_39.diff

build:
  script: python -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
  host:
    - python
    - pip
    - setuptools
    - pybind11
    - numpy
    - openjph
  run:
    - python
    - numpy >=1.24

test:
  requires:
    - pip
  commands:
    - pip check
  imports:
    - ojph

about:
  home: https://github.com/ramonaoptics/ojph
  summary: 'Python bindings for OpenJPH, a JPEG 2000 codec'
  license: BSD-3-Clause
  license_file: LICENSE.txt
  dev_url: https://github.com/ramonaoptics/ojph

extra:
  recipe-maintainers:
    - hmaarrfk
