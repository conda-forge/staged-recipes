- import os, datetime, subprocess
+ import os, subprocess

  if not os.path.exists(tinycc_dir):
-     subprocess.run(["git", "clone", "--depth", "1",
-                     "git://repo.or.cz/tinycc.git"], ...)
+     raise RuntimeError("TinyCC source must be supplied by the build recipe")

- subprocess.run(["make", 'CFLAGS="-fPIC"'], ...)
+ build_env = os.environ.copy()
+ build_env["CFLAGS"] = (
+     build_env.get("CFLAGS", "") + " -fPIC"
+ ).strip()
+ subprocess.run(
+     ["make", "-j" + build_env.get("CPU_COUNT", "1")],
+     cwd=tinycc_dir,
+     env=build_env,
+     check=True,
+ )

- version = datetime.datetime.now(...).strftime("%Y.%m.%d")
+ version = os.environ["TCCBOX_VERSION"]
