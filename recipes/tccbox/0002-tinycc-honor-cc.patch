 case $targetos in
   Darwin)
     ...
     if test -z "$build_cross"; then
-      cc=`command -v cc`
-      cc=`readlink $cc || echo clang`
       tcc_usrinclude="`xcrun --show-sdk-path`/usr/include"
