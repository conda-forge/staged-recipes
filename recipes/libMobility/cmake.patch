diff --git a/CMakeLists.txt b/CMakeLists.txt
index 8e92bb5..a995611 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -30,24 +30,21 @@ if(BLAS_FOUND AND LAPACK_FOUND)
   find_path(MKL_INCLUDE_DIR NAMES mkl.h PATHS $ENV{MKLROOT}/include)
   if(MKL_INCLUDE_DIR)
     message(STATUS "Intel MKL environment detected")
-    add_compile_definitions(USE_MKL)
+    add_compile_definitions(PUBLIC USE_MKL)
     include_directories(${MKL_INCLUDE_DIR})
   endif()
-  message(STATUS "BLAS include dirs: ${BLAS_INCLUDE_DIRS}")
-  message(STATUS "LAPACK include dirs: ${LAPACKE_INCLUDE_DIRS}")
-  find_path(BLAS_INCLUDE_DIR NAMES cblas.h mkl.h PATHS
+  find_path(BLAS_INCLUDE_DIR NAMES lapacke.h PATHS
     $ENV{CONDA_PREFIX}/include
     $ENV{PREFIX}/include
     $ENV{MKLROOT}/include
     $ENV{BLAS_HOME}/include
     /usr/include
     /usr/local/include
-    NO_DEFAULT_PATH  # Avoids searching in default paths
   )
   include_directories(${BLAS_INCLUDE_DIR})
   # Linking BLAS and LAPACK libraries
-  link_libraries(${BLAS_LIBRARIES} ${LAPACK_LIBRARIES})
-
+  add_library(BLAS::BLAS)
+  link_libraries(${LAPACK_LIBRARIES} ${LAPACKE_LIBRARIES})
 else()
   message(FATAL_ERROR "BLAS and LAPACK not found. Please install them.")
 endif()
