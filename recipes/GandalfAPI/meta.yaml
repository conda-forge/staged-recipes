{% set name = "GandalfAPI" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/GandalfAPI-{{ version }}.tar.gz
  sha256: 9739fdaae06f6dbbbf6f58deb9bc85ae2908f55970fc70417553b94738b31a61

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - requests >=2.25.1

test:
  imports:
    - GandalfAPI
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/GandalfAPI/
  summary: A Python SDK for accessing The Lord of the Rings movie API
  license: AGPL-3.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - hassaanfarooq01
