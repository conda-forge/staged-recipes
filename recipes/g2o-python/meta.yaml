{% set name = "g2o-python" %}
{% set version = "0.0.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 536cb6c468d56573db1620de7e5e730c3fb275b1c2c74a6e9e1af900e9655528	

build:
  number: 0
  script: {{ PYTHON }} -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - cmake
    - make  # [not win]
    - ninja  # [win]
  host:
    - python
    - pip
    - numpy
    - eigen
    - scikit-build
  run:
    - python
    - numpy
    - eigen
    - scikit-build

test:
  imports:
    - g2o

about:
  home: https://github.com/miquelmassot/g2o-python
  license: g2o (BSD), Ceres (BSD), freeglut (X-Consortium)
  license_family: BSD
  summary: "Wrapper for g2o python bindings"
  description: "This is a wrapper of the python binding of g2o using its branch official branch pymem."
  dev_url: https://github.com/miquelmassot/g2o-python

extra:
  recipe-maintainers:
    - miquelmassot
