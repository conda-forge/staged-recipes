{% set name = "libdai" %}
{% set version = "0.3.2" %}
{% set sha256 = "546f89decff4b95c5d578d96194c8ac5caac4e34fb8b31f3d5193a16bd1b43d3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: libdai-{{ version }}.tar.gz
  url: https://github.com/DerThorsten/libdai/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0

requirements:
  build:
    - toolchain
    - cmake
    - boost-cpp >=1.4
    - gmp
  run:
    - gmp

test:
  commands:
    - test -d ${PREFIX}/include/dai  # [unix]
    - test -f ${PREFIX}/include/dai/alldai.h  # [unix]
    - if exist %LIBRARY_PREFIX%\include\dai\alldai.h (exit 0) else (exit 1)  # [win]

about:
  home: https://github.com/DerThorsten/libdai
  license: BSD
  license_family: BSD
  license_file: LICENSE
  summary: 'libDAI'
  description: 'libDAI'
  dev_url: https://github.com/DerThorsten/libdai

extra:
  recipe-maintainers:
    - DerThorsten
