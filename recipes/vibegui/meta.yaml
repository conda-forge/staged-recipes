{% set name = "vibegui" %}
{% set version = "1.0.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/vibegui-{{ version }}.tar.gz
  sha256: 5d714225cbf9b6fa2e775511acd71b0ccc54917fe4c07e18f26e2402248da170

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61.0
    - hatch
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - qtpy >=2.4.3

test:
  imports:
    - vibegui
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip
    - pyside6

about:
  summary: A Python library to create simple GUIs and read/write config data using multiple backends (Qtpy/PySide6/PyQt, WxPython, TKinter, GTK3/GTK4, and Flet) using a JSON schema.
  home: https://pypi.org/project/vibegui/
  dev_url: https://github.com/jacobwilliams/vibegui
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jacobwilliams
