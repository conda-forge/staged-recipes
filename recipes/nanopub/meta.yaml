{% set name = "nanopub" %}
{% set version = "1.2.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}
source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: ed656c009eab5f20d711350b353c4d88dc22424160d3fc55693eea85c959a59e

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - python >=3.5
  host:
    - python >=3.5
    - pip
  run:
    - python >=3.5
    - rdflib
    - requests
    - click
    - yatiml

test:
  imports:
    - {{ name }}
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/fair-workflows/nanopub
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: "Python client for Nanopub"

extra:
  recipe-maintainers:
    - svenvanderburg
    - raar1
    - dsmits
