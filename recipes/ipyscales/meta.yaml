{% set name = "ipyscales" %}
{% set version = "0.2.0" %}
{% set sha256 = "8d77d4b29ccbab9c0a45a4794406e6d66812707efc26ce32f86cb3d64ad51128" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - python
    - pip
  run:
    - python
    - ipywidgets >=7,<8

test:
  # List all the packages/modules that `run_test.py` imports.
  imports:
    - ipywidgets >=7,<8
    - {{ name }}
    - pytest
    - pytest-cov
    - nbval

about:
  home: http://github.com/vidartf/ipywdatawidgets
  license: BSD 3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: 'A set of widgets to help facilitate reuse of large datasets across widgets'

extra:
  recipe-maintainers:
    - vidartf
