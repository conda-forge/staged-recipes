package:
  name: hiveplot
  version: "0.1.8.3"

source:
  fn: hiveplot-0.1.8.3.tar.gz
  url: https://files.pythonhosted.org/packages/4e/d5/ad3026d308372b9e8b51f3c904c9043996e6f526e9247b996f7123b56d77/hiveplot-0.1.8.3.tar.gz
  md5: 2f3d35e7e94dd509daf1b3974780d21a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hiveplot = hiveplot:main
    #
    # Would create an entry point called hiveplot that calls hiveplot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib
    - pycodestyle

  run:
    - python
    - matplotlib
    - pycodestyle

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ericmjl/hiveplot
  license: MIT
  summary: 'Hive plots in Python!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
