{% set name = "termplotlib" %}
{% set version = "0.3.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: d53daeb5e30a49d508d41bdc70379cdf0183fbb8a63c0086d8ade123ee7a3579

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python >=3
    - pip
    - setuptools
  run:
    - python >=3
    - numpy
    - gnuplot

test:
  imports:
    - termplotlib
  source_files:
    - README.md
    - tests
  requires:
    - pytest
    - pytest-codeblocks
  commands:
    - python -m pytest --codeblocks

about:
  home: https://github.com/nschloe/termplotlib
  license: GPL-3.0-or-later
  license_family: GPL
  license_file: LICENSE.txt
  summary: 'Python plotting for the command line'
  dev_url: https://github.com/nschloe/termplotlib

extra:
  recipe-maintainers:
    - nschloe
