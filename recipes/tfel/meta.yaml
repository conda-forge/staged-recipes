package:
  name: "tfel"
  version:  "3.3"

source:
  git_rev: TFEL-3.3.0
  git_url: https://github.com/thelfer/tfel.git
  git_depth: 1
  patches:
    - tfel-3.3.patch        # [osx]
    
requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('fortran') }}
    - git
    - cmake
    - python
    - py-boost

  run:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - python
    - py-boost

about:
  home: http://tfel.sourceforge.net/
  license: GPLv3
  license_file: LICENCE
  summary: ""
