{% set name = "mof" %}
{% set version = "0.9.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/esheldon/mof/archive/refs/tags/{{ version }}.tar.gz
  sha256: 0656923125d575b95533f5740a4a98f41fed261fe55da40228781551bbf5f26e

build:
  # can't be noarch due to py2to3
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  skip: True  # [py>=310 or win]

requirements:
  host:
    - python
    - pip
  run:
    - python
    - numpy
    - numba
    - ngmix >=1,<2
    - esutil
    - galsim
    - meds
    - pyyaml
    - fitsio

test:
  files:
    - {{ RECIPE_DIR }}/config/config8-bd-fitbdf-001.yaml
  imports:
    - mof
    - mof.fofs
    - mof.galsimfit
    - mof.moflib
    - mof.pdfs
    - mof.peaks
    - mof.priors
    - mof.procflags
    - mof.stamps
  commands:
    - mof-test config/config8-bd-fitbdf-001.yaml 1123 2

about:
  home: https://github.com/esheldon/mof
  summary: 'The Multi Object Fitting deblender'
  license: GPL-2.0-or-later
  license_family: GPL
  license_file: LICENSE

extra:
  recipe-maintainers:
    - beckermr
