@echo off
REM Windows build script - currently not supported due to giza dependency
echo "Windows builds are not currently supported for python-pgplot"
echo "This is due to the giza dependency not being available on Windows"
exit /b 1
