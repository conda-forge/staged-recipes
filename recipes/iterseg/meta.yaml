{% set name = "iterseg" %}
{% set version = "0.0.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/iterseg-{{ version }}.tar.gz
  sha256: 25d64e7d4c7a9dc5413fc4d8ee8a238825332eaac7dee2e58e9e12136e1d1c99

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.7
    - pip
  run:
    - python >=3.7
    - numpy
    - dask-core
    - pytorch
    - scikit-image
    - pandas
    - zarr
    - matplotlib-base
    - napari
    - umetrix
    - pyqt
    - pyqt
    - numba
    - scipy
    - seaborn

test:
  imports:
    - iterseg
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/abigailmcgovern/iterseg
  summary: napari plugin for iteratively improving unet-watershed segmentation
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
