{% set name = "cython-blis" %}
{% set version = "0.2.3.dev0" %}
{% set sha256sum = "357b3c7ca7b4d26adc58fc6fa5cf697e094462f3f245164a87b740e6326e91b9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/b/blis/blis-{{ version }}.tar.gz
  sha256: {{ sha256sum }}

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - clangdev  # [win or osx]
  host:
    - python
    - pip
    - msinttypes  # [win and py27]
    - python
    - setuptools
    - numpy 1.15.*
    - wheel >=0.32,<0.33
  run:
    - python

test:
  requires:
    - pytest <4
    - hypothesis
  imports:
    - blis
  commands:
    - python -m pytest --tb=native --pyargs {{ name }}

about:
  home: http://github.com/explosion/cython-blis
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Fast matrix-multiplication as a self-contained Python library – no system dependencies!'

  doc_url: https://github.com/explosion/cython-blis
  dev_url: https://github.com/explosion/cython-blis

extra:
  recipe-maintainers:
    - honnibal
    - ines
