diff --git a/CMakeLists.txt b/CMakeLists.txt
index a5ddc49..10b2b86 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -17,6 +17,7 @@ SET(QNNPACK_LIBRARY_TYPE "default" CACHE STRING "Type of library (shared, static
 SET_PROPERTY(CACHE QNNPACK_LIBRARY_TYPE PROPERTY STRINGS default static shared)
 OPTION(QNNPACK_BUILD_TESTS "Build QNNPACK unit tests" ON)
 OPTION(QNNPACK_BUILD_BENCHMARKS "Build QNNPACK benchmarks" ON)
+OPTION(QNNPACK_USE_SYSTEM_LIBS "Use system-provided dependency libraries" OFF)
 
 # ---[ CMake options
 IF(QNNPACK_BUILD_TESTS)
@@ -53,6 +54,7 @@ SET(CONFU_DEPENDENCIES_SOURCE_DIR "${CMAKE_SOURCE_DIR}/deps"
 SET(CONFU_DEPENDENCIES_BINARY_DIR "${CMAKE_BINARY_DIR}/deps"
   CACHE PATH "Confu-style dependencies binary directory")
 
+IF (NOT QNNPACK_USE_SYSTEM_LIBS)
 IF(NOT DEFINED CLOG_SOURCE_DIR)
   SET(CLOG_SOURCE_DIR "${PROJECT_SOURCE_DIR}/deps/clog")
 ENDIF()
@@ -106,6 +108,7 @@ IF(NOT DEFINED PTHREADPOOL_SOURCE_DIR)
     WORKING_DIRECTORY "${CONFU_DEPENDENCIES_BINARY_DIR}/pthreadpool-download")
   SET(PTHREADPOOL_SOURCE_DIR "${CONFU_DEPENDENCIES_SOURCE_DIR}/pthreadpool" CACHE STRING "pthreadpool source directory")
 ENDIF()
+ENDIF()  # QNNPACK_USE_SYSTEM_LIBS
 
 IF(QNNPACK_BUILD_TESTS AND NOT DEFINED GOOGLETEST_SOURCE_DIR)
   MESSAGE(STATUS "Downloading Google Test to ${CONFU_DEPENDENCIES_SOURCE_DIR}/googletest (define GOOGLETEST_SOURCE_DIR to avoid it)")
@@ -272,6 +275,7 @@ TARGET_INCLUDE_DIRECTORIES(qnnpack PRIVATE src)
 SET_TARGET_PROPERTIES(qnnpack PROPERTIES PUBLIC_HEADER include/qnnpack.h)
 
 # ---[ Configure clog
+IF(NOT QNNPACK_USE_SYSTEM_LIBS)
 IF(NOT TARGET clog)
   SET(CLOG_BUILD_TESTS OFF CACHE BOOL "")
   SET(CLOG_RUNTIME_TYPE "${CPUINFO_RUNTIME_TYPE}" CACHE STRING "")
@@ -281,9 +285,13 @@ IF(NOT TARGET clog)
   # We build static version of clog but a dynamic library may indirectly depend on it
   SET_PROPERTY(TARGET clog PROPERTY POSITION_INDEPENDENT_CODE ON)
 ENDIF()
+ELSE()
+    FIND_LIBRARY(CLOG_LIBRARY clog REQUIRED)
+ENDIF()
 TARGET_LINK_LIBRARIES(qnnpack PRIVATE clog)
 
 # ---[ Configure cpuinfo
+IF(NOT QNNPACK_USE_SYSTEM_LIBS)
 IF(NOT TARGET cpuinfo)
   SET(CPUINFO_BUILD_TOOLS OFF CACHE BOOL "")
   SET(CPUINFO_BUILD_UNIT_TESTS OFF CACHE BOOL "")
@@ -293,9 +301,13 @@ IF(NOT TARGET cpuinfo)
     "${CPUINFO_SOURCE_DIR}"
     "${CONFU_DEPENDENCIES_BINARY_DIR}/cpuinfo")
 ENDIF()
+ELSE()
+    FIND_LIBRARY(CPUINFO_LIBRARY cpuinfo REQUIRED)
+ENDIF()
 TARGET_LINK_LIBRARIES(qnnpack PRIVATE cpuinfo)
 
 # ---[ Configure pthreadpool
+IF(NOT QNNPACK_USE_SYSTEM_LIBS)
 IF(NOT TARGET pthreadpool)
   SET(PTHREADPOOL_BUILD_TESTS OFF CACHE BOOL "")
   SET(PTHREADPOOL_BUILD_BENCHMARKS OFF CACHE BOOL "")
@@ -303,6 +315,9 @@ IF(NOT TARGET pthreadpool)
     "${PTHREADPOOL_SOURCE_DIR}"
     "${CONFU_DEPENDENCIES_BINARY_DIR}/pthreadpool")
 ENDIF()
+ELSE()
+    FIND_LIBRARY(PTHREADPOOL_LIBRARY pthreadpool REQUIRED)
+ENDIF()
 IF(QNNPACK_CUSTOM_THREADPOOL)
   # Depend on pthreadpool interface, but not on implementation.
   # This is used when QNNPACK user (e.g. Caffe2) provides its own threadpool implementation.
@@ -312,6 +327,7 @@ ELSE()
 ENDIF()
 
 # ---[ Configure FXdiv
+IF(NOT QNNPACK_USE_SYSTEM_LIBS)
 IF(NOT TARGET fxdiv)
   SET(FXDIV_BUILD_TESTS OFF CACHE BOOL "")
   SET(FXDIV_BUILD_BENCHMARKS OFF CACHE BOOL "")
@@ -319,17 +335,19 @@ IF(NOT TARGET fxdiv)
     "${FXDIV_SOURCE_DIR}"
     "${CONFU_DEPENDENCIES_BINARY_DIR}/fxdiv")
 ENDIF()
-TARGET_LINK_LIBRARIES(qnnpack PRIVATE fxdiv)
+ENDIF()
 
 # ---[ Configure psimd
+IF(NOT QNNPACK_USE_SYSTEM_LIBS)
 IF(NOT TARGET psimd)
   ADD_SUBDIRECTORY(
     "${PSIMD_SOURCE_DIR}"
     "${CONFU_DEPENDENCIES_BINARY_DIR}/psimd")
 ENDIF()
-TARGET_LINK_LIBRARIES(qnnpack PRIVATE psimd)
+ENDIF()
 
 # ---[ Configure FP16
+IF(NOT QNNPACK_USE_SYSTEM_LIBS)
 IF(NOT TARGET fp16)
   SET(FP16_BUILD_TESTS OFF CACHE BOOL "")
   SET(FP16_BUILD_BENCHMARKS OFF CACHE BOOL "")
@@ -337,7 +355,7 @@ IF(NOT TARGET fp16)
     "${FP16_SOURCE_DIR}"
     "${CONFU_DEPENDENCIES_BINARY_DIR}/fp16")
 ENDIF()
-TARGET_LINK_LIBRARIES(qnnpack PRIVATE fp16)
+ENDIF()
 
 INSTALL(TARGETS qnnpack
     LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}

