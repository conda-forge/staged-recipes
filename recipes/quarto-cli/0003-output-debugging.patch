diff --git a/src/command/check/check.ts b/src/command/check/check.ts
index e3135f1c8..aa33f8705 100644
--- a/src/command/check/check.ts
+++ b/src/command/check/check.ts
@@ -82,7 +82,7 @@ title: "Title"
     );
     const result = await render(mdPath, {
       services,
-      flags: { quiet: true },
+      flags: { quiet: false },
     });
     if (result.error) {
       throw result.error;
diff --git a/src/command/render/render-files.ts b/src/command/render/render-files.ts
index 20c2f2521..f3f1ed35c 100644
--- a/src/command/render/render-files.ts
+++ b/src/command/render/render-files.ts
@@ -253,6 +253,7 @@ export async function renderFiles(
     const numWidth = String(files.length).length;
 
     for (let i = 0; i < files.length; i++) {
+      console.log("renderFiles")
       const file = files[i];
 
       if (progress) {
@@ -344,6 +345,7 @@ export async function renderFiles(
           },
         });
         try {
+          console.log("init DenoDom")
           // one time denoDom init for html compatible formats
           if (isHtmlCompatible(context.format)) {
             await initDenoDom();
@@ -374,6 +376,7 @@ export async function renderFiles(
           // collect line numbers to facilitate runtime error reporting
           const { ojsBlockLineNumbers } = annotateOjsLineNumbers(context);
 
+          console.log("execute")
           // execute
           const baseExecuteResult = await renderExecute(
             context,
@@ -450,6 +453,8 @@ export async function renderFiles(
             };
           });
 
+
+          console.log("render-pandoc")
           // callback
           pushTiming("render-pandoc");
           await pandocRenderer.onRender(format, {
@@ -469,7 +474,8 @@ export async function renderFiles(
     if (progress) {
       info("");
     }
-
+    
+    console.log("waiting on pandoc to complete")
     return await pandocRenderer.onComplete(false, options.flags?.quiet);
   } catch (error) {
     return {
diff --git a/src/command/render/render-shared.ts b/src/command/render/render-shared.ts
index 2af88065e..be9f7d1dc 100644
--- a/src/command/render/render-shared.ts
+++ b/src/command/render/render-shared.ts
@@ -90,6 +90,7 @@ export async function render(
     }
   }
 
+  console.log("Rendering file: " + path + "with options: " + JSON.stringify(options));
   // otherwise it's just a file render
   const result = await renderFiles([{ path }], options);
 
