{% set name = "pycap-dss" %}
{% set version = "1.0.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pycap_dss-{{ version }}.tar.gz
  sha256: 70cfe791552487979e38d3c5e9054cc90bd060564aae9e17d98f799fa74b6ff4

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=61.0
    - wheel
    - versioneer
    - pip
  run:
    - python >=3.10
    - ipython
    - jupyter
    - notebook
    - ipykernel
    - matplotlib-base
    - pyyaml
    - geopandas
    - numpy
    - scipy
    - pandas
    - gdal
    - fiona
    - rasterio >=1.0
    - rasterstats
    - shapely
    - rtree
    - pyproj >=2.0
    - pyshp
    - xlrd
    - openpyxl
    - requests

test:
  imports:
    - pycap
  commands:
    - pip check
  requires:
    - python >=3.10
    - pip

about:
  home: https://github.com/doi-usgs/pycap-dss
  license: CC0-1.0
  license_file: LICENSE
  summary: Analytical solutions for drawdown and sreamflow depletion with groundwater pumping
  description: |
    pycap-dss is a framework for analytical solutions of stream depletion and drawdown from high-capacity groundwater pumping wells.
  dev_url: https://github.com/doi-usgs/pycap-dss


extra:
  recipe-maintainers:
    - mnfienen
