{% set name = "pycap-dss" %}
{% set version = "1.0.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pycap_dss-{{ version }}.tar.gz
  sha256: a761f79801c5d0465e58206d04ba95dc3df2300c7834740a1c639481082c059d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=61.0
    - wheel
    - versioneer
    - pip
  run:
    - python >=3.10
    - ipython
    - jupyter
    - notebook
    - ipykernel
    - matplotlib-base
    - pyyaml
    - geopandas
    - numpy
    - scipy
    - pandas
    - gdal
    - fiona
    - rasterio >=1.0
    - rasterstats
    - shapely
    - rtree
    - pyproj >=2.0
    - pyshp
    - xlrd
    - openpyxl
    - requests

test:
  imports:
    - pycap
  commands:
    - pip check
  requires:
    - python >=3.10
    - pip

about:
  home: https://github.com/doi-usgs/pycap-dss
  license: CC0-1.0
  license_file: LICENSE
  summary: Analytical solutions for drawdown and sreamflow depletion with groundwater pumping
  description: |
    pycap-dss is a framework for analytical solutions of stream depletion and drawdown from high-capacity groundwater pumping wells.
  dev_url: https://github.com/doi-usgs/pycap-dss


extra:
  recipe-maintainers:
    - mnfienen
