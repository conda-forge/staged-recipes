{% set name = "cfanalytics" %}
{% set version = "0.1.3" %}
{% set sha256 = "db812a4d93ce3bcc0b03f59daf7b6cd84f703db53940b134ed27577a14f8c2ab" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [py<36]
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - requests
    - aiohttp
    - pandas

test:
  imports:
    - cfanalytics

about:
  home: https://github.com/raybellwaves/cfanalytics
  license: BSD
  license_file: LICENSE
  summary: 'Downloading, analyzing and visualizing CrossFit data'

extra:
  recipe-maintainers:
    - raybellwaves
