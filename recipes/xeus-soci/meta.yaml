{% set name = "xeus-soci" %}
{% set version = "0.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/jupyter-xeus/xeus-soci/archive/{{ version }}.tar.gz
  sha256: 411dcb04a336e16ee54cc359627de4982e9b20f0c50a43c73db5489aff96c275

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
    - make  # [unix]
  host:
    - xeus >=0.23.2,<0.25
    - cppzmq
    - zeromq
    - nlohmann_json
    - xtl
    - cpp-tabulate >=1.3,<2
    - sqlite
    - postgresql
    - mysql
    - xvega >=0.0.4
    - xproperty

test:
  commands:
    - test -f ${PREFIX}/bin/xsoci  # [unix]
    - if exist %LIBRARY_PREFIX%\bin\xsoci.exe (exit 0) else (exit 1)  # [win]

about:
  home: https://github.com/jupyter-xeus/xeus-xsoci
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'Jupyter kernel for SOCI based on xeus'
  doc_url: https://xeus-xsoci.readthedocs.io

extra:
  recipe-maintainers:
    - SylvainCorlay
    - JohanMabille
    - marimeireles
