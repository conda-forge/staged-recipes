package:
  name: pmesh
  version: "0.1.17"

source:
  fn: pmesh-0.1.17.tar.gz
  url: https://pypi.python.org/packages/b3/0b/6138e553931d0b81172c7ee7d5cfba2538a8e775c86aafcbbaaa7f2e89c7/pmesh-0.1.17.tar.gz
  md5: 741543766853e42fe1301ce2e2522a54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pmesh = pmesh:main
    #
    # Would create an entry point called pmesh that calls pmesh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cython
    - numpy
    - mpi4py
    - mpsort
    - pfft-python

  run:
    - python
    - cython
    - numpy
    - mpi4py
    - mpsort
    - pfft-python

test:
  # Python imports
  imports:
    - pmesh
    - pmesh.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL3
  summary: 'Particle Mesh in Python'
  license_family: GPL3

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
