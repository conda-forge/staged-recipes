context:
  version: 0.0.12
  python_min: 3.10

package:
  name: gepa
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/g/gepa/gepa-${{ version }}.tar.gz
    sha256: 0c725790c28399e333a37f32dc858a674dc9e748fcccac1df632acdf9f0302ef

build:
  number: 0
  script: ${{ PYTHON }} -m pip install .
  noarch: python

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=77.0.1
    - wheel
    - pip
  run:
    - python >=${{ python_min }}

tests:
  - python:
      python_version: ${{ python_min }}.*
      imports:
        - gepa
      pip_check: true

about:
  homepage: https://github.com/gepa-ai/gepa
  summary: A framework for optimizing textual system components (AI prompts, code snippets, etc.) using LLM-based reflection and Pareto-efficient evolutionary search.
  license: MIT
  license_file: LICENSE
extra:
  recipe-maintainers:
    - moritzwilksch
