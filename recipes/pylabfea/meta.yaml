{% set version = "4.4.2" %}
{% set python_min = "3.10" %}

package:
  name: pylabfea
  version: {{ version }}

source:
  url: https://github.com/AHartmaier/pyLabFEA/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 73a1368c1d1ccfc24eff02d16f714eda0b9f31045a7fa3aaf997ac703b5a6e70

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - numpy
    - matplotlib-base
    - scipy
    - scikit-learn
    - ipympl

test:
  requires:
    - pip
  imports:
    - pylabfea
  commands:
    - pip check


about:
  home: https://github.com/AHartmaier/pyLabFEA
  license: GPL-3.0-or-later
  license_file: LICENSE
  summary: "Finite element analysis toolkit in Python"
  description: |
    pyLabFEA is a lightweight Python library for finite element analysis.
    It aims to provide educational and research tools for small-scale FEA modeling 
    with machine learning constitutive models.
  dev_url: https://github.com/AHartmaier/pyLabFEA

extra:
  recipe-maintainers:
    - AHartmaier
