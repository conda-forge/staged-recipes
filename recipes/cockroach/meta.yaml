{% set provider = "cockroach" %}
{% set goname = "github.com/cockroachdb/"+provider.lower() %}
{% set version = "19.1.5" %}

{% set name = goname.split('/')[-1] %}
{% set pkg_src = ('src/'+goname).replace("/",os.sep) %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
#  - folder: {{ pkg_src }}
    url: https://binaries.cockroachdb.com/{{ provider }}-v{{ version }}.src.tgz
    sha256: 1e3329a56e5a1729ed3ac4ff0a97943163325dd4825e8c7c8c1d9fd57bfddfde
    patches:
      - patch_suite4.patch

build:
  number: 0
#  binary_relocation: False
#  script:
#    - mkdir -p $(go env GOPATH)/src/github.com/cockroachdb
#    - cd $(go env GOPATH)/src/github.com/cockroachdb
#    - git clone https://github.com/cockroachdb/cockroach
#    - cd cockroach
#    - make build
#    - mkdir -p $(go env GOPATH)/src/github.com/cockroachdb
#    - cd $(go env GOPATH)/src/github.com/cockroachdb
#
#    - curl -Lo $PREFIX/bin/cockroach https://binaries.cockroachdb.com/{{ provider }}-v{{ version }}.src.tgz  # [unix]
#    - curl -Lo %LIBRARY_BIN%\cockroach.exe https://binaries.cockroachdb.com/{{ provider }}-v{{ version }}.src.tgz  # [win]
#    - chmod +x $PREFIX/bin/cockroach  # [unix]
#    - cd cockroach-v19.1.5
#    - make build
#    -
#    -
#    - curl -Lo $PREFIX/bin/gitea https://dl.gitea.io/gitea/{{ version }}/gitea-{{ version }}-linux-amd64  # [linux64]
#    - curl -Lo $PREFIX/bin/gitea https://dl.gitea.io/gitea/{{ version }}/gitea-{{ version }}-linux-386  # [linux32]
#    - curl -Lo $PREFIX/bin/gitea https://dl.gitea.io/gitea/{{ version }}/gitea-{{ version }}-darwin-10.6-amd64  # [osx]
#    - curl -Lo %LIBRARY_BIN%\gitea.exe https://dl.gitea.io/gitea/{{ version }}/gitea-{{ version }}-windows-4.0-amd64.exe  # [win64]
#    - curl -Lo %LIBRARY_BIN%\gitea.exe https://dl.gitea.io/gitea/{{ version }}/gitea-{{ version }}-windows-4.0-386.exe  # [win32]
#    - chmod +x $PREFIX/bin/gitea  # [unix]

requirements:
  build:
    - {{ compiler('cxx') }}
#    - {{ compiler('c') }}
#    - {{ compiler('cgo') }}
    - go-cgo
    - autoconf >=2.68  # [not win]
    - cmake >=3.1
    - make >=3.81
    - nodejs >=6.11.0
    - yarn >=1.7
    - bison  # [not win]

test:
  commands:
    - cockroach --version

about:
  home: https://www.cockroachlabs.com/
  license: OTHER
  license_file: LICENSE
  summary: CockroachDB - the open source, cloud-native SQL database.
  doc_url: https://www.cockroachlabs.com/docs/stable/
  dev_url: https://github.com/cockroachdb/cockroach

extra:
  recipe-maintainers:
    - mwilson8
    - rluria14
    - sodre
