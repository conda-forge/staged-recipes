{% set name = "amn" %}
{% set version = "0.1.2" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brsynth/amn_library/archive/refs/tags/{{ version }}.tar.gz
  sha256: ba9ae3a1d65ed6824c1dc139e9b43b53093789f954a77f481e588966f1273d59

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - numpy =1.23.5
    - pandas
    - matplotlib
    - scikit-learn
    - xgboost 
    - keras
    - cobra
    - pydantic =1.10.11
    - typing_extensions =4.5.0
    - pip
    - ipykernel
    - tensorflow =2.12.1
    - tensorflow-probability
    - importlib-resources

test:
  source_files: 
    - tests/
  imports:
    - amn
  requires:
    - python {{ python_min }}
  commands:
    - python -m amn --help

about:
  home: https://github.com/brsynth/amn_library
  summary: "Build, Training & Prediction with Artificial Metabolic Networks (AMN)"
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ramez17khaled
