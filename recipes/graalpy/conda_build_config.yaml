graalpy_distribution:
  - standalone
  # - graalvm

ignore_version:
  - python

# Following are dummy keys to get a single version, as in python_abi
python:
 - "2.7"
python_impl:
 - '*'
numpy:
 - '1.16'
