context:
  version: "0.17.0"

package:
  name: ocaml-sexplib0
  version: ${{ version }}

source:
  url: https://github.com/janestreet/sexplib0/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 5b0910b5dab8ec63633be5dbf92a3e4863d415d803cad9dddf99dba43ce7498b

build:
  number: 0
  skip:
    - win
  script:
    - dune build --profile=release -p sexplib0 @install
    - dune install --prefix="${PREFIX}" --libdir="${PREFIX}/lib/ocaml" sexplib0

requirements:
  build:
    - ${{ stdlib("c") }}
    - ${{ compiler("ocaml") }}
    - ocaml-dune >=3.11.0
  host:
    - ocaml >=4.14.0

tests:
  - package_contents:
      strict: true
      files:
        - lib/ocaml/sexplib0/*
        - doc/sexplib0/*

about:
  homepage: https://github.com/janestreet/sexplib0
  repository: https://github.com/janestreet/sexplib0
  summary: Library containing the definition of S-expressions and some base converters
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - ManuelLerchner
    - MementoRC
