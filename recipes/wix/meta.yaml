{% set version = "3.10.3" %}

package:
  name: wix
  version: {{ version }}

source:
  path: ./src

build:
  number: 0
  skip: true  # [not win]

requirements:
  run:
   - vc  14

about:
  home: http://wixtoolset.org/
  summary: WiX Toolset
  license: MS-RL
  license_file: COPYING

extra:
  recipe-maintainers:
    - vidartf
