{% set name = "rpcompletion" %}
{% set version = "6.8.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brsynth/rpCompletion/archive/refs/tags/{{ version }}.tar.gz
  sha256: 266bf580017331bfa1a09e988f9ff435ba9b695092afc8c584f7128bdd0048a2

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - pandas
    - rdkit
    - rr_cache
    - brs_utils
    - rxn_rebuild
    - rplibs

test:
  requires:
    - python {{ python_min }}
  imports:
    - rpcompletion

about:
  home: https://github.com/brsynth/rpCompletion/tree/main
  summary: "Completes mono-component reactions output by RetroPath2.0 with the appropriate cofactors."
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ramez17khaled
