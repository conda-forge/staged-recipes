diff --git a/setup.py b/setup.py
index c61b80b..6c1fb27 100644
--- a/setup.py
+++ b/setup.py
@@ -26,32 +26,6 @@ setup(
     description="A collection of quantum algorithms built using pyQuil and Forest",
     url="https://github.com/rigetticomputing/grove.git",
     packages=find_packages(exclude=["*.tests", "*.tests.*", "tests.*", "tests"]),
-    install_requires=[
-        'numpy',
-        'scipy',
-        'pyquil >= 1.6.1',
-        'funcsigs',
-        'mock',
-        'networkx',
-        'matplotlib'
-    ],
-    extras_require={
-        'tomography': [
-            'cython',
-            'cvxpy',
-            'scs',
-            'qutip'
-        ]
-    },
-    setup_requires=[
-        'pytest-runner',
-        'numpy'
-    ],
-    tests_require=[
-        'tox',
-        'pytest >= 3.0.0',
-        'mock'
-    ],
     license='LICENSE',
     keywords='quantum quil programming hybrid'
 )
