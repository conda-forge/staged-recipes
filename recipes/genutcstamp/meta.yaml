{% set name = "genutcstamp" %}
{% set version = "1.0.3" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: a01a67cd8f79edbe084998895c7deec90b7179b1b5869e6fefa36b478fc4f5c5

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps --no-build-isolation -vv

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}

test:
  imports:
    - genutcstamp
    - GenUTCStamp
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://pypi.org/project/GenUTCStamp/
  license: MIT
  license_file: LICENSE
  summary: Dependency-free UTC timestamp string generation helper (YYYY-MM-DDTHH:MM:SSZ).

extra:
  recipe-maintainers:
    - Marcin-kowalczyk-polenergia
