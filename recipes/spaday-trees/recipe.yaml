context:
  version: "0.2.0"
  python_min: "3.11"

package:
  name: spaday-trees
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/s/spaday-trees/spaday_trees-${{ version }}.tar.gz
  sha256: 64c1d3c5c4a73a5830b344aee4da42399f31d510c98d4f6b7d5c5ed4b13a773e

build:
  number: 0
  noarch: python
  script:
    env:
      SKIP_HATCH_JS: "1"
    content:
      - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling
    - hatch-js
  run:
    - python >=${{ python_min }}
    - spaday >=0.6.0

tests:
  - python:
      imports:
        - spaday_trees
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  license: Apache-2.0
  license_file: LICENSE
  summary: Trees, by Pierre, for spaday
  homepage: https://github.com/1kbgz/spaday-trees
  repository: https://github.com/1kbgz/spaday-trees

extra:
  recipe-maintainers:
    - timkpaine
