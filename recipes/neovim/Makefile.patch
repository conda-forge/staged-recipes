--- Makefile	2016-08-22 02:14:28.000000000 +0000
+++ Makefile	2016-11-04 14:27:01.952863829 +0000
@@ -4,6 +4,8 @@
 # See contrib/local.mk.example
 -include local.mk
 
+LIBTOOLIZE=$(PREFIX)/bin/libtoolize
+
 CMAKE_BUILD_TYPE ?= Debug
 
 CMAKE_FLAGS := -DCMAKE_BUILD_TYPE=$(CMAKE_BUILD_TYPE)
@@ -41,6 +43,8 @@
 DEPS_CMAKE_FLAGS ?=
 USE_BUNDLED_DEPS ?=
 
+CMAKE_EXTRA_FLAGS := -DCMAKE_INSTALL_PREFIX=$(PREFIX)
+
 ifneq (,$(USE_BUNDLED_DEPS))
   BUNDLED_CMAKE_FLAG := -DUSE_BUNDLED=$(USE_BUNDLED_DEPS)
 endif
