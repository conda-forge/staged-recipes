{% set name = "pythonradex" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pythonradex-{{ version }}.tar.gz
  sha256: 04274a9f6c0da57a9021b3f77318469c76dce5b9e64799a95b97313eadb9261f

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - hatchling
    - pip
  run:
    - python >=3.10
    - scipy >=1.14
    - numpy >=2.0
    - numba >=0.61.0

test:
  imports:
    - pythonradex.radiative_transfer
    - pythonradex.LAMDA_file
    - pythonradex.helpers
    - pythonradex.molecule
  requires:
    - pip
    - pytest
  source_files:
    - src/
    - tests/
  commands:
    - pip check
    - pytest ./tests

about:
  home: https://pypi.org/project/pythonradex/
  summary: 1D non-LTE radiative transfer
  dev_url: https://github.com/gica3618/pythonradex
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - gica3618
