{% set name = "pretab" %}
{% set version = "0.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/OpenTabular/PreTab/archive/v{{ version }}.tar.gz
  sha256: 2f7958f5052b3d2dd20365a8b98cc58da5088ba1bcfe6b2017e2283bf090e8af

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - numpy
    - scikit-learn
    - scipy
    - pandas

test:
  imports:
    - pretab
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/OpenTabular/PreTab
  summary: A python package for preprocessing tabular data
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
