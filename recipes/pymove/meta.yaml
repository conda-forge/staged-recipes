{% set name = "pymove" %}
{% set version = "1.1.8" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 5acac666533de707a37b8274f1a59db2eeaf49190915dbc429b36e37b8fef8d3

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - pip
    - branca
    - dask[dataframe]
    - folium >=0.10.1
    - geojson
    - holidays
    - joblib
    - matplotlib
    - mplleaflet
    - numpy
    - pandas
    - pip
    - psutil
    - python
    - pytz ==2019.3
    - scipy
    - shapely
    - scikit-learn
    - tqdm
  run:
    - branca
    - dask[dataframe]
    - folium >=0.10.1
    - geojson
    - holidays
    - joblib
    - matplotlib
    - mplleaflet
    - numpy
    - pandas
    - psutil
    - python
    - pytz ==2019.3
    - scipy
    - shapely
    - scikit-learn
    - tqdm

test:
  imports:
    - pymove
    - pymove.core
    - pymove.models
    - pymove.models.pattern_mining
    - pymove.osm_module
    - pymove.preprocessing
    - pymove.uncertainty
    - pymove.utils
    - pymove.visualization

about:
  home: "https://github.com/InsightLab/PyMove"
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: "A lib python to processing and visualization of trajectories and other spatial-temporal data"
  doc_url: 
  dev_url: 

extra:
  recipe-maintainers:
    - flych3r
