{% set name = "git-annex" %}
{% set version = "6.20180316" %}
{% set sha256 = "8b15ebd8dd2b8bef52d040c25c75000f59bc23ccc5c8b261aa244eae63f4ba71" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://hackage.haskell.org/package/{{ name }}-{{ version }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [win or osx]

requirements:
  build:
    - toolchain
    - pkg-config
    - perl 5.26.*
    - git >=2
    - stack
    - ncurses 5.9
    - libmagic >=5.30
    - libgsasl
    - gmp 6.1.*
    - rsync
    - libffi
    - gnutls
    - libxml2 2.9.*
    - sqlite 3.20.*
    - chrpath  # [linux]
  run:
    - toolchain
    - perl 5.26.*
    - git >=2
    - rsync
    - ncurses 5.9
    - libmagic
    - libgsasl
    - gmp 6.1.*
    - libffi
    - gnutls
    - libxml2 2.9.*
    - sqlite 3.20.*
    - chrpath  # [linux]

about:
  home: https://git-annex.branchable.com
  license: GPLv3
  license_family: GPL
  license_file: COPYRIGHT
  summary: 'Version control large files with git without storing them'
  description: |
    git-annex lets you use git to track large, distributed files without checking them into git

extra:
  recipe-maintainers:
    - notestaff
