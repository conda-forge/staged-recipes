--- a/src/hdf5/CMakeLists.txt
+++ b/src/hdf5/CMakeLists.txt
@@ -28,6 +28,11 @@ if(WIN32)
          if( TARGET hdf5::hdf5_hl-shared )
            target_link_libraries( al PRIVATE hdf5::hdf5_hl-shared )
          endif()
+       elseif( TARGET hdf5::hdf5 )
+         target_link_libraries( al PRIVATE hdf5::hdf5 )
+         if( TARGET hdf5::hdf5_hl )
+           target_link_libraries( al PRIVATE hdf5::hdf5_hl )
+         endif()
        elseif( TARGET hdf5::hdf5-static )
          target_link_libraries( al PRIVATE hdf5::hdf5-static )
          if( TARGET hdf5::hdf5_hl-static )
@@ -37,6 +42,11 @@ if(WIN32)
          # Legacy FindHDF5 module
          target_include_directories( al PRIVATE ${HDF5_C_INCLUDE_DIRS} )
          target_link_libraries( al PRIVATE ${HDF5_C_LIBRARIES} )
+         if( HDF5_C_HL_LIBRARIES )
+           target_link_libraries( al PRIVATE ${HDF5_C_HL_LIBRARIES} )
+         elseif( HDF5_HL_LIBRARIES )
+           target_link_libraries( al PRIVATE ${HDF5_HL_LIBRARIES} )
+         endif()
          target_compile_definitions( al PRIVATE ${HDF5_C_DEFINITIONS} )
        endif()
 