{% set name = "tf_retinanet" %}
{% set version = "3.0.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/fizyr/tf-retinanet.git
  sha256: 2d1cda5002b3b321bad507615634427731e46cf90bad688f11711683a5c9d39b

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python >=2.7
    - cython >=0.28
    - numpy >=1.14.0
    - pip
  run:
    - python >=2.7
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - tf_retinanet
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/fizyr/tf-retinanet
  summary: The Cython compiler for writing C extensions in the Python language.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - OliverEdy
