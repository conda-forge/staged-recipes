{% set name = "rp2biosensor" %}
{% set version = "1.0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/brsynth/rp2biosensor/archive/refs/tags/{{ version }}.tar.gz
  sha256: 4c620aafc0612de7af6b0c60a2c8273effb936601501f8c94d612c13a86947ce

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install -vv .

requirements:
  host:
    - python >=3.7,<3.9
    - pip
  run:
    - python >=3.7,<3.9
    - networkx
    - rdkit
    - rr_cache

test:
  source_files:
    - tests
  imports:
    - rp2biosensor
  requires:
    - pytest
  commands:
    - python -m rp2biosensor --help
    - pytest -v

about:
  home: https://github.com/brsynth/rp2biosensor/
  summary: Build Sensing-Enabling Metabolic Pathways from RetroPath2.0 output
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - tduigou
    - kenza12
