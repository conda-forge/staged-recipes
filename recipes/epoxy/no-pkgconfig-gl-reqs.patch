diff --git a/src/meson.build b/src/meson.build
index fe092d1..7138643 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -108,5 +108,4 @@ pkg.generate(
     'epoxy_has_wgl=@0@'.format(epoxy_has_wgl),
   ],
   filebase: 'epoxy',
-  requires_private: ' '.join(gl_reqs),
 )
