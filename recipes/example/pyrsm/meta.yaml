{% set name = "pyrsm" %}
{% set version = "0.5.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyrsm-{{ version }}.tar.gz
  sha256: 3aa17388350032b4833a7528fadda86f5253b79a8f4c118315b4462fcc868d1f

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python
  run:
    - importlib-resources >=3.3.1
    - ipynbname >=2021.3.2
    - ipython >=8.0.1
    - matplotlib-base >=3.1.1
    - numpy >=1.17.3
    - pandas >=0.25.2
    - python
    - scikit-learn >=1.0.2
    - scipy >=1.4.1
    - seaborn >=0.9.0
    - statsmodels >=0.10.1

test:
  imports:
    - pyrsm
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/pyrsm/
  summary: Python functions for Customer Analytics at the Rady School of Management (RSM)
  dev_url: https://github.com/vnijs/pyrsm
  license: AGPL-3.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - vnijs
