{% set name = "bio-pyvol" %}
{% set version = "1.7.7" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 1d4b00c05f9f65d59e4d86170150e0bb8ad94057edcf4123488b1218862120a6

build:
  number: 0
  entry_points:
    - pyvol=pyvol.__main__:main
  script: "{{ PYTHON }} -m pip install . -vv"
  noarch: python

requirements:
  # build:
  #   - {{ compiler('cxx') }}
  host:
    - pip
    - python
  run:
    - biopython >=1.73
    - configparser
    - msms >=2.6.1
    - numpy >=1.16.1
    - pandas >=0.24.1
    - python
    - scikit-learn >=0.20.2
    - scipy >=1.2.1
    - trimesh >=2.36.29

test:
  imports:
    - pyvol
  commands:
    - pyvol --help

about:
  home: https://github.com/schlessinger-lab/pyvol
  license: MIT
  license_file: LICENSE.txt
  summary: "a PyMOL plugin and python package for visualization, comparison, and volume calculation of protein drug-binding sites"
  doc_url: https://schlessinger-lab.github.io/pyvol/
  dev_url: https://github.com/schlessinger-lab/pyvol/
  doc_source_url: https://github.com/schlessinger-lab/pyvol/README.rst

extra:
  recipe-maintainers:
    - rhs2132
