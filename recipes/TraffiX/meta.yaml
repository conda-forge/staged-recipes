{% set name = "TraffiX" %}
{% set version = "0.1.1a0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/TraffiX-{{ version }}.tar.gz
  sha256: 6e5af96e0726f4e9cfc3fb4124e0c6aa80aceb67e543784ab780c8cd9e0046c6

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - osmnx >=1.3.0,<1.4.dev0
    - pandas >=1.2.4,<1.3.dev0

test:
  imports:
    - TraffiX
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Ultios/StraPy
  summary: A package for macroscopic transportation assignment.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - Ultios
