{% set name = "kanga" %}
{% set version = "0.0.17" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 0e659f7c54530565bbae103844e5b736b7eb6e12ab6b07039023a0a5d6ea7375

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - matplotlib >=3.3.3
    - numpy >=1.19.2
    - python >=3.6
    - scipy >=1.5.2
    - seaborn >=0.11.0
    - statsmodels >=0.12.0

test:
  imports:
    - kanga

about:
  home: "https://github.com/papamarkou/kanga"
  license: MIT
  license_file: LICENSE.txt
  summary: "MCMC diagnostics"

extra:
  recipe-maintainers:
    - papamarkou
