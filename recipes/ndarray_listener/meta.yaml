{% set name = "ndarray_listener" %}
{% set version = "1.0.8" %}
{% set sha256 = "c8509acbe0d401e6e577467ad8818d54d2b0f96f4d724d0e08fbda6adcfc706d" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - pytest-runner
  run:
    - python
    - numpy

test:
  requires:
    - pytest
  imports:
    - ndarray_listener
  commands:
    - python -c "import ndarray_listener; ndarray_listener.test()"

about:
  home: https://github.com/Horta/ndarray_listener
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'NumPy ndarray listener'

  description: |
    NumPy array that listen.
  doc_url: https://github.com/Horta/ndarray_listener
  dev_url: https://github.com/Horta/ndarray_listener

extra:
  recipe-maintainers:
    - Horta
