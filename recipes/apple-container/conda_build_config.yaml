# `container` uses macOS 26 virtualization and networking APIs, so it must be
# built against the macOS 26 SDK (resolved to MacOSX26.1.sdk by conda-forge's
# conda-forge-ci-setup).
MACOSX_SDK_VERSION:
  - "26"
