c_stdlib_version:  # [linux]
  - "2.28"         # [linux]

# Build the Tegra (Jetson) flavour of the forward compatibility package, which comes from a
# different NVIDIA redistributable than the SBSA (data center ARM) one.
arm_variant_type:  # [aarch64]
  - tegra          # [aarch64]
