schema_version: 1

context:
  version: "0.3.1"

package:
  name: vacspin
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/v/vacspin/vacspin-${{ version }}.tar.gz
  sha256: 537a4e16669555524b9688c34d49a899f74185e8d52d2f3a2bb29f72a41af1aa

build:
  noarch: python
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - numpy >=1.22

tests:
  - python:
      imports:
        - vacspin
      python_version:
        - ${{ python_min }}.*
        - "*"
      pip_check: true

about:
  homepage: https://github.com/TaN-MM-Org/vacspin
  license: Apache-2.0
  license_file: LICENSE
  summary: "Group-IV colour-centre spin-photon interfaces, from the strained Hamiltonian to single-shot readout"
  description: |
    Strained spin Hamiltonians, optical transitions and cyclicity, cavity
    Purcell budgets and single-shot readout statistics for group-IV
    colour centres in diamond (SiV-, GeV-, SnV-). Every built-in parameter
    set carries a citation to its source.
  repository: https://github.com/TaN-MM-Org/vacspin
  documentation: https://github.com/TaN-MM-Org/vacspin#readme

extra:
  recipe-maintainers:
    - Tanvir-Mahmud-Mahim
