package:
  name: ndcube
  version: "0.1.dev273"

source:
  fn: ndcube-0.1.dev273.tar.gz
  url: https://pypi.python.org/packages/21/e7/32f31309c75a064277babcdca88d6bb5dadefaea000303c61e177beba4d2/ndcube-0.1.dev273.tar.gz
  md5: 36a200f32aa1ec697e1f0f0b8e6d4391
#  patches:
   # List any patch files here
   # - fix.patch

build:
  script: python setup.py install --offline --no-git --single-version-externally-managed --record record.txt
  # noarch: python
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ndcube = ndcube:main
    #
    # Would create an entry point called ndcube that calls ndcube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - astropy

  run:
    - python
    - astropy
    - matplotlib
    - sunpy

test:
  # Python imports
  imports:
    - ndcube
    - ndcube.tests
    - ndcube.utils
    - ndcube.utils.tests
    - ndcube.visualization

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sunpy.org
  license: BSD 2-Clause
  summary: 'A base package for multi-dimensional contiguious and non-contiguious spatially aware arrays.'
  license_family: BSD

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
