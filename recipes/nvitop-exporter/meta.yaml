{% set name = "nvitop-exporter" %}
{% set version = "1.4.0" %}
{% set sha256 = "70b1793f4c23b2c703216422816e1916a6162d78b8035ff9183301d5d098e86d" %}
{% set build_number = 0 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name|replace("-", "_") }}/{{ name|replace("-", "_") }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  # Package `windows-curses` is not available yet
  skip: true  # [win]
  entry_points:
    - nvitop-exporter = nvitop_exporter.cli:main
  script: {{ PYTHON }} -m pip install . -vv
  number: {{ build_number }}

requirements:
  build:
    - python                              # [build_platform != target_platform]
    - cross-python_{{ target_platform }}  # [build_platform != target_platform]
  host:
    - pip
    - setuptools
    - python {{ python_min }}
  run:
    - nvitop =={{ version }}
    - prometheus_client >=0.4.0
    - python

test:
  imports:
    - nvitop
    - nvitop_exporter
  commands:
    - pip check
    - nvitop-exporter --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/XuehaiPan/nvitop
  summary: Prometheus exporter built on top of `nvitop`.
  dev_url: https://github.com/XuehaiPan/nvitop
  doc_url: https://nvitop.readthedocs.io
  license: Apache-2.0
  license_family: Apache
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - XuehaiPan
