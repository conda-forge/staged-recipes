{% set name = "cabal-install" %}
{% set version = "2.4.1.0" %}
{% set sha256 = "61eb64a5addafca026aff9277291f4643fe07e83886f76d059d42c734fed829c" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  - url: https://github.com/haskell/cabal/archive/{{ name }}-v{{ version }}.tar.gz
    sha256: {{ sha256 }}

build:
  number: 0
  skip: True  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - ghc
    - zlib
  host:
    - ghc
    - zlib
test:
  commands:
    - cabal --help

about:
  home: https://haskell.org/cabal/
  license: BSD 3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'cabal-install is the command line interface to Cabal and hackage.'

  doc_url: https://www.haskell.org/cabal/index.html
  dev_url: https://github.com/haskell/cabal/tree/master/cabal-install

extra:
  recipe-maintainers:
    - eggzilla
