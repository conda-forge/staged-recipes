{% set name = "aim" %}
{% set version = "2.6.0" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 3d1559200d699390f9021569919c0a50a63b38167393377756b3908a83648e4e

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"
  entry_points:
    - aim=aim.cli.cli:cli_entry_point

requirements:
  host:
    - pip
    - python >=3.6
    - googlesearch
    - protobuf
    - anytree
    - pyrser
    - psutil
  run:
    - python >=3.6
    - googlesearch
    - protobuf
    - anytree
    - pyrser
    - psutil
test:
  imports:
    - aim

about:
  home: https://github.com/aimhubio/aim
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "A super-easy way to record, search and compare 1000s of ML training runs"
  dev_url: https://github.com/aimhubio/aim

extra:
  recipe-maintainers:
    - psortos
