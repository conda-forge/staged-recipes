{% set name = "ylearn" %}
{% set version = "0.1.1.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/DataCanvasIO/{{ name }}/archive/refs/tags/{{ version }}.tar.gz
  sha256: f14c1d416fd314e8531d745ab3c7a84bdb47beb72a448c869f7c204d7ff49216

build:
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - cython
    - python
    - numpy
    - scipy
    - pip
  run:
    - python
    - {{ pin_compatible('numpy') }}
    - pandas >=0.25.3
    - scikit-learn >=0.22.1
    - scipy
    - networkx
    - ipython
    - joblib
    - pydot
    - pytorch  # [not win]

test:
  imports:
    - ylearn
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/DataCanvasIO/YLearn
  license: Apache-2.0
  license_file: LICENSE
  summary: 'YLearn is a python package for causal inference.'
  description: |
    YLearn, a pun of "learn why", is a python package for causal inference 
    which supports various aspects of causal inference ranging from causal 
    effect identification, estimation, and causal graph discovery, etc.
  doc_url: https://ylearn.readthedocs.io/
  dev_url: https://github.com/DataCanvasIO/YLearn
extra:
  recipe-maintainers:
    - lixfz
