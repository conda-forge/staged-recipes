{% set version = "4.9" %}

package:
  name: cgal
  version: {{ version }}

source:
  fn: CGAL-{{ version }}.zip
  url: https://github.com/CGAL/cgal/releases/download/releases%2FCGAL-{{ version }}/CGAL-{{ version }}.zip
  sha256: 34669adf0378e30555df28cc4eef3ef7f46f35c984f4c99a3a16417035dfe083
  patches:
    - cgal-mpir.patch  # [win]
    # https://github.com/CGAL/cgal/pull/1759
    - fix-exponent-type.patch

build:
  number: 1
  features:
    - vc9  # [win and py27]
    - vc10  # [win and py34]
    - vc14  # [win and py35]

requirements:
  build:
    - toolchain
    - python
    - cmake
    - boost 1.62.*
    - mpfr
    - zlib 1.2.*
    - eigen
    - swig
  run:
    - python
    - boost 1.62.*
    - mpfr

test:
  commands:
    - test -f ${PREFIX}/include/CGAL/AABB_tree.h  # [unix]
    - test -f ${PREFIX}/lib/libCGAL.so  # [linux]
    - test -f ${PREFIX}/lib/libCGAL.dylib  # [osx]
    - if not exist %LIBRARY_INC%\CGAL\AABB_tree.h exit 1  # [win]
  imports:
    - CGAL

about:
  home: http://www.cgal.org/
  license: GPL3/LGPL3
  summary: Computational Geometry Algorithms Library

extra:
  recipe-maintainers:
    - xantares
