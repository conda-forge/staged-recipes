Conda-forge supplies libmagic on Windows through python-magic's dependencies,
so the separate PyPI python-magic-bin distribution is unnecessary.
Keep wheel metadata consistent with the conda runtime requirements.

--- a/pyproject.toml
+++ b/pyproject.toml
@@ -23,8 +23,7 @@
     "librosa>=0.11.0",
     "numpy<=2.2",
     "polars>=1.38.0",
-    "python-magic>=0.4.27 ; sys_platform != 'win32'",
-    "python-magic-bin>=0.4.14 ; sys_platform == 'win32'",
+    "python-magic>=0.4.27",
     "scipy>=1.15.3",
     "toml>=0.10.2",
     "tqdm>=4.67.3",
