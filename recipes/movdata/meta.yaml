{% set name = "movdata" %}
{% set version = "0.1.20rc10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/orbit-ml-{{ version }}.tar.gz
  sha256: 4b047278125fdb3a07ddc3b157b07c57a07490e97a5842b02a80556abc49f854

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - python >=3.6
    - pygeos
    - shapely
    - geopandas
    - gdal
    - rasterio
test:
  imports:
    - movdata
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/wildlife-dynamics/movdata
  summary: Movement Ecology Tools for Python
  license: BSD-3-Clause
  license_file: LICENSE
  description: |
    MovData is a Python library that provides tools for analysing movement data
    within the fields of ecology and conservation.

    PyPI: [https://pypi.org/project/movdata/](https://pypi.org/project/movdata/)
  dev_url: https://github.com/wildlife-dynamics/movdata

extra:
  recipe-maintainers:
    - walljcg
    - blac-siren
