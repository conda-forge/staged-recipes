{% set name = "als-finder" %}
{% set version = "1.1.0" %}
{% set python_min = "3.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/cms-2024-hudak/als-finder/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 7b6fdf1764dbca2dec0eb2fb3b2a76c7a5ff08391bf9d185357a74d6d7f40b43

build:
  noarch: python
  number: 0
  entry_points:
    - als-finder=als_finder.cli:cli

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
    - setuptools_scm
  run:
    - python >={{ python_min }}
    - geopandas
    - requests
    - click
    - python-dotenv
    - shapely
    - pyproj
    - tqdm
    - pyogrio
    - pdal
    - python-pdal

test:
  requires:
    - python {{ python_min }}
  imports:
    - als_finder
  commands:
    - als-finder --help

about:
  home: https://github.com/cms-2024-hudak/als-finder
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Cloud-native CLI engine for discovering raw LiDAR point cloud metadata globally.

extra:
  recipe-maintainers:
    - jgrn307
