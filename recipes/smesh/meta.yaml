{% set name = "smesh" %}
{% set version = "8.3.0b0" %}
{% set sha256 = "12b3d140d39cd5bef699f4c5488ce9a25a1d501a7db796de9e56a92401080b58" %}

package:
    name: {{ name }}
    version: {{ version }}

source:
    fn: {{ name }}_{{ version }}.tar.gz
    url: https://github.com/LaughlinResearch/SMESH/archive/8.3.0-b0.tar.gz
    sha256: {{ sha256 }}

build:
    number: 0
    features:
        - vc14  # [win]
    skip: true  # [py<35 or win32]

requirements:
    build:
        - gcc  # [osx]
        - vc 14  # [win]
        - toolchain
        - cmake
        - ninja
        - boost-cpp 1.66*
        - occt 7.2.0
        - netgen
        - vtk 8.*
        # - libmed
        - pthread-stubs  # [unix]
        - pthreads-win32  # [win]

    run:
        - vc 14  # [win]
        - boost-cpp 1.66*
        - occt 7.2.0
        - netgen
        - vtk 8.*
        # - libmed
        - pthread-stubs  # [unix]
        - pthreads-win32  # [win]

test:
    requires:
        - cmake
        - ninja
    files:
        - CMakeLists.txt
    commands:
        - cmake -G "Ninja" .  # [unix]

about:
    home: https://github.com/LaughlinResearch/SMESH
    license: LGPL2.1
    license_family: LGPL
    license_file:  LICENSE.txt
    summary: A complete MESH framework based on the OCCT library.
    description: Standalone version of the meshing library from the Salome Platform.

extra:
    recipe-maintainers:
        - looooo
