{% set name = "scgraph" %}
{% set version = "2.14.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/scgraph-{{ version }}.tar.gz
  sha256: c0460f851ee101a5f06b057cf10f8f5f1408e4ab7ceb9ff01f77c453a8bb8d08

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}

test:
  imports:
    - scgraph
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/connor-makowski/scgraph
  summary: Determine an approximate route between two points on earth
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - vinisalazar
