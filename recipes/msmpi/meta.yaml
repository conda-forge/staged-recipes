{% set version="7.0.12437.6" %}

package:
  name: msmpi
  version: {{ version }}

build:
  skip: True  # [not win]

requirements:
  run:
    - msmpi-bin {{ version }}
    - msmpi-sdk {{ version }}

about:
  summary: metapackage for Microsoft MPI programs and libraries

extra:
  recipe-maintainers:
    - msarahan
