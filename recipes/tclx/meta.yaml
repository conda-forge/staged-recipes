{% set name = "tclx" %}
{% set version = "8.4.4" %}

package:
  name: {{ name|lower }}
  version: v{{ version }}

source:
  # If getting the source from GitHub, remove the line above,
  # uncomment the line below, and modify as needed. Use releases if available:
  url: https://github.com/flightaware/tclx/archive/refs/tags/{{ version }}.tar.gz
  sha256: 44d834d0996e9a7bbaf22d85d9dc17eac2ccc43ab0243a21087e3944c7bc3825

build:
  # Uncomment the following line if the package is pure Python and the recipe is exactly the same for all platforms.
  # It is okay if the dependencies are not built for all platforms/versions, although selectors are still not allowed.
  # See https://conda-forge.org/docs/maintainer/knowledge_base.html#noarch-python for more details.
  # noarch: python
  number: 0

requirements:
  build:
    # If your project compiles code (such as a C extension) then add the required compilers as separate entries here.
    # Compilers are named 'c', 'cxx' and 'fortran'.
    - {{ compiler('c') }}
  host:
    - tk

test:
  commands:
    - echo package require Tclx;exit | tclsh --  # [win]

about:
  # home: https://github.com/simplejson/simplejson
  # Remember to specify the license variants for BSD, Apache, GPL, and LGPL.
  # Use the SPDX identifier, e.g: GPL-2.0-only instead of GNU General Public License version 2.0
  # See https://spdx.org/licenses/
  # license: MIT
  # The license_family, i.e. "BSD" if license is "BSD-3-Clause". (optional)
  #license_family: MIT
  # It is strongly encouraged to include a license file in the package,
  # (even if the license doesn't require it) using the license_file entry.
  # See https://docs.conda.io/projects/conda-build/en/latest/resources/define-metadata.html#license-file
  # license_file: LICENSE.txt
  # summary: 'Simple, fast, extensible JSON encoder/decoder for Python'
  # The remaining entries in this section are optional, but recommended.
  # description: |
  #  simplejson is a simple, fast, complete, correct and extensible
  #   JSON <https://json.org> encoder and decoder for Python 2.5+ and
  #   Python 3.3+. It is pure Python code with no dependencies, but includes
  #   an optional C extension for a serious speed boost.
  # doc_url: https://simplejson.readthedocs.io/
  # dev_url: https://github.com/simplejson/simplejson

extra:
  recipe-maintainers:
    - nandandevinagara
