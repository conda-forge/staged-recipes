{% set name = "create-forwarder-dll" %}
{% set version = "0.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/create_forwarder_dll-{{ version }}.tar.gz
  sha256: 18e999625b3ee208e8f30f452f95934bbbcc1103088b3036fe5011a173f5a5f4

build:
  entry_points:
    - create-forwarder-dll = create_forwarder_dll:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=65
    - setuptools-scm >=8.1
    - pip
  run:
    - python >={{ python_min }}

test:
  imports:
    - create_forwarder_dll
  commands:
    - pip check
    - create-forwarder-dll --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/isuruf/create-forwarder-dll
  summary: Creates a DLL that forwards all the symbols to another DLL
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - isuruf
