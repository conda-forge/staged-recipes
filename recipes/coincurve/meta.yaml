{% set name = "coincurve" %}
{% set version = "19.0.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/MementoRC/coincurve/archive/refs/tags/pre-v{{ version }}.tar.gz
  sha256: e7e1af71665e87c2e74b9bde58c555859498db427fb26095e08833d110155c10
  patches:
    # - dbg_to_be_removed.patch

build:
  number: 0
  script:  {{ PYTHON }} -m pip install . --use-pep517 -vvv

requirements:
  build:
    - {{ compiler('c') }}
    - pkg-config  # [not win]
  host:
    - cffi >=1.3.0
    - libsecp256k1
    - requests
    - pip
    - python
  run:
    - asn1crypto
    - {{ pin_compatible('cffi') }}
    - python

test:
  imports:
    - coincurve
  source_files:
    - tests
  requires:
    - pip
    - pytest
    - pytest-benchmark
  commands:
    - pip check
    - pytest -v tests

about:
  home: https://github.com/ofek/coincurve
  summary: Cross-platform Python CFFI bindings for libsecp256k1
  doc_url: https://ofek.dev/coincurve/
  description: |
    This library provides well-tested Python bindings for libsecp256k1,
    the heavily optimized C library used by Bitcoin Core for operations
    on the elliptic curve secp256k1.
  license: MIT OR Apache-2.0
  license_file:
    - LICENSE-MIT
    - LICENSE-APACHE

extra:
  recipe-maintainers:
    - ofek
    - MementoRC
