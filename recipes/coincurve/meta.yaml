{% set name = "coincurve" %}
{% set version = "19.0.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://github.com/MementoRC/coincurve/archive/refs/tags/pre-v{{ version }}.tar.gz
  sha256: 8a88bf2eeb5bcaa5e3eb6c59508642421311d27e6802766b659378ba17b5c57c

build:
  number: 0
  script:  {{ PYTHON }} -m pip install . --use-pep517 -vvv

requirements:
  build:
    - {{ compiler('c') }}
    - pkg-config  # [not win]
  host:
    - cffi >=1.3.0
    - libsecp256k1
    - requests
    - pip
    - pkgconfig
    - python
  run:
    - asn1crypto
    - {{ pin_compatible('cffi') }}
    - python

test:
  imports:
    - coincurve
  source_files:
    - tests
  requires:
    - pip
    - pytest
    - pytest-benchmark
  commands:
    - pip check
    - pytest -v tests

about:
  home: https://github.com/ofek/coincurve
  summary: Cross-platform Python CFFI bindings for libsecp256k1
  doc_url: https://ofek.dev/coincurve/
  description: |
    This library provides well-tested Python bindings for libsecp256k1,
    the heavily optimized C library used by Bitcoin Core for operations
    on the elliptic curve secp256k1.
  license: MIT OR Apache-2.0
  license_file:
    - LICENSE-MIT
    - LICENSE-APACHE

extra:
  recipe-maintainers:
    - ofek
    - MementoRC
