{% set name = "articdb" %}
{% set version = "1.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  # This is the last revision of `experimental/conda-forge-release`
  # url: https://github.com/man-group/ArcticDB/archive/5c7ef369e23c5c168548888e9a8bdc2a9dbd6db1.tar.gz
  # sha256: e130588a84ad6e271d12b28fb9f0da64638f128a6d53c02f98f0c31ffcf297d8
  git_url:  https://github.com/man-group/ArcticDB
  git_rev:  experimental/conda-forge-release

build:
  # We skip the build on Windows because one of the dependencies (folly)
  # is not available for Windows on conda-forge
  # See: https://github.com/conda-forge/folly-feedstock/pull/98
  skip: true  # [win]
  script: ARCTICDB_USING_CONDA=1 {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ compiler('c') }}
    - cmake
    - make

  host:
    - python
    - pip
    - folly
    - lz4-c
    - prometheus-cpp
    - double-conversion
    - libevent
    - libprotobuf <4
    - libmongocxx
    - zstd
    - pybind11
    - pcre
    - gflags
    - cyrus-sasl =2.1.27
    - msgpack-c
    - aws-sdk-cpp
    - bitmagic
    - krb5 =1.20.1
    - xxhash
    - semimap
    - grpcio-tools
    - protobuf
    - spdlog
    - robin_hood
    - recycle
    - lmdb
    - lmdbxx
    - remotery
  run:
    - python
    - numpy
    - pandas
    - enum-compat
    - python-attr
    - six
    - psutil
    - pyyaml
    - decorator
    - prometheus_client

test:
  imports:
    - arcticdb

about:
  home: https://arcticdb.io/
  summary: "ArcticDB is a high performance, serverless DataFrame database built for the Python Data Science ecosystem."
  description: |
    "**ArcticDB** is a high performance, serverless **DataFrame database** built for
    the Python Data Science ecosystem. Launched in March 2023, it is the successor
    to [Arctic](https://github.com/man-group/arctic).
    ArcticDB offers an intuitive Python-centric API enabling you to read and write
    Pandas DataFrames to S3 or LMDB utilising a fast C++ data-processing and
    compression engine."
  license: BUSL-1.1
  license_family: OTHER
  license_file: LICENSE.txt
  doc_url: https://docs.arcticdb.io
  dev_url: https://github.com/man-group/ArcticDB

extra:
  recipe-maintainers:
    - DerThorsten
    - jjerphan
