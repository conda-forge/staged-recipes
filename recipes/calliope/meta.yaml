{% set name = "calliope" %}
{% set version = "0.4.0_rc1" %}
{% set sha256 = "35d929bac53a599f9efa7252bd1b778277032ce5aad8875ba56d9776a68b03bf" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: calliope-3a7bbb42ad3cb2478adb57c3794078dd709e1c1e.zip
  url: https://github.com/calliope-project/calliope/archive/3a7bbb42ad3cb2478adb57c3794078dd709e1c1e.zip
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  skip: True  # [py<35]
  entry_points:
    - {{ name }} = {{ name }}.cli:cli

requirements:
  build:
    - python
    - setuptools
  run:
    - python >=3.5
    - basemap
    - bottleneck
    - click >=3.3
    - descartes
    - glpk ==4.59
    - hdf5
    - libnetcdf
    - matplotlib >=1.5
    - netcdf4 >=1.2.2
    - networkx
    - numexpr >=2.3.1
    - numpy >=1.11
    - pandas >=0.19.0,<0.20
    - pyomo >=4.4,<4.5
    - pytest
    - pytest-cov
    - pyyaml >=3.11
    - scipy
    - seaborn
    - shapely
    - xarray >=0.8.2

test:
  imports:
    - calliope
    - calliope.analysis
  commands:
    - calliope --help

about:
  home: https://www.callio.pe/
  license: Apache-2.0
  license_file: LICENSE
  summary: A multi-scale energy systems (MUSES) modeling framework
  description: |
    Calliope is a framework to develop energy system models, with a
    focus on flexibility, high spatial and temporal resolution, the
    ability to execute many runs based on the same base model, and
    a clear separation of framework (code) and model (data).
  doc_url: https://calliope.readthedocs.io/
  dev_url: https://github.com/calliope-project/calliope

extra:
  recipe-maintainers:
    - sjpfenninger
