{% set name = "calliope" %}
{% set version = "0.4.0_rc1" %}
{% set sha256 = "22a2bd2392a26c93fa69887646be2b5fef3a1bd25467aa8626b3c59af4be09a9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: calliope-1a2ae509f9fd19d032401af47745db86a005d411.zip
  url: https://github.com/calliope-project/calliope/archive/1a2ae509f9fd19d032401af47745db86a005d411.zip
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  skip: True  # [py<35]
  entry_points:
    - {{ name }} = {{ name }}.cli:cli

requirements:
  build:
    - python
    - setuptools
  run:
    - python >=3.5
    - basemap
    - bottleneck
    - click >=3.3
    - descartes
    - glpk ==4.59
    - hdf5
    - libnetcdf
    - matplotlib >=1.5
    - netcdf4 >=1.2.2
    - networkx
    - numexpr >=2.3.1
    - numpy >=1.11
    - pandas >=0.19.0,<0.20
    - pyomo >=4.4,<4.5
    - pytest
    - pytest-cov
    - pyyaml >=3.11
    - scipy
    - seaborn
    - shapely
    - xarray >=0.8.2

test:
  imports:
    - calliope
    - calliope.analysis
  commands:
    - calliope --help

about:
  home: https://www.callio.pe/
  license: Apache-2.0
  license_file: LICENSE
  summary: A multi-scale energy systems (MUSES) modeling framework
  description: |
    Calliope is a framework to develop energy system models, with a
    focus on flexibility, high spatial and temporal resolution, the
    ability to execute many runs based on the same base model, and
    a clear separation of framework (code) and model (data).
  doc_url: https://calliope.readthedocs.io/
  dev_url: https://github.com/calliope-project/calliope

extra:
  recipe-maintainers:
    - sjpfenninger
