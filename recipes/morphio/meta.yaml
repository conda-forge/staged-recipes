{% set name = "morphio" %}
{% set version = "3.3.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/MorphIO-{{ version }}.tar.gz
  sha256: 0f2e55470d92a3d89f2141ae905ee104fd16257b93dafb90682d90171de2f4e6

build:
  skip: true  # [py<37]
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - ninja
  host:
    - python
    - hdf5
    - h5py  # [win]
    - pip
    - setuptools >=24.2.0
    - setuptools-scm
    - wheel
  run:
    - python
    - numpy >=1.14.1
    - h5py  # [win]

test:
  imports:
    - morphio
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/BlueBrain/MorphIO/
  summary: A neuron morphology IO library
  license: MIT AND LGPL-3.0 AND BSD-3-Clause AND GPL-3.0
  license_file:
    - COPYING.LESSER
    - COPYING
    - 3rdparty/ghc_filesystem/LICENSE

extra:
  recipe-maintainers:
    - willGraham01
