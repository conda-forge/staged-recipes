{% set name = "openeo" %}
{% set version = "0.8.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/Open-EO/openeo-python-client/archive/refs/tags/v{{ version }}.tar.gz
  sha256: c75a0907182563b37370c8f067367cd24d661c5e7a58a66763204b781fb9f988

build:
  number: 0
  #skip: True  # [not win]" to limit to Windows.
  skip: True  # [py<35]
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python
    - pip
    - setuptools
    - numpy >=1.17.0
  run:
    - python
    - deprecated >=1.2.12
    - {{ pin_compatible('numpy') }}
    - pandas >0.20.0
    - requests
    - shapely >=1.6.4
    - xarray >=0.12.3

test:
  imports:
    - openeo
  requires:
    - pip
    - pytest-runner
    - matplotlib-base
    - mock
    - requests-mock
  source_files:
    - tests
  commands:
    - pip check
    - pytest

about:
  home: https://github.com/Open-EO/openeo-python-client
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: 'Python Client Library for the openEO API. Allows you to interact with openEO backends from your own (local) Python environment.'

extra:
  recipe-maintainers:
    - sebastian-luna-valero
    - soxofaan
    - jdries
