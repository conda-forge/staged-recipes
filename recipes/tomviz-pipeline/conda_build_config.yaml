# Hold uploads on a label until the tomviz release that uses the new
# tomviz_pipeline module ships: a default install of tomviz-pipeline
# must keep resolving to the old layout (<=3.0.0, from the tomviz
# feedstock) until then. Flip to "conda-forge main" at release.
channel_targets:
  - conda-forge tomviz_pipeline_dev
