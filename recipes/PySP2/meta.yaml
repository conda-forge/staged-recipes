{% set name = "pysp2" %}
{% set version = "1.0.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pysp2-{{ version }}.tar.gz
  sha256: 5379caf42a9af3da1b9c306cd1ba8664a4d2588ebd7da30a528a6a6dc6944811

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - matplotlib-base
    - numpy
    - pandas
    - python
    - scipy
    - xarray
    - xlrd
    - act-atmos
    - numba

test:
  imports:
    - pysp2
  commands:
    - pytest 
  requires:
    - pip
    - pytest
  source_files:
    - tests/*.py

about:
  home: https://pypi.org/project/pysp2/
  summary: Py-SP2
  dev_url: https://github.com/rcjackson/PySP2
  license: BSD-2-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rcjackson
