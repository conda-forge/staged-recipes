# rattler-build automatically discovers and includes variant configurations from either:
#   - variants.yaml file located next to the recipe
#   - conda_build_config.yaml file located next to the recipe
# To specify variant configurations from other locations (..\config) use the --variant-config:
#   - rattler-build build --variant-config ..\config\variant_config.yaml --recipe recipe.yaml
c_compiler:
  - vs2022

cxx_compiler:
  - vs2022

c_stdlib:
  - vs

qt_main:
  - 5.15.*