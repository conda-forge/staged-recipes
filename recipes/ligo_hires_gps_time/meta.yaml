{% set name = "ligo_hires_gps_time" %}
{% set version = "0.5.2" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://git.ligo.org/cds/software/cr_tools/ligo_hires_gps_time/-/archive/{{ version }}/{{ version }}.tar.gz
  sha256: 814fdbb4b07937dec18e789f0fa92afc050539c0378ef61867ba6761f2812778 

build:
  skip: true  # [win or osx]
  error_overlinking: true
  error_overdepending: true
  number: 0

requirements:
  build:
   - python
   - {{ compiler('rust') }} 
   - {{ compiler('c') }}
   - {{ stdlib("c") }}

  host:
   - python
   - pip  
   - ruff
   - maturin

  run:
   - python

test:
  requires:
    - pytest
    - pip
  source_files:
    - test_pip.py   
    - test_thump.py
  commands:
    - pytest
    - pip check

about:
  home: https://git.ligo.org/cds/software/cr_tools/ligo_hires_gps_time
  dev_url: https://git.ligo.org/cds/software/cr_tools/ligo_hires_gps_time.git
  license: GPL-3.0-or-later
  license_family: GPL
  license_file:
    - COPYING
    - COPYING-GPL-3
  summary: Library for storing and manipulating hiresolution GPS time stamps for LIGO data streams.

extra:
  recipe-maintainers:
    - evonreis
