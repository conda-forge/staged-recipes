{% set name = "thatool" %}
{% set version = "1.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 4c738c36c4d4eb94da50287641bfd9b5f4b8f09aaa6499f5f61bd6a8736d2e18

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - pip
    - python
  run:
    - python
    - shapely
    - lmfit


test:
  imports:
    # - thatool
    # - thatool.filetool
    # - thatool.free_energy_cal
    # - thatool.modeling
    # - thatool.parameter
    # - thatool.utils

about:
  home: The package home page
  license: MIT
  license_family: MIT
  license_file: 
  summary: "TYPE YOUR DESCRIPTION HERE"
  doc_url: 
  dev_url: 

extra:
  recipe-maintainers:
    - thangckt
