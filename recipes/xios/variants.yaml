# staged-recipes' base configs (.ci_support/*.yaml) define no `mpi` key, so the
# recipe has to supply one. mpich is the flavour this recipe was built and
# tested against; openmpi can be added on the feedstock later.
mpi:
  - mpich
