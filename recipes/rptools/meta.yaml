{% set name = "rptools" %}
{% set version = "5.7.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  path: ..
  url: https://github.com/brsynth/{{ name }}/archive/{{ version }}.tar.gz
  sha256: 8e156525b86b070fd81e084ad6c39e90d9e1739aac3d587305979ba5b3b43aa6

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install -vv .

requirements:
  host:
    - python >3.6
    - pip
  run:
    - python >3.6
    - brs_utils
    - requests
    - urllib3
    - cobra
    - timeout-decorator
    - rdkit
    - python-libsbml
    - numpy
    - pandas
    - scipy
    - equilibrator-api
    - equilibrator-pathway
    - openbabel
    - colored
    - networkx >=2.5
    - filetype
    - rr_cache
    - rxn_rebuild
    - chemlite
    - h5py
    - xgboost
    - beautifulsoup4

test:
  source_files:
    - tests
  imports:
    - {{ name|lower }}
  requires:
    - pytest
    - pytest-cov
    - pytest-mock
  commands:
    - python -m {{ name|lower }} --help
    - cd tests
    - pytest -v --cov --cov-report term-missing

about:
  home: https://github.com/brsynth/{{ name|lower }}
  summary: "Suite of tools that work on rpSBML data"
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - breakthewall
    - kenza12

