{% set name = "pylexique" %}
{% set version = "1.0.3" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pylexique-{{ version }}.tar.gz
  sha256: d804fe34f9fe1b29a236e5d6901154e2a1ac0a3d0ad3925499f685881356fd37

build:
  number: 0
  entry_points:
    - pylexique=pylexique.cli:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - pip
    - pytest-runner
    - python
    - python-blosc
    - hdf5
    - pytables
  run:
    - click >=7.1
    - cython
    - python-blosc
    - hdf5
    - numexpr
    - numpy
    - pytables
    - python

test:
  imports:
    - pylexique
    - python-blosc
    - hdf5
    - pytable
  commands:
    - pip check
    - pylexique --help
  requires:
    - pip

about:
  description: |+
    Pylexique is a Python wrapper around Lexique83.
    It allows to extract lexical information from more than 140 000 French words in an Object Oriented way.
  home: https://github.com/SekouDiaoNlp/pylexique
  summary: Pylexique is a Python wrapper around Lexique83
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - SekouDiaoNlp
