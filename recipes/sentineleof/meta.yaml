{% set name = "sentineleof" %}
{% set version = "0.3.5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: c36c7fcaab29a445cdf3562db5bc02484724cd3d3bd167a9e133617a082939ee

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - python
    - pip
  run:
    - python
    - requests
    - python-dateutil
    - click
  entry_points:
    - eof = eof.cli:cli

test:
  imports:
    - eof
  requires:
    - pytest
  commands:
    - eof --help
    - "{{ PYTHON }} -m pytest -v eof"

about:
  home: https://github.com/scottstanie/sentineleof
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: 'Precise orbit file download for Sentinel 1'
  description: |
    Tool to download Sentinel 1 precise/restituted orbit files (.EOF files)
    for processing SLCs
  dev_url: https://github.com/scottstanie/sentineleof

extra:
  recipe-maintainers:
    - avalentino
    - scottstanie
