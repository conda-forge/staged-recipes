{% set name = "conda-auth" %}
{% set version = "0.1.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/conda-incubator/{{ name }}/archive/refs/tags/{{ version}}.tar.gz
  sha256: 9ea9427fcb850d471086b1774a4051ef99df2d165cfd1b2948dc2ca6f00a1f83

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8,<4.0
    - pip
  run:
    - python >=3.8.1,<4.0.0
    - click
    - conda >=23.9.0
    - keyring
    - requests
    - ruamel.yaml

test:
  imports:
    - conda_auth
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/conda-incubator/conda-auth
  summary: Conda plugin for improved access to private channels
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - travishathaway
    - jezdez
