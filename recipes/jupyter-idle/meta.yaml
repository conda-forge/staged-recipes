{% set name = "jupyter-idle" %}
{% set version = "0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/jupyter_idle-{{ version }}.tar.gz
  sha256: d6488ff8d14212514d8cd7c1d3a9501fbd76aebedc119f020a69badb0825d75f

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.9
    - pip
  run:
    - python >=3.9
    - jupyter_server >=2.0.0
    - tornado

test:
  imports:
    - jupyter_idle
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://aws.amazon.com/sagemaker/
  summary: Jupyter Server Extension for checking last activity time
  license: LicenseRef-.amazon.com.-AmznSL-1.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - rahrad123
