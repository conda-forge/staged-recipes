--- a/python/CMakeLists.txt
+++ b/python/CMakeLists.txt
@@ -20,7 +20,13 @@ if (DEFINED SKBUILD)
     message(STATUS "Building with Scikit-Build")
 endif ()

-if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/../cpp/)
+option(USE_SYSTEM_PATCHWORKPP "Use system-installed patchworkpp instead of bundled source" OFF)
+
+if(USE_SYSTEM_PATCHWORKPP)
+  find_package(Eigen3 REQUIRED)
+  find_package(patchworkpp REQUIRED)
+elseif(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/../cpp/)
   add_subdirectory(${CMAKE_CURRENT_SOURCE_DIR}/../cpp ${CMAKE_CURRENT_BINARY_DIR}/patchworkpp_cpp)
 else()
   cmake_minimum_required(VERSION 3.18)
