context:
  name: fsspec-data
  version: "0.2.3"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/f/fsspec-data/fsspec_data-${{ version }}.tar.gz
  sha256: 07ca6e78b672fd59c36fa41ef3a8d234070439c14cc0ce768763d325963eb439

build:
  number: 0
  python:
    version_independent: true
  script:
    - if: unix
      then:
        - export PYO3_PYTHON="$PYTHON"
        - export RUSTFLAGS=""
      else:
        - set "PYO3_PYTHON=%PYTHON%"
        - set "RUSTFLAGS="
    - cargo-bundle-licenses --format yaml --output THIRDPARTY.yml
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  skip: (is_abi3 and not is_python_min) or target_platform == "osx-arm64" or target_platform == "linux-aarch64"

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - crossenv
    - ${{ compiler("c") }}
    - ${{ stdlib("c") }}
    - ${{ compiler("rust") }}
    - cargo-bundle-licenses
  host:
    - python
    - pip
    - hatchling
    - hatch-rs >=0.2.1
    - if: is_abi3
      then:
        - python-abi3
  run:
    - python
    - fsspec
    - fsspec-rs >=0.1.2
    - pyarrow
  ignore_run_exports:
    from_package:
      - cross-python_${{ target_platform }}

tests:
  - python:
      imports:
        - fsspec_data
      pip_check: true
      python_version:
        - ${{ python_min }}.*
        - "*"
  - if: is_abi3
    then:
      script:
        - if: win
          then: abi3audit %PREFIX%/Lib/site-packages/fsspec_data/fsspec_data.pyd -s -v --assume-minimum-abi3 ${{ python_min }}
          else:
            - export SP_DIR=$(python -c "import site; print(site.getsitepackages()[0])")
            - abi3audit $SP_DIR/fsspec_data/fsspec_data.abi3.so -s -v --assume-minimum-abi3 ${{ python_min }}
      requirements:
        run:
          - abi3audit

about:
  homepage: https://github.com/1kbgz/fsspec-data
  repository: https://github.com/1kbgz/fsspec-data
  summary: Python Filesystem interface for non-filesystem data sources
  license: Apache-2.0
  license_file:
    - LICENSE
    - THIRDPARTY.yml

extra:
  recipe-maintainers:
    - timkpaine
