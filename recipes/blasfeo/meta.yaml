{% set name = "libblasfeo" %}
{% set version = "0.1.3.999" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  - url: https://github.com/giaf/blasfeo/archive/b6bc34c6cb1995aea60968403016110bd3288e3b.tar.gz
    sha256: a35f61d33e99e4b132bc36c9d1e64449aa6cd37985132b2b24800a65261ffb1f

build:
  number: 0
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x.x') }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - pkg-config
    - ninja

test:
  commands:
    - test -f $PREFIX/include/blasfeo.h  # [unix]
    - test -f $PREFIX/lib/libblasfeo${SHLIB_EXT}  # [unix]
    - test -f $PREFIX/cmake/blasfeoConfigconfig.cmake   # [unix]
    - if not exist %PREFIX%\\Library\\cmake\\blasfeoConfigconfig.cmake exit 1  # [win]
    - if not exist %LIBRARY_INC%\\blasfeo.h exit 1  # [win]
    - if not exist %LIBRARY_LIB%\\blasfeo.lib exit 1  # [win]
    - if not exist %LIBRARY_BIN%\\blasfeo.dll exit 1  # [win]

about:
  home: https://github.com/giaf/blasfeo
  license: MIT
  license_file: LICENSE
  summary: Basic linear algebra subroutines for embedded optimization

extra:
  feedstock-name: blasfeo
  recipe-maintainers:
    - traversaro
