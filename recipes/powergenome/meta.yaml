{% set name = "powergenome" %}
{% set version = "0.6.0a0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/powergenome-{{ version }}.tar.gz
  sha256: 8ed4a42ea707a0ca439628e83af27f830da15a05aafa05a27aa3a63641ea5626

build:
  entry_points:
    - run_powergenome_multiple = powergenome.run_powergenome_multiple_outputs_cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.9,<3.12
    - pip
  run:
    - python >=3.9,<3.12
    - catalystcoop.pudl >=0.6.0,<=2022.11.30
    - beautifulsoup4
    - statsmodels
    - python-dotenv
    - flatten-dict
    - ruamel.yaml
    - pyyaml
    - frozendict
    - openpyxl >=3.0
    - geopandas
    - descartes

test:
  imports:
    - powergenome
  commands:
    - pip check
    - run_powergenome_multiple --help
  requires:
    - pip

about:
  home: https://github.com/PowerGenome/PowerGenome
  summary: Construct input data for use in capacity expansion models
  license: MIT
  license_file: LICENSE.md

extra:
  recipe-maintainers:
    - gschivley
