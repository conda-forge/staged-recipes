{% set name = "pandas_plink" %}
{% set version = "1.0.6rc8" %}
{% set sha256 = "c50b49a0167c1fb12d40650b9ace6f16e92ea192f9cc2988acbccd4fc23a7d6d" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  features:
    - vc14             # [win]
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - pytest-runner
    - cffi
    - toolchain
    - vc 14           # [win]
    - numpy
    - libffi >=3.0.0
  run:
    - python
    - pandas
    - cffi
    - tqdm
    - libffi >=3.0.0

test:
  requires:
    - pytest
    - pandas
    - numpy
    - cffi
    - libffi >=3.0.0
  imports:
    - pandas_plink
  commands:
    - python -c "import pandas_plink; pandas_plink.test()"

about:
  home: https://github.com/Horta/pandas-plink
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Convert PLINK files to Pandas data frame'
  doc_url: http://pandas-plink.readthedocs.io
  dev_url: https://github.com/Horta/pandas-plink

extra:
  recipe-maintainers:
    - Horta
