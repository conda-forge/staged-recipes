{% set name = "pandas_plink" %}
{% set version = "1.0.6rc9" %}
{% set sha256 = "d7f61aa248ed6c4128f0c5833f4d16d26d49e1a761a4fe965b400e85db31596c" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  features:
    - vc14             # [win]
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - pytest-runner
    - cffi
    - toolchain
    - vc 14           # [win]
    - numpy
    - libffi
    - pandas
    - tqdm
  run:
    - python
    - pandas
    - cffi
    - tqdm
    - libffi

test:
  requires:
    - pytest
    - pandas
    - numpy
    - cffi
    - libffi
  imports:
    - pandas_plink
    - pytest
    - pandas
    - numpy
    - cffi
  commands:
    - python -c "import pandas_plink; pandas_plink.test()"

about:
  home: https://github.com/Horta/pandas-plink
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Convert PLINK files to Pandas data frame'
  doc_url: http://pandas-plink.readthedocs.io
  dev_url: https://github.com/Horta/pandas-plink

extra:
  recipe-maintainers:
    - Horta
