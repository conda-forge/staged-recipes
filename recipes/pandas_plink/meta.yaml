{% set name = "pandas_plink" %}
{% set version = "1.0.6" %}
{% set sha256 = "34e1c176b80fa213e8dfe6b17dc9e63f6987873b02eac392566adfc953623669" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  features:
    - vc14             # [win]
  script: python setup.py install --record record.txt

requirements:
  build:
    - toolchain
    - vc 14           # [win]
    - python
    - setuptools
    - pytest-runner
    - cffi
    - libffi
    - numpy
    - pandas
    - tqdm
  run:
    - python
    - numpy
    - pandas
    - cffi
    - libffi
    - tqdm

test:
  requires:
    - pytest
  imports:
    - pandas_plink
  commands:
    - python -c "import pandas_plink; pandas_plink.test()"

about:
  home: https://github.com/Horta/pandas-plink
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Convert PLINK files to Pandas data frame'
  doc_url: http://pandas-plink.readthedocs.io
  dev_url: https://github.com/Horta/pandas-plink

extra:
  recipe-maintainers:
    - Horta
