{% set name = "pandas_plink" %}
{% set version = "1.0.13" %}
{% set sha256 = "7275e8ad5936a85a264c7231ac9f51cd65eb55da" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: true  # [win or not py27]
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - toolchain
    - cffi >=1.7
    - libffi
    - pytest-runner
  run:
    - python
    - cffi >=1.7
    - numpy
    - pandas
    - tqdm

test:
  requires:
    - pytest
  imports:
    - pandas_plink
  commands:
    - python -c "import pandas_plink; pandas_plink.test()"

about:
  home: http://github.com/Horta/pandas-plink
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Convert PLINK files to Pandas data frame'
  doc_url: http://pandas-plink.readthedocs.io
  dev_url: https://github.com/Horta/pandas-plink

extra:
  recipe-maintainers:
    - Horta
