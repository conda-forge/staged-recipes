{% set name = "pandas_plink" %}
{% set version = "1.0.8" %}
{% set sha256 = "2773e9d00d446adfc1c6c3ee38edd6933009958eaafb6009657cafa9bd1cc056" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  features:
    - vc14             # [win]
  script: python setup.py install --record record.txt

requirements:
  build:
    - toolchain
    - vc 14           # [win]
    - python
    - setuptools
    - pytest-runner
    - cffi
    - libffi
    - numpy
    - pandas >=0.18
    - tqdm
  run:
    - python
    - numpy
    - pandas >=0.18
    - cffi
    - libffi
    - tqdm

test:
  requires:
    - pytest
  imports:
    - pandas_plink
  commands:
    - python -c "import pandas_plink; pandas_plink.test()"

about:
  home: https://github.com/Horta/pandas-plink
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Convert PLINK files to Pandas data frame'
  doc_url: http://pandas-plink.readthedocs.io
  dev_url: https://github.com/Horta/pandas-plink

extra:
  recipe-maintainers:
    - Horta
