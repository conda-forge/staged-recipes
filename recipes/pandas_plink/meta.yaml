{% set name = "pandas_plink" %}
{% set version = "1.0.10" %}
{% set sha256 = "bddef47c410892c1bad947bb9b87a6aac214217ce6da17b9e44c881d882bc22a" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - python
    - setuptools
    - cffi >=1.7
    - pytest-runner

  run:
    - python
    - cffi >=1.7
    - numpy
    - pandas
    - tqdm

test:
  requires:
    - pytest
  imports:
    - pandas_plink
  commands:
    - python -c "import pandas_plink; pandas_plink.test()"

about:
  home: https://github.com/Horta/pandas-plink
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Convert PLINK files to Pandas data frame'
  doc_url: http://pandas-plink.readthedocs.io
  dev_url: https://github.com/Horta/pandas-plink

extra:
  recipe-maintainers:
    - Horta
