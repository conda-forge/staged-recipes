{% set name = "openmmgridforce" %}
{% set version = "0.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/jimtufts/openmmgridforce/archive/refs/tags/{{ version }}.tar.gz
  sha256: 7f6a6b6bfae17df5b6a1bd79988017bdc6948c3e8066334bd5865a9a405cd4c1 

build:
  number: 0
  skip: True  # [win or osx]

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
    - swig =3.0.12
    - make  # [unix]
    - {{ stdlib('c') }}
  host:
    - python
    - numpy
    - openmm >=8.0
    - setuptools
  run:
    - python
    - {{ pin_compatible('numpy') }}
    - {{ pin_compatible('openmm', max_pin='x.x') }}

test:
  imports:
    - gridforceplugin
  requires:
    - pip
  commands:
    - pip check

about:
  home: https://github.com/jimtufts/openmmgridforce
  summary: 'OpenMM plugin for grid-based forces'
  description: |
    GridForce plugin for OpenMM that enables grid-based force calculations.
  license: BSD-2-Clause
  license_family: BSD
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jimtufts
