{%set name = "ebooklib" %}
{%set camelName = "EbookLib" %}
{%set version = "0.15" %}
{%set hash_type = "sha256" %}
{%set hash_val = "cba18893047b27cdc516ba61e05b76da5f087a3c3dd69c3121dda415fd3e9be1" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ camelName }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ camelName }}/{{ camelName }}-{{ version }}.tar.gz
  {{ hash_type }}: {{ hash_val }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record=record.txt

requirements:
  build:
    - python
    - setuptools
    - six
    - lxml

  run:
    - python
    - lxml
    - six

test:
  imports:
    - ebooklib
    - ebooklib.plugins

about:
  home: https://github.com/aerkalov/ebooklib
  license: AGPL
  summary: 'Ebook library which can handle EPUB2/EPUB3 and Kindle format'

extra:
  recipe-maintainers:
    - pmlandwehr
