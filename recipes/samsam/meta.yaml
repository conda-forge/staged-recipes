{% set name = "samsam" %}
{% set version = "1.0.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/samsam-{{ version }}.tar.gz
  sha256: 85a59aa3cb45d2853a892aece356e6d5033d8df01c592ab349ca62663c896883

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - numpy >=1.16
    - python >=3.6
    - scipy >=1.2

test:
  imports:
    - samsam
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://gitlab.unige.ch/jean-baptiste.delisle/samsam
  summary: Scaled Adaptive Metropolis.
  license: GPL-3.0
  license_file: COPYING

extra:
  recipe-maintainers:
    - jbdelisle
