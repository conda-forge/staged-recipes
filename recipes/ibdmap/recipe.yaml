# This example shows how to define a recipe using the new YAML based recipe format introduced by
# CEP 13.

# For more information about this format see: https://prefix-dev.github.io/rattler-build/latest/reference/recipe_file/

# The main differences with the old format is that no preprocessing is required for the file to be valid YAML.
# This means:
# - No "selectors", use YAML if-then-else expressions instead (https://prefix-dev.github.io/rattler-build/latest/selectors/)
# - Jinja expressions are formatted with a leading `$` to make them valid YAML
#
# See https://conda-forge.org/docs/maintainer/example_recipes for a collection of example recipes.

# Note: there are many handy hints in comments in this example -- remove them when you've finalized your recipe

# Define variables in this section that you can use in other parts.
context:
  version: "deab871d3605ec5c265ac064dbf077ffeb27b557"

package:
  name: ibdmap
  version: ${{ version }}

source:
  url: https://github.com/rjbohlender/IBDMap/archive/${{ version }}.tar.gz
  sha256: f8be538044ba2a746655f5032e3fe0670c4ac8e30307dc8d2b8812c1cce887fd

build:
  # Uncomment the following line if the package is pure Python and the recipe is exactly the same for all platforms.
  # It is okay if the dependencies are not built for all platforms/versions, although selectors are still not allowed.
  # See https://conda-forge.org/docs/maintainer/knowledge_base.html#noarch-python for more details.
  # noarch: python
  # If the installation is complex, or different between Unix and Windows, use separate build.bat and build.sh files instead of this key.
  # By default, the package will be built for the Python versions supported by conda-forge and for all major OSs.
  # Uncomment the following lines to limit to Python 3.5 and newer (for example)
  # skip:
  #   - match(python, "<3.5")
  # or the following to limit to Windows.
  # skip:
  #   - win
  number: 0

requirements:
  build:
    # If your project compiles code (such as a C extension) then add the required compilers as separate entries here.
    # Compiler names include 'c', 'cxx' and 'fortran', among others.
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}  # If you need any compiler, add the C standard library ("stdlib") too
    - cmake
    - ninja
  host:
    - armadillo
    - libarrow
    - libblas
    - libparquet
    - libboost-python-devel
    - libboost-devel

tests:
  # Some packages might need a `test/commands` key to check CLI.
  - script:
      - ibdmap --help

about:
  homepage: https://github.com/rjbohlender/IBDMap
  summary: 'Identity by Descent association mapping'
  description: |
    IBDMap is a multithreaded scalable application for IBD (Identity By Descent) 
    mapping of binary traits. IBDMap implements a test statistic developed by 
    Browning & Thompson et al., 2012 [cite] that detects regions where IBD sharing 
    is statistically enriched among case-case pairs than among case-control pairs.

  # Remember to specify the license variants for BSD, Apache, GPL, and LGPL.
  # Use the SPDX identifier, e.g: GPL-2.0-only instead of GNU General Public License version 2.0
  # See https://spdx.org/licenses/
  license: Apache-2.0
  # It is required to include a license file in the package,
  # (even if the license doesn't require it) using the license_file entry.
  # Please also note that some projects have multiple license files which all need to be added using a valid yaml list.
  # See https://docs.conda.io/projects/conda-build/en/latest/resources/define-metadata.html#license-file
  license_file: LICENSE
  # The documentation and repository URLs are optional.
  documentation: https://github.com/rjbohlender/IBDMap
  repository: https://github.com/rjbohlender/IBDMap

extra:
  recipe-maintainers:
    # GitHub IDs for maintainers of the recipe.
    # Always check with the people listed below if they are OK becoming maintainers of the recipe. (There will be spam!)
    - pettyalex
