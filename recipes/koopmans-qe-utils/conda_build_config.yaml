# conda-forge's global pin is openmpi=4. We need openmpi=5 to pull in
# the qe 7.5 builds tagged for openmpi 5 (`_1`, `_2`). The openmpi-4-only
# qe 7.5 build (`_0`) on osx-64 ships a broken `libgfortran 5.*` pin
# that no modern libgfortran satisfies, so we must avoid it.
openmpi:
  - '5'
