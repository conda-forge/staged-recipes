{% set name = "mkdocs-quiz" %}
{% set version = "1.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/mkdocs_quiz-{{ version }}.tar.gz
  sha256: 1239e3e1edf443112a037218cbcfc91b7d07519d8dd9d58313d8b92793f5a7d3

build:
  noarch: python
  entry_points:
    - mkdocs-quiz = mkdocs_quiz.cli:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=61.0
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - mkdocs >=1.0.0
    - importlib-resources >=5.0

test:
  imports:
    - mkdocs_quiz
  commands:
    - mkdocs-quiz --help
  requires:
    - python {{ python_min }}

about:
  home: https://ewels.github.io/mkdocs-quiz/
  doc_url: https://ewels.github.io/mkdocs-quiz/
  dev_url: https://github.com/ewels/mkdocs-quiz
  summary: A MkDocs plugin to create interactive quizzes in your markdown.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ewels
    - skyface753
