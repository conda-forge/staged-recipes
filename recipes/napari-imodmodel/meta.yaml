{% set name = "napari-imodmodel" %}
{% set version = "1.0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari_imodmodel-{{ version }}.tar.gz
  sha256: 2fe52c0cd4fcf0eaa390906ca58d510810646a5b28b948c4eaf9fa6671eada8a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - setuptools >=42.0.0
    - wheel
    - pip
  run:
    - python >=3.8
    - numpy
    - imodmodel

test:
  imports:
    - napari_imodmodel
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/MoritzWM/napari-imodmodel
  summary: Open IMOD model files in napari
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
