{% set name = "causalimpact" %}
{% set version = "0.1.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/causalimpact-{{ version }}.tar.gz
  sha256: 83e97fc5f98a8008f34a5b949170cbb6c4e76adc8a94e4229e31e314d90de8b4

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python
  run:
    - importlib-metadata  # [py<38]
    - python

test:
  imports:
    - causalimpact
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/jamalsenouci/causalimpact/
  summary: Python Package for causal inference using Bayesian structural time-series models
  dev_url: https://github.com/jamalsenouci/causalimpact/
  license: Apache
  license_file: license.md

extra:
  recipe-maintainers:
    - jamalsenouci
