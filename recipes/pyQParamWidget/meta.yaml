{% set name = "pyQParamWidget" %}
{% set version = "0.0.8" %}
{% set sha256 = "fa7e1287438b226cc2e6bc61473e75cd24c9331a9076360e0286b41adb0e3138" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyQParamWidget-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10
    - setuptools >=64.0
    - setuptools-scm >=8.0
    - pip
  run:
    - python >=3.10
    - pyqt

test:
  imports:
    - pyQParamWidget
  commands:
    - pip check
  requires:
    - pip

about:
  summary: A PyQt5 QWidget and QDialog to edit application parameters.
  home: https://prjemian.github.io/pyQParamWidget/
  license: CC-BY-4.0
  license_file: pyqparamwidget/LICENSE.txt

extra:
  recipe-maintainers:
    - prjemian
