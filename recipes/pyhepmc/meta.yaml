{% set name = "pyhepmc" %}
{% set version = "2.13.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyhepmc-{{ version }}.tar.gz
  sha256: 5570b451eff4d11962b4f6155f5157eda5427c9db0f8193c644043ce3fc6af73

build:
  skip: true  # [py<38]
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - python >=3.8
    - setuptools >=46.4
    - setuptools-scm >=6.2
    - cmake >=3.13
    - wheel
    - pip
  run:
    - python >=3.8
    - numpy

test:
  imports:
    - pyhepmc
  commands:
    - pip check
  requires:
    - pip

about:
  summary: Pythonic interface to the HepMC3 C++ library licensed under LGPL-v3.
  license: BSD-3-Clause-flex AND LGPL-3.0
  license_file:
    - LICENSE
    - extern/HepMC3/COPYING

extra:
  recipe-maintainers:
    - cvazquezs
