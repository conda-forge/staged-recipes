{% set name = "quantum-kite" %}
{% set version = "1.1.3-a" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://gitlab.com/ejh516/kpm_transport/-/archive/master/kpm_transport-master.tar.gz
  sha256: 45ea359a69332a18a13d5376fd69a2750e85ea56daf65353e02f8880ffd80561

build:
  number: 0
  skip: true  # [not linux]

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake
    - make
  host:
    - llvm-openmp  # [osx]
    - libgomp  # [linux]
    - hdf5
    - eigen
    - pip
  run:
    - pybinding
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - kite
  commands:
    - test -f $PREFIX/bin/KITEx  # [unix]
    - if not exist %LIBRARY_BIN%\\KITEx exit 1  # [win]
    - test -f $PREFIX/bin/KITE-tools  # [unix]
    - if not exist %LIBRARY_BIN%\\KITE-tools exit 1  # [win]


about:
  home: https://quantum-kite.com
  license: GPL-3.0-only
  license_family: GPL

  license_file: LICENSE.md
  summary: 'High-performance quantum transport software'

  description: |
    Quantum KITE is an open-source software for accurate real space evaluations
    of electronic structure and response functions of large-scale tight-binding
    models.
  dev_url: https://gitlab.com/ejh516/kpm_transport
extra:
  recipe-maintainers:
    - ejh516
