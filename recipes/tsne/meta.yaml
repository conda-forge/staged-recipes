{% set name = "tsne" %}
{% set version = "v0.1.9" %}
{% set sha256 = "1d41ebfc24494f949d87bd137eb68760179db80f6b70ac7be309dc93f9a2b296" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/WiscEvan/tsne/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  skip: true  # [py3k and py<37]
  skip: true  # [win]
  script: {{ PYTHON }} -m pip install . --ignore-installed --no-deps -vv

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - python
    - distro
    - pip
    - setuptools
    - cython
    - numpy
    - openblas
    - libcxx
  run:
    - python
    - {{ pin_compatible('numpy') }}
    - libopenblas
    - scipy

test:
  imports:
    - tsne
  requires:
    - pytest
    - scikit-learn
  commands:
    - python -c "from tsne import bh_sne"
    - py.test --pyargs tsne

about:
  home: https://github.com/danielfrg/py_tsne
  license: Apache-2.0
  license_file: LICENSE
  description: TSNE implementations for python
  summary: "Python library containing T-SNE algorithms"
  doc_url: https://pypi.org/project/tsne/
  dev_url: https://github.com/danielfrg/py_tsne

extra:
  recipe-maintainers:
    - danielfrg
    - WiscEvan
