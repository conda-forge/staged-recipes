diff --git a/application/main.py b/application/main.py
index aa0ea05..31a2dae 100644
--- a/application/main.py
+++ b/application/main.py
@@ -531,29 +531,27 @@ def update(force):
         write_latest_version_fetch(latest_version.base_version)
 
         if latest_version > current_version:
-            with yaspin(text=f"Updating to v{latest_version}", timer=True) as sp:
-                result = subprocess.run(
-                    ["pipx", "install", "pocker-tui", "--force"],
-                    text=True,
-                    capture_output=True,
-                    check=True,
-                )
-                sp.ok(text="[DONE]")
-                if "installed package pocker-tui" in result.stdout:
-                    click.echo(
-                        f"Pocker updated from{Fore.LIGHTYELLOW_EX} v{current_version}{Style.RESET_ALL}"
-                        f"{Fore.LIGHTGREEN_EX} -> v{latest_version}{Style.RESET_ALL}"
-                    )
-                    update_changelog()
-                    read_changelog(current_version)
-                    return
+            click.echo(
+                f"📦 {Fore.CYAN}A new version of pocker-tui is available:{Style.RESET_ALL} "
+                f"{Fore.LIGHTYELLOW_EX}v{current_version}{Style.RESET_ALL} → "
+                f"{Fore.LIGHTGREEN_EX}v{latest_version}{Style.RESET_ALL}\n"
+            )
+
+            click.echo(
+                f"{Fore.MAGENTA}This installation was detected as coming from conda-forge.{Style.RESET_ALL}"
+            )
+            click.echo(
+                f"Please update using your conda environment, for example:\n"
+                f"  {Fore.GREEN}conda update pocker-tui{Style.RESET_ALL}\n"
+            )
 
-                print("An error occured!")
-                print("STDOUT: ", result.stdout)
-                print("STDERR: ", result.stderr)
+            update_changelog()
+            read_changelog(current_version)
+            return
 
         print(
             f"{Fore.LIGHTGREEN_EX}Already running latest (v{latest_version}){Style.RESET_ALL}"
         )
+
     except Exception as e:
         print(f"Error during update: {e}")
diff --git a/application/util/helper.py b/application/util/helper.py
index 6a1e49a..007192f 100644
--- a/application/util/helper.py
+++ b/application/util/helper.py
@@ -26,18 +26,13 @@ def get_latest_version():
         return None
 
 
-def get_current_version():
-    try:
-        result = subprocess.run(
-            ["pipx", "list", "--short"], text=True, capture_output=True, check=True
-        )
 
-        for line in result.stdout.splitlines():
-            if "pocker-tui" in line:
-                version = line.split()[1]
-                return version
 
-    except subprocess.CalledProcessError:
+def get_current_version():
+    from importlib.metadata import version, PackageNotFoundError
+    try:
+        return version("pocker-tui")
+    except PackageNotFoundError:
         return None
 
 
