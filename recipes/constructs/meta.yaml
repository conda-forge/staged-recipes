{% set name = "constructs" %}
{% set version = "10.1.19" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

build:
  skip: true  # [py<37]
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/constructs-{{ version }}.tar.gz
  sha256: 6985980b0ccb18e735902a245fc2c2bd41b0806ba1c81f53646c0adade0cfdcb

requirements:
  host:
    - pip
    - python
  run:
    - jsii >=1.59.0,<2.0.0
    - publication >=0.0.3
    - python

test:
  imports:
    - constructs
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/aws/constructs
  summary: A programming model for software-defined state
  dev_url: https://github.com/aws/constructs.git
  license: Apache-2.0
  license_file: NOTICE

extra:
  recipe-maintainers:
    - manics
