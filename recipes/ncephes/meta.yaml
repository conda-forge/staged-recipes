{% set name = "ncephes" %}
{% set version = "1.0.8" %}
{% set sha256 = "b0d4e57c59d86c22c83c3996a88ada5eabe534c64810a1be909da14e7c1f98fe" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - pytest-runner
    - build_capi
    - cffi
    - pycparser
    - toolchain
    - llvmdev
  run:
    - python
    - cffi

test:
  requires:
    - pytest
  imports:
    - ncephes
  commands:
    - python -c "import ncephes; ncephes.test()"

about:
  home: https://github.com/Horta/ncephes
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Python interface for the Cephes library'
  doc_url: http://ncephes.readthedocs.io
  dev_url: https://github.com/Horta/ncephes

extra:
  recipe-maintainers:
    - Horta
