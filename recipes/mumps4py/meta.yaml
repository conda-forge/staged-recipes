{% set name = "mumps4py" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}


source:
  url: https://github.com/imadki/mumps4py/archive/refs/tags/{{ version }}.tar.gz
  sha256: 1d6a72a4d8a2d034bfbd80c197f0a8c8630bf2a0423f331b4022c6b84fded5a9


build:
  number: 0
  script: |
    set MUMPS_INC=%PREFIX%\Library\include                                                                        # [win]
    set MUMPS_LIB=%PREFIX%\Library\lib                                                                            # [win]
    set MUMPS_SOLVERS=dmumps,cmumps,zmumps,smumps                                                                 # [win]
    set PATH=%PATH%;%MUMPS_LIB%                                                                                   # [win]
    export MUMPS_INC="$PREFIX/include"                                                                            # [osx or linux]
    export MUMPS_LIB="$PREFIX/lib"                                                                                # [osx or linux]
    export MUMPS_SOLVERS="dmumps,cmumps,zmumps,smumps"                                                            # [osx or linux]
    export DYLD_LIBRARY_PATH="$MUMPS_LIB:$DYLD_LIBRARY_PATH"                                                      # [osx]
    pip install . --no-deps -vv --global-option="build_ext" --global-option="--inplace"                           # [osx or linux]
    pip install . --no-deps -vv --config-settings="--build-option=build_ext" --config-settings="--build-option=--inplace" --config-settings="--build-option=--compiler=msvc"   # [win]
  skip: True  # [py<39]


requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
    - {{ compiler('cxx') }}
    - cmake
  host:
    - cython
    - python
    - mumps-seq      # [win]
    - mumps-mpi      # [osx or linux]
    - mumps-include  # [osx or linux]
    - libscotch      # [osx]
    - numpy
    - setuptools
    - mpi4py
    - pip
  run:
    - cython
    - python
    - mpi4py
    - numpy
    - mumps-seq      # [win]
    - mumps-mpi      # [osx or linux]
    - mumps-include  # [osx or linux]
    - libscotch      # [osx]

test:
  source_files:
    - tests
  imports:
    - mumps4py
  requires:
    - cython
    - mumps-seq      # [win]
    - mumps-mpi      # [osx or linux]
    - mumps-include  # [osx or linux]
    - libscotch      # [osx]
    - python
    - mpi4py
    - numpy
    - pytest
  commands:
    - pytest tests/


about:
  home: https://github.com/imadki/mumps4py
  license: MIT
  summary: 'Python Interface for the MUMPS Solver'
  license_file: LICENSE

extra:
  recipe-maintainers:
    - williampiat3
